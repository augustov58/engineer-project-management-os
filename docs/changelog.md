# Change log

One row per slice, moved out of [CONTEXT.md](../CONTEXT.md) on 2026-09-01 so that the file every
session reads first stays small. No row was rewritten. Append a row here when a slice lands.

| Date | Change |
|------|--------|
| 2026-09-01 | Slice 17 built (issue #18): **project memory** — the curated prose per project, versioned, with the agent proposing edits and the engineer confirming, and `AgentRunService` standing up the Pi port ADR-0002 requires. The sixth record type built on thin prior authority: the PRD sketch's `project_memory` line and three glossary entries written for the deferred copilot. ADR-0040 records what it settled: there is **no identity table** — a project has exactly one memory, so the project is the identity and the versions hang off it directly, where `documents` needed the split because a job has many; the proposal **is** the approval request, refusing the sketch's `approval_requests` as a second record of the same question, with the base content snapshotted at the write so the diff under review cannot drift as memory moves underneath it (ADR-0027's snapshot shape); the proposal is never edited — accept-with-edit writes the *version* from the engineer's text and keeps the agent's words on the proposal, ADR-0034's two-facts transcript posture for a second record; a run is a row with the four stamps and no kind column, produces at most one proposal (`run_id` unique), and has **no retry route** for ADR-0035's reason — every input is still in the database, so asking again is another row; the **size budget** (4,000 characters) rides on every memory read and is surfaced with a meter that pushes back, never enforced, because what is worth keeping is the engineer's judgement; the domain tools call the internal API over HTTP and never the database, registered with **underscored names** because the provider APIs reject the PRD's dotted spelling (`memory.propose_edit` survives only as the display label), and the session's tool list is an **allowlist**, so bash, edit and write are absent by construction rather than denied, with the remaining file tools scoped by `cwd` to a per-project workspace; the SDK is imported lazily so no test process ever loads Pi, the default adapter refuses with "no model provider is configured" (the `Transcriber` posture — there is no offline stand-in for a model), and `AGENT=pi` builds the real one; and every memory mutation writes an `audit_entries` row in the same transaction, append-only by construction and scoped to memory — widening it is its own change. The acceptance criterion "tests substitute `AgentRunService` and assert no proposal commits without confirmation" is the test named for it: the run finishes, the proposal is pending, and the memory is exactly what the engineer last wrote. 472 tests across 17 files, up from 452. Verified in the browser end to end — write directly, ask (fails honestly with no provider configured), a proposal seeded through the tool's own route showing as a diff, edit-and-accept committing the corrected text with the agent's words still standing, the audit growing by one line per mutation — because neither `pnpm typecheck` nor `pnpm test` sees the frontend, and `pnpm --filter web build` is still failing on the pre-existing `components/ui/field.tsx` errors recorded in slice 12's note, which this slice also leaves alone. The agent-browser CLI's synthetic clicks did not trigger React state updates reliably during verification; programmatic `.click()` and `requestSubmit()` did, and the flows themselves are confirmed working. |
| 2026-08-17 | Planning complete. PRD, architecture, ADRs, and glossary finalized. |
| 2026-08-18 | Code workspace created. Development not yet started. |
| 2026-08-24 | Grilling session: 8 superseding ADRs (0012-0019), glossary rewritten to real field vocabulary, MVP resequenced. Money model, health score, RBAC, and vector search cut. |
| 2026-08-24 | Slice 1 built (issue #2): pnpm monorepo, Fastify API, Next.js frontend, PostgreSQL + Prisma migrations, Redis + BullMQ idle, and the test harness every later slice copies. ADR-0021 (Fastify) and ADR-0022 (`TimeSource`) recorded; the PRD stack line no longer says "Fastify or NestJS". |
| 2026-08-25 | Slice 3 built (issue #4): the **open item** — create against a project, resolve with a note and a date, reopen, and the cross-project **pending items** view, filterable by who owes the next move and sorted by age. ADR-0024 records the three things the PRD's data-model sketch left out: a column for what is unresolved, a resolution model, and `subject_type` as a one-value enum. Glossary gained **Counterfactual**, **Waiting on**, **Owner**, **Resolved** and **Pending items**, none of which had an entry. |
| 2026-08-25 | Slice 2 built (issue #3): the `Project` record — create, list live, view one, archive one-way — behind a `/v1` prefix (ADR-0023). `skeleton_records` dropped, as slice 1 said it should be. Glossary gained **Project number** and **Archived**, which had no entry anywhere, and the stale **Project** and **Milestone** definitions were corrected. |
| 2026-08-24 | Full-MVP spec written and published as GitHub issue #1 (`ready-for-agent`), covering all 7 workflows and all 6 sequence steps. Vault updated with what the spec decided: ADR-0020 (Proposed), provisional split into historical + derived, ball-in-court as a history, report renders to PDF, transcription vendor added to the open picks. Two vault contradictions surfaced and recorded, not resolved. |
| 2026-08-25 | Slice 6 built (issue #7): **reissue and supersede**. Correcting an issuance records a new submission carrying `supersedes_id` and writes nothing at all to the one it replaces, so "no path edits an issued submission" holds because no such route exists — a test asserts `PATCH`, `PUT` and `DELETE` on a submission are refused with the record unchanged after. ADR-0028 records the three things left open: *superseded* is derived from a successor existing rather than stored, which settles the spec's "never edited beyond being marked superseded" against ADR-0026 in the vault's favour; "at most one successor, and the chain is linear" is a unique constraint on the column rather than a guard; and exposure excludes superseded ancestors, a filter ADR-0027 did not authorise and which carry-forward makes necessary. What the superseded set rested on comes forward by default and is editable before committing, with the successor's own snapshot stamped fresh. Glossary **Reissue** gained what was built; **Submission** and **Exposure** were amended. A frontend bug found by loading the page: a state update made from a ref during the hydration commit is discarded, so the "going out on N unresolved items" warning claimed nothing while two boxes sat ticked. |
| 2026-08-25 | Slice 5 built (issue #6): **provisional state** and **exposure**. Provisional is two facts and the record now keeps both — `issued_provisional` stamped at the moment of issuance and never recomputed, and *currently provisional* derived on every read and stored nowhere — so resolving everything a set rested on takes it out of exposure without unsaying that it went out on unconfirmed inputs. ADR-0027 records the three things neither the sketch nor ADR-0026 settled: the snapshot is one nullable boolean on `submission_open_items` carrying three real states, not two columns admitting a meaningless fourth; detaching is narrowed to items attached after the issuance, which settles the collision ADR-0026 flagged for this ticket rather than moving the snapshot off the table; and exposure is a list whose length is the count, so clicking a count lands on exactly the records it counted and there is no figure in the payload to combine into a score (ADR-0016). Archived projects leave the across-every-project count and keep their own, following the glossary's line under **Pending items**. Glossary **Provisional** and **Exposure** both gained what was built. |
| 2026-08-25 | Slice 4 built (issue #5): the **submission** — a dated issuance to a named recipient at a phase, with sheet list and revision — and per-project **phases**, defined, renamed and reordered as free text, with a current phase a new submission defaults to. ADR-0026 records the two things the sketch left out: what an issuance rests on is a `submission_open_items` join rather than a second subject on the open item, because reading ADR-0024's enum expectation literally would have made issue #7's carry-forward destroy the record of what the original rested on; and a phase is a foreign key, so a rename propagates on purpose. No route updates a submission, so issue #7's edit prohibition holds by construction. Glossary gained **Current phase**, **Sheet list**, **Revision** and **Rests on**. |
| 2026-08-27 | Slice 7 built (issue #8): **assumption records** — the `ASSUMPTIONS` and `FLAGS / VERIFY` blocks captured verbatim from a helper skill, with the code edition and the date, bound to the submission they justified. The first record type no ADR said anything about: the whole prior authority was two glossary sentences and the five-word sketch `assumption_records (assumptions, flags, counterfactuals, code_edition, dated)`, which has no submission foreign key at all. ADR-0029 records the four things it left open: the blocks are text columns holding exactly what was pasted and an entry of either is addressed by its **line number**, because splitting them into rows at capture is the transcription by hand the record type exists to avoid and the sigils the three calculators print are their convention rather than a contract; `counterfactuals` is rows keyed by the line they are about, because the spec asks for one per assumed input and one field cannot say which input each belongs to; a record is bound to one submission and nothing edits or deletes one, so a rerun is another record dated its own day; and a flag raised as an open item is attached *after* the issuance, which makes the set currently provisional and puts it into exposure while leaving `issued_provisional` exactly as it was stamped. Glossary **Assumption record** gained what was built and had "from a calculation" reconciled to the PRD's "from a helper skill"; **Counterfactual** gained its cardinality on this record. Verified in the browser end to end, because neither `pnpm typecheck` nor `pnpm test` sees the frontend. |
| 2026-08-27 | Slice 8 built (issue #9): **site visits and observations** — one dated observation event against a building, with a start, a nullable end and a per-floor schedule, producing observations that carry what was observed, when, and where. The second record type no ADR said anything about: the prior authority was four glossary sentences and the two-word sketch `site_visits`, `observations`, with no columns at all, and the location grammar written down in exactly one place in the whole vault. ADR-0030 records the four things it left open: the location is four columns with `Floor N — <qualifier>, <Side|Sector>` rendered on every read, because a single string would dissolve two independent axes into one; exactly one of side or sector is set, with *neither* refused as well as both since the grammar has no optional segment, enforced at the boundary and again by a CHECK constraint; the floor is free text holding the designation without the word, because buildings have B1, M and PH that an integer could not record; and a walk exists before it is over, so the end is nullable and the visit's date is the day of its start, derived. Nothing makes an observation a finding — no status, no category, no promotion route — so the non-issues table stays the default path and issue #10 will point at an observation rather than write a column on it; a test asserts the exact key set so it cannot be added quietly. Glossary **Site visit**, **Observation** and **Location** all gained what was built. Review caught the column named `note`, a word the glossary's `_Avoid_` list strikes for this record, with a comment above it quoting the rule it broke; renamed to `observed` before it reached a deployed database. Verified in the browser end to end, twice, because neither `pnpm typecheck` nor `pnpm test` sees the frontend. |
| 2026-08-27 | Slice 9 built (issue #10): **issues with stable per-project identifiers** — an observation becomes a finding carrying a category from the closed set of five, an identifier scoped to the job, every later sighting of it, the open items being chased for it, and a close that takes the note that closed it. The third record type no ADR said anything about: the whole prior authority was one glossary sentence and the five-word sketch `issues (stable per-project ID, category, location)`, and the glossary defines **Issue** twice in two incompatible senses, the field-finding one being what the MVP spec resolves it to. ADR-0031 records the five things it left open: the identifier comes off `projects.issues_allocated`, a **high-water mark and not a count**, read and incremented inside the transaction that writes the issue, because `MAX(number) + 1` and `COUNT(*) + 1` both hand the same number out twice the moment a row goes away while "never reused or renumbered" is the property being promised — the counter is stripped from the project on the wire and a test asserts the exact key set, since what a project's issues *are* is the list whose length is the count, the shape ADR-0027 gave exposure; an issue **owns no content**, so the `location` the sketch named on it and the summary nobody asked for are both absent and what was seen, when and where is read through the sightings, one re-observed on three walks having three of them; a sighting is a row with `observation_id` unique, which stops a double tap burning an identifier that can never be given back, and those rows are the whole of the history rather than a per-visit status, which would be the `tasks` record the data model excludes arriving by the back door; closing is `closed_at` plus `closure_note` moving together, ADR-0024's shape and for its reasons, with reopening clearing both and a second close refused rather than repeated; and an open item raised on a finding is the `issue_open_items` join with the item's subject still `PROJECT`, which **overrules ADR-0030's prediction** that story 69 would need a second value in `OpenItemSubject` — under that reading the item would reach the pending items view with no job beside it, the opposite of what the story asks, and nothing in that view had to change for it to work. The category is text with a CHECK naming the five rather than a database enum, because a Prisma enum member cannot be named `Physical / Safety` and the real words would then live in a lookup in the API and again in the frontend. Nothing deletes an issue and no route renumbers one; a test asserts `PATCH`, `PUT` and `DELETE` on one are all 404. Glossary **Issue** gained what was built; the duplicate agent-system **Issue** entry and the sketch's `issues_log` beside `risks` are left unreconciled, as ADR-0031 records — whoever settles ADR-0011 has to find another name or another table. Review caught three things the hand pass had not thought to walk: `issues_allocated` reaching the wire from the one project-returning route that did not strip it, a bad issue number in a URL rendering an error page where every other bad URL renders a 404, and a finding closed since a walk still reading as open on that walk. All three fixed, and the helper that strips the counter renamed `projectOnTheWire` — it is the only one in `server.ts` that removes a field where `withDate`, `withLocation` and `withSightings` all add one, and reading as a converter is part of why the miss was easy. Verified in the browser end to end — record as an issue, re-observe from a later walk, raise an open item that then appears in the pending items view carrying its job, close with a backdated date, reopen — because neither `pnpm typecheck` nor `pnpm test` sees the frontend. |
| 2026-08-28 | Slice 10 built (issue #11): **photo binning** — a photograph added to a walk, binned to a floor by its timestamp against the per-floor schedule and bound to a finding by the filename grammar already in use, either, both or neither, and both corrected in one action. The fourth record type no ADR said anything about, and the first whose blocker was not a design question but **a fact about the world nobody had written down**: ADR-0031, the glossary and the schema's own doc comment all recorded, in the same words, that the filename grammar was described in ADR-0018 as "already in use" and written down nowhere, and all three refused to invent it. It was asked for and supplied, and ADR-0032 is where it now lives — `issue` or `iss`, then the number, separated by a hyphen, an underscore, a space or nothing. **A marker is required and a bare integer never counts**, which is the load-bearing half: those filenames carry the floor as well as the finding, so `3-west stair-issue-12.jpg` opens with a bare `3`, and reading any integer as an identifier would bind every photograph taken on floor 3 to issue 3 — a mechanism wrong about most of a hundred photographs is worse than one that binds none, because then all hundred have to be checked. Six more things ADR-0032 settled: a binding is a **column stamped when the photograph is added**, breaking this product's own derive-on-read reflex, because a derived value has nowhere to keep a correction and a floor time fixed the next morning would silently move photographs between floors; a photograph binds to a floor only if **exactly one** window contains its timestamp, which extends the ticket's "outside every window is left unbound rather than guessed at" to the two-window case the schedule actually produces when an engineer doubles back, with an uncompleted floor's window staying open so the last floor of a walk still bins; `taken_at` is required and never falls back to the injected clock, since that fallback is the same guess under another name, and nothing reads EXIF; photo evidence lands on the **floor and the finding and not on the observation**, correcting a promise the glossary's **Observation** entry had carried since slice 8, because no mechanism picks one observation out of the dozen made on a floor; the bytes go to an `ObjectStore` port — the shape ADR-0022 gave `TimeSource` — with a filesystem adapter now and S3 when there is somewhere to deploy to, and are served **through the API rather than a presigned URL**, which would be a second thing reachable without ADR-0020's still-Proposed edge gate; and binning runs in the request rather than on BullMQ, a deliberate drift from the PRD diagram and the spec stack line, because it is date comparison and one regular expression. Glossary gained **Photo** and **Photo evidence**, and **Observation** was corrected. Object storage was on no open-vendor list anywhere despite the stack saying "S3-compatible" since ADR-0003; it is on both now. Review caught five things the hand pass had not reached, three of which the first browser pass *could not* have reached because it used a 116-byte PNG: the Next byte proxy interpolated a caller-controlled path segment raw, so `/photos/..%2Fexposure%23/bytes` returned the exposure register — an open GET proxy for every route on an API deliberately bound to loopback, reproduced at 200 and 3994 bytes and reproduced again against the fix; the form could not add a real photograph at all, because a server action's body defaults to one megabyte, and raising it was only half the fix since the whole selection went in one body and a hundred photographs would have been one 300 MB request, now 16mb and one request per file; the grammar missed the underscore names it claims to accept, `\b` counting `_` as a word character so `photo_issue_4.jpg` bound to nothing; a filename number above int32 — `ISS-20260723131500.jpg`, an ordinary messaging-app name — 500'd the add and **lost the photograph**, the opposite of "left unbound rather than guessed at" in the one place that rule is load-bearing; and a zero-byte file in a selection shifted every later timestamp by one, binning photographs to the wrong floor and returning 201. Four quality fixes with them: the object store's `put` moved out of the transaction that writes the row, since a network write inside an interactive transaction blows Prisma's five-second timeout and rolls back a row whose object already stored; `FilesystemObjectStore` resolves its root, a trailing slash in `OBJECT_STORE_DIR` having made the guard refuse every write to its own store; a browser that reports no type for a HEIC gets it from the extension; and the duplicated read shape became one named const. Two findings were deliberately not fixed and are recorded in ADR-0032: the floor select is narrower than the column it writes, offering the walk's scheduled and observed floors and no free-text box; and `ObjectStore.get()` lets an ENOENT out raw, the one error shape in the slice that is not this product's `{message}`. Verified in the browser end to end — three photographs added at once, binding to both, to a floor only and to a finding only, one re-binned from a single select change, then re-verified after the fixes with real 11.11 MiB and 9.53 MiB JPEGs, which is what the first pass could not reach — because neither `pnpm typecheck` nor `pnpm test` sees the frontend. |
| 2026-08-29 | Slice 11 built (issue #12): **voice capture** — the engineer speaks instead of typing, the audio goes to object storage, a queued job asks a transcription vendor what was said, and what comes back is a **draft** read and corrected before an observation exists. The fifth record type no ADR said anything about, and the one with the thinnest prior authority of any of them: the glossary had no entry for Voice capture, Transcript, Draft, Audio or Progress, and the data model sketch named **no table for it at all** — not a thin line, no line. ADR-0034 records five decisions. The **draft is a record of its own and not a state of an observation**: the ticket's own words read as a column on `observations`, which ADR-0030 forbids and built a test to keep forbidding, so committing writes an ordinary observation and stamps `observation_id` on the capture — slice 9's shape, where promotion writes nothing at all to the row it points at. The **transcript is verbatim and a correction never rewrites it**: the corrected words are the observation's and both facts are kept, which is what makes story 52's "transcription error never becomes record error" checkable afterwards rather than merely asserted; nothing parses a transcript, which is ADR-0029's posture toward a calculation's output and ADR-0032's toward EXIF, and it is why `AgentRunService` is not in this slice despite ADR-0004 and the PRD's `observations.create_draft` putting it here. The **state is four stamps and no status column**, derived on read, for the reason `resolved_at` and `closed_at` have none, with a retry clearing the failure the way reopening an issue clears its close. **This is the first thing that ever enqueues** — BullMQ has been wired and idle since slice 1, and ADR-0032's "it is date comparison and one regular expression" does not carry to a vendor call of unbounded duration; that sentence is now spent rather than a general licence. The worker runs in the API's process from the same injected dependencies, so the harness runs a real worker over the real Redis instead of a second copy of the wiring. And a **resend after a signal drop is answered with the row rather than refused**, the one place this slice departs from its own precedent on purpose: a photograph's duplicate filename is refused because there the refusal *is* the answer, but a refusal here cannot tell the phone whether the first attempt landed, so it would keep the recording forever or throw one away — and story 112 is about not losing one. The phone writes every recording to IndexedDB **before** sending and deletes it only once the API answers, retrying on load and on `online`. Progress is the state over SSE and never a percentage, polled from PostgreSQL rather than carried on a second transport, on a `reply.hijack()` route with no Fastify plugin so ADR-0023's single `register` call stays the only place a prefix could be added. "Leaves the audio recoverable" is three things and the third is load-bearing: the bytes stay in the store and are served through the API, retry queues it again, and **a failed capture can still be committed by hand** — a vendor that never answered must not stop the walk being written up. The vendor is still an open pick and sits behind a `Transcriber` port with **no adapter written**: the default refuses and says so, so a development run exercises the failure path honestly, and a labelled stub behind `TRANSCRIBER=stub` exists only so the review screen is reachable and is recorded in ADR-0034 as the weakest thing in the slice. `observationBodySchema` and `observationData` are exported from `routes/site-visits.ts` and used by both writers, which is what ADR-0030 asked for when it predicted this route and named the guard a later writer forgets; `ObservationFields` is shared by the typed form and the draft form for the same reason, with per-instance field ids because a walk with three drafts renders four copies at once. Verified in the browser end to end at `localhost` — recorded through a real `MediaRecorder` (232 KB of WebM, the microphone hardware stubbed by an oscillator the way a fake capture device would), sent, transcribed, the draft appearing without a reload, corrected and committed as `Floor 3 — Stair B, Side A` dated from the recording rather than the review; then a second recording against the refusing default, which read as failed with the vendor's own sentence, whose 84 KB of audio still came back through the proxy, which offered the draft form anyway, and which recovered to *awaiting review* live when asked again with a vendor present. The new byte proxy was checked against ADR-0032's open-GET-proxy hole and 404s a traversal. **Two things are recorded as gaps, not done**: `getUserMedia` needs a secure context, so recording does not work on a phone over `http://<address>:3000` and story 111 is not real until there is a deployed instance or a tunnel — the screen says so rather than failing silently; and the thin browser-driven pass the MVP spec authorises for exactly this path is still not stood up, so recording and poor-signal reconciliation are exercised at the record level and by hand, not by a test — which also means **there is no longer a later ticket to carry that pass**, and README's claim that only #12 was left to carry it was corrected rather than quietly left standing. Review found six more things, four of them defects the hand pass could not have reached. A capture stuck at *queued* was a dead end on the screen: the retry route accepts one and its own comment names that case — Redis has no volume and `queue.add` can throw after the row is written — but "Ask again" was offered only where there was a failure, so the one recovery the route exists for was unreachable, and the phone had already dropped its copy. A recording the API will always refuse — a type outside the closed three, a body over the cap — was held in IndexedDB and resent on every load and every `online` for the life of the device, with a banner that never cleared: story 112 asks capture to *reconcile*, and that never reconciled, so a 4xx now releases the recording and shows the message while anything else keeps holding it. The worker could write a second transcript over the first, defeating in fact the refusal the retry route makes in words, since two taps while queued enqueue two jobs; it is a compare-and-set now, and a job for a recording already answered returns without asking the vendor a second time. The SSE heartbeat could write after the response had ended — `preClose` ended the socket but left the timers running — which is `'error'` on a `ServerResponse` with no listener and takes the process down, so the hook now holds each stream's *stop* rather than its reply. And two narrower ones: the recorder invented `audio/webm` for a browser that reported no type, which would have stored audio as something it is not and served it back under `nosniff` as a recording that silently would not play — the empty string goes up instead and the API refuses it, which is the photo form's answer to a browser that cannot name a HEIC; and the record button was re-entrant, so a double tap in the window before `getUserMedia` resolved opened two microphone streams and left the first one's tracks live. Four quality fixes with them: the duplicated capture lookup in the add route became one helper, the four-state union is read through two named predicates rather than switched on in six places, the web proxy segment was renamed `captures` to `voice-captures` to match what the record is called everywhere else, and CONTEXT.md's own decisions table turned out never to have listed ADR-0033. Four findings were **not** acted on and are recorded as deliberate: the retry route, the stub adapter, the shared `ObservationFields` and `wall-clock.ts` were each flagged as scope beyond the ticket, and each stays — the retry route is what "leaves the audio recoverable" is made of, the stub is what makes the review screen reachable before a vendor exists, and the two shared pieces exist because the alternative was a second copy of the one-axis form and a second copy of the timezone helper, which is the drift ADR-0030 predicted by name. |
| 2026-08-29 | `apps/api/src/server.ts` split, as its own change between issues #11 and #12 rather than inside a slice. Four ADRs had recorded the deferral and grown the figure in it — 1,745 lines across seven record types in ADR-0029, then 2,228, then 2,779, then 3,246 across fourteen in ADR-0032 — each for the same good reason, that splitting inside a feature slice mixes two changes and a reviewer then cannot tell which one a failing test is about. ADR-0033 records the shape. A record type is a file under `apps/api/src/routes/`, named to match the test file that drives it, holding its schemas and its single-use helpers beside its routes; the alternative of splitting by layer was refused because it puts the schema, the route and the derivation three files apart and the reason they belong together survives in none of them. The obstacle was never size but that the records genuinely read each other — a site visit returns its photographs, a photograph is corrected against a walk and is evidence of a finding, a finding is read with the location of every sighting — which is `site-visits → photos → issues → site-visits`, and not the harmless kind, since `photosTaken` is a `const` reading `issueInclude` at module load. Three leaves resolve it: `http.ts` (the validation vocabulary, the one way a Prisma error is read, `RouteDependencies`), `refusals.ts` (every way the API says no) and `wire.ts` (only the read shapes two or more records return — `withDerivedState` and `withLines` are used by one each and stayed put). The route modules are plain functions and not Fastify plugins, because nine plugins would be nine more places a prefix could be added and ADR-0023's single-`register` guarantee would become a convention. Verified as a move rather than a rewrite: `tsc` passes, 282 tests pass, `app.printRoutes()` is byte-identical before and after, and a normalised line-multiset diff accounts for every line of the original — six are absent, the old combined Fastify import and a five-line banner explaining why the photo helpers sat above the issue helpers *in that file*. `server.ts` is now 76 lines; thirteen files, 3,476 lines, largest 728. Nothing about the API changed, so `apps/web` is untouched and there is no migration. Review found no cycle, no initialisation hazard and a clean dependency seam, and six things worth acting on: two single-record refusals (`alreadySuperseded`, `endsBeforeItStarted`) had been argued into `refusals.ts` by the change that wrote the warning against exactly that, and moved back; three section banners had ridden to the end of the *preceding* record's module, each now announcing a section in another file, and were deleted; and three new header claims were false — "one per test file" (there is no `phases.test.ts` and submissions has three), `http.ts` crediting itself with `wire.ts`'s acyclicity, and "every way this API says no, in one place" when thirty-two inline 4xx bodies remain. The one recommendation **refused** was hoisting the open-item schemas into `http.ts` to remove the two cross-module load-time schema reads: the argument for it was that one fails silently by handing ajv `properties: undefined`, and that is not what happens — a `const` in an ESM cycle raises `ReferenceError` at load, verified against the exact shape in both load orders, so the hazard is loud and a comment at each read naming the sink constraint is the proportionate answer. |
| 2026-08-29 | Slice 12 built (issue #13): the **site visit report** — a walk generated into the PDF that is issued outside the tool, with the metadata block, the per-floor schedule, the notable observations and every finding sighted on that walk, its stable identifier, its category and its photographs. `site_visit_reports`, `routes/reports.ts`, `report.ts` (the walk as HTML), `pdf.ts` (Chrome through puppeteer), `stream.ts` (a new shared leaf), and three routes: `POST /v1/site-visits/:id/reports`, `GET /v1/site-visit-reports/:id/pdf`, `GET /v1/site-visits/:id/reports/stream`. **Step 3 of the revised sequence is complete with it.** ADR-0035 records the decisions. A report is a record of a **rendering** and not a document kept current: nothing edits one, and generating again INSERTs another row — ADR-0028's reissue shape and ADR-0029's rerun shape arriving for a third record — which is why there is **no retry route** at all, the one place this slice departs from the voice capture it otherwise copies. A recording is retried in place because its audio is irreplaceable and the phone has already let go of it; a report's every input is still in the database, so a second attempt is a second row and the first keeps saying what happened to it. That is also the answer to story 66: a finding read as having no photograph gets one, and the report is generated again. Its state is **four stamps** derived on read — `rendering_since`, `rendered_at` + `storage_key`, `failed_at` + `failure`, queued being all four null — the fourth record asked for a status column and the fourth to refuse (ADR-0024, ADR-0031, ADR-0034), and nothing here is ever cleared, since clearing is what a retry in place needs. A report **owns nothing it prints**: metadata, schedule, observations and findings are all read at the moment of rendering and copied into no column, so it cannot come to disagree with the record it is a rendering of. Two questions ADR-0031 had deferred to this ticket **by name** are now answered: an issue's identifier prints as **`Issue N`**, the record's name and the integer, which invents nothing — it is ADR-0030's floor rule, where the column holds `3` and the render supplies the word, and it is what the filename grammar already carries in as `issue-7`, with `T-1-007` refused as ADR-0031 refused `T-12-003`; and a report prints **this walk's sightings, all of them**, the same `where` clause `GET /v1/site-visits/:id/issues-without-photos` already uses, which is ADR-0032's reasoning about evidence applied to locations ("July's photograph does not evidence August's re-observation"). The renderer is deliberately **not behind a port**, which departs from ADR-0022, ADR-0032 and ADR-0034: each of those ports defers a pick no test can exercise — a bucket that does not exist, a vendor account nobody has chosen — and Chrome is not a pick, needing no account, no key, no network and no per-call cost, with puppeteer pinning its own. A port would defer nothing and would cost the acceptance test its subject, since the ticket requires asserting on the resulting *document*; the test for a port is whether the thing behind it is a pick. Rendering is **on BullMQ**, ADR-0034's case rather than ADR-0032's, as one queue and a second job name dispatched on `job.name` inside `buildWorker`, concurrency still 1 — one browser printing at a time is the right number on one machine, and BullMQ is no longer carrying a single kind of job. `stream.ts` is a **new leaf**: ADR-0033 said a thing used by one record moves out when a second reaches for it, and a walk's reports are that second reader; the 24 voice tests pass unchanged against it. `storage_key` is **nullable**, inverting ADR-0032's bytes-before-row order because the queue now sits between the row and the document — the invariant itself is untouched, the key being written in the same statement as `rendered_at` and after the object is stored. There is no `content_type` column: a report is always `application/pdf`, and a column holding one value forever is a place for it to one day hold another. Three things the document itself taught, none of them guessable from the screen: **`letter-spacing` above about a tenth of an em destroys a PDF's text layer**, because Chrome emits every glyph as its own text run and a tracked-out heading prints as `N O TA B L E …`, unsearchable and uncopyable in the one artifact this product issues outside itself — measured, breaking at `0.11em` and fine at `0.09em`; photographs are inlined as **data URIs**, since the renderer must load one string and a linked `<img>` would need the API reachable from inside the process serving it, and are bounded to 70mm tall or a portrait phone photograph is a page each; and every value printed is **HTML-escaped**, this being the one place where what the engineer spoke becomes markup. `startTestApi({ worker: false })` substitutes nothing — it starts no worker, which is a state production has too — and is how *queued* became a state a test can stand in and look at. 324 tests pass across 13 files, up from 306. New dependencies in `apps/api`: `puppeteer` 25.9.0, with `pnpm-workspace.yaml` allowing its postinstall under `onlyBuiltDependencies` so it downloads its own Chrome, and `pdfjs-dist` 6.3.289 as a devDependency used only to read the rendered PDF's text back in the test. **Recorded honestly: `pnpm typecheck` and `pnpm --filter web build` were already failing before this slice**, on `apps/web/components/ui/field.tsx` — unused shadcn scaffolding from the Tailwind slice that no longer typechecks now that two `@types/react` resolve (19.2.2 pinned, 19.2.18 through radix's peer). Nothing imports it. Verified pre-existing by checking out a pristine tree and running `pnpm install --frozen-lockfile && pnpm -r typecheck`; not fixed here, because it is not this slice's change. It does mean this slice's frontend was verified by loading the pages against `pnpm dev` rather than by a production build. |
| 2026-09-01 | Review of slice 17 (issue #18), then a fix branch for what it found. The slice itself is sound — all eleven acceptance criteria met bar one, 472 tests passing, ADR-0040 and the vault genuinely updated. Two things were wrong and are fixed here. **The agent's file tools were never sandboxed**: ADR-0040 kept `read`, `grep`, `find` and `ls` and said `cwd` scoped them to a per-project workspace, but the SDK's own `resolvePath` uses the base directory only for a *relative* path — executed against the real resolver, `~/.pi/agent/auth.json` resolves to the SDK's credential store and `../../apps/api/.env` walks out to the repository, with no containment check in any of the four tools. A run could have read either and passed it to `memory_propose_edit`, where it would be stored, rendered and committed to memory on accept. The tools are removed rather than fenced, because a memory run reads no file and every fact it needs already arrives over HTTP; ADR-0041 records it. Latent rather than live — `AGENT` is unset, so no run has ever reached the adapter. **And slice 17's migration silently downgraded four foreign keys** from `ON DELETE RESTRICT` to `ON DELETE SET NULL` — `submissions.supersedes_id`, `photos.issue_id`, `register_entries.submission_id`, `register_entries.previous_round_id` — none of which that slice had any business touching. Prisma defaults an *optional* relation to `SetNull` and those four carry no explicit `onDelete`, so `migrate dev` rewrote the SQL to match the schema; the same commit added a doc comment asserting "every foreign key in this schema is RESTRICT on delete", which it had just made false, and slice 11 had warned about this exact hazard by name. Restored explicitly in the schema and in migration `20260901023535_restore_restrict_on_delete`. A full sweep of `pg_constraint` found **five** non-RESTRICT keys, not four: `projects.current_phase_id` was created `SET NULL` in its own migration and is deliberate, so its behaviour is untouched and the doc comment was narrowed to tell the truth instead. Three further findings from the review are recorded and **not** fixed here, being memory's own and not security: accept commits against its snapshotted base with no check that memory has not moved since, so a direct write between proposal and accept is silently dropped while the screen shows a diff against the older base; `reject` has no compare-and-set, so a concurrent accept and reject leave both stamps set and two contradictory lines in the append-only audit; and `readMemory` breaks a `created_at` tie on a random uuid, so "the current memory is the latest" is a coin flip for two versions written in the same millisecond, which the harness's frozen clock makes reachable. |
| 2026-09-01 | Slice 18 built (issue #19): **the ingest address and untrusted inbound mail** — change-log entry restored, the slice's own session having recorded the milestone row, the 0042 decision row and the vault updates but missed this table. See the 0042 row above for what it settled; ADR-0042 is the record. |
| 2026-09-01 | Slice 21 built (issue #22): **the edge gate** — one long-lived shared secret in front of every route, and the whole of this deployment's access control. This is the first slice whose **first acceptance criterion was a decision rather than code**: [[0020-single-secret-edge-gate]] had been *Proposed* since the MVP spec was written and the ticket refused to be built until the author confirmed it. Confirmed as written; the ADR is now Accepted and carries a new section recording what building it settled, which is where slices 18-20 would have written a new ADR. ADR-0012 removed **identity** and could not remove **access control** — no `users`, `roles` or `permissions` table is added here, and none is needed. **The ADR says *the* edge and this deployment has two**, so `apps/web` and `apps/api` each carry a gate against the same secret; a gate on one leaves the other reachable. The engineer's presentation is the **cookie** the ADR names, in `apps/web/proxy.ts` — Next 16 deprecated `middleware.ts` and renamed the convention — with `/unlock` taking the secret once and setting an HttpOnly, SameSite=Lax, year-long cookie. The API's is the `x-edge-secret` **header**, and that is not a second mechanism for one fact: a cookie is how a *browser* carries a credential and no browser reaches the API, which is the same invariant ADR-0032 refused a presigned URL over. Both compare `timingSafeEqual` over SHA-256 digests, so neither the comparison nor a length mismatch is a side channel. `EDGE_SECRET` is **required with no default** — `buildServer` takes it the way it takes `objectStore`, and a default is a way to run ungated by accident; `apps/api` guards it with the same `requireEnv` as `DATABASE_URL` and `apps/web` throws in `next.config.ts`, the only thing Next loads before it serves. Both refusals were verified by booting each app with the value emptied. **Three decisions went against the grain and are recorded rather than smoothed over.** `GET /v1/health` is **gated**, though a managed platform's HTTP check cannot present a header (Fly can be configured to; Render cannot) — one named exception is a property a test can hold and two is the start of a list, so a deployment uses a TCP check or configures the header, and the README says so. `/unlock` is exempted **inside the proxy function and never in the matcher**, because Next's own docs warn that a server action is a POST to the route it is used on, so a matcher exclusion silently un-gates that route's actions too. And a request without the cookie is redirected **only if it is a page navigation**; everything else gets a 401 where it stands, because an `EventSource` follows a redirect into an HTML page and then reconnects forever without ever showing anybody an error. The test is **exhaustive rather than representative**: the harness collects every route Fastify registers through an `onRoute` hook, and the sweep walks all **142** and asserts the allowed set is exactly `POST /v1/ingest/inbound-mail` — so a route added in a later slice is covered without anybody remembering. **Proven by removing the hook and watching 141 of the 142 answer.** 559 tests across 20 files, up from 553. Verified live in the browser: locked out of `/`, redirected to `/unlock?next=%2F`, a wrong secret refused in place, the right one unlocking and landing on the morning screen with live API data, and both SSE streams on a project page connecting through the gate with a clean console. Verified against the running API by curl: five routes 401 without the secret, a wrong and a truncated secret both 401, the ingest webhook reaching its own route, and a real `_next/static` chunk served at 200. **Not done, and recorded:** `pnpm --filter web build` still fails only on the pre-existing `components/ui/field.tsx` errors. Its cause is now diagnosed — prisma resolves `@types/react@19.2.18` as a **peer**, beside the 19.2.2 `apps/web` pins, so tsc reads the ambient `React` namespace from one copy and `import from 'react'` from the other. A pnpm `overrides` entry does not fix it, because overrides do not rewrite peer resolutions; that attempt was made, found not to work, and reverted rather than left as noise on this branch. The frontend was verified against `next dev` in a browser instead, which is what this repo has done since slice 12. Review (two-axis, /code-review) found **one blocking defect, and both axes found the same one**: of `apps/web`'s twenty-five outbound calls, twenty-four got the secret and `setProcessingLocation` did not — so slice 20's whole screen answered 401, and **nothing in the repo could have caught it**, since the API's sweep only tests anonymous callers and `apps/web` has no tests at all. It was found because the standards axis enumerated the call sites rather than trusting the pattern the original edit had grepped for; the missed one wrapped its URL onto its own line. Fixed, and then **the class was closed rather than the instance**: `apiFetch` is now the only thing that reaches the API, and `apiPath` is reduced to returning a *path and not a URL* so a second door cannot be built out of it — this product's own habit of holding an invariant by construction, applied where it had not been. Also fixed: the open-redirect guard on `/unlock?next=` refused `//elsewhere.example` but let `/\elsewhere.example` through, because `URL` normalises a backslash to a slash, so it now **resolves** rather than prefix-tests; two AGENTS.md rules the diff had made stale (`server.ts` "the boundary and nothing else" still listed three things, and the leaves list omitted `edge-gate.ts`); a route count written into prose that would go stale on the next route; and the harness handing out its live route array. **Found from a review note rather than a finding, and closed:** `pnpm dev` copies `.env.example` to `.env`, so a deployment that shipped the working tree would have run on the in-source development secret while passing every check — both apps now refuse that value when `NODE_ENV` is production, and the ADR records that `apps/web`'s half always fires (Next sets it) while `apps/api`'s only fires if the deployment does. **One judgement call declined and priced:** the header name and the refusal sentence are duplicated across the two apps where `refusals.ts` would ordinarily make them one — they are two packages with different module resolution, so sharing means a third workspace package, and a header-name drift is the loudest failure available rather than a silent one. Independently verified during review, against the running servers: `/_next/image` is **not** a bypass (its internal fetch of the exempt `/unlock` reached the app; its fetch of the gated `/pending` never did), RSC payload requests and server-action POSTs are refused 401, and `/unlock` renders no record data. The processing-location screen was re-driven in the browser after the fix, both directions, with the sign-off cleared and re-recorded. |
| 2026-09-01 | Slice 20 built (issue #21): **the processing location per project**, landing **inside the employer-consent gate without lifting it** for the fourth slice running. ADR-0044 records the decisions. It opens by **resolving the contradiction the vault has carried under "Open, deliberately" since 2026-08-24** — the ticket's own seventh acceptance criterion — in **ADR-0013's favour, on the author's explicit call**: cloud is the default and local is the per-project second option. Three things support it and are now written down. Decisively, ADR-0008 line 3 names *its* qualifier and it names 0013, while the glossary's `Processing location` entry claimed that same role in the same sentence and in the opposite direction — two documents qualifying 0008 and disagreeing, which nobody had noticed until this slice. The PRD agreed with 0013 twice already (lines 85 and 151), so the glossary was the outlier. And 0013's grounds — work machines cannot run local models — have not changed. A **third** formulation was found at PRD line 129, "the processing location agreed with the firm", which is neither default and circular besides; it is corrected too. The glossary entry is rewritten with the struck sentence preserved in 0044 rather than deleted, and 0013 needed no change and got none. **What the resolution cost is recorded rather than smoothed over**: under the glossary's reading "a project on cloud has a recorded sign-off" would have been a CHECK, and it cannot be one now, because a project reaches `CLOUD` by being created and the invariant is false by design. `processing_location` is a **database enum** on ADR-0036's test rather than ADR-0031's, both members naming themselves; the written CHECK holds the pairing alone (`reference IS NULL = at IS NULL`). **`LOCAL` is a permission and not a selector** — there is no local OCR and `OcrProvider` takes no project, so it means the port is never called and manual entry is the path; a two-adapter selector with one adapter was refused. The sign-off therefore gates the **switch** and not the state: switching to cloud requires the reference and the date and refuses a second, recording the first against a never-switched project is not a second, and **switching to local requires nothing and is always available** — consent can be withdrawn, which keeps ADR-0039's principle while inverting its mechanism. Going local **clears** the sign-off so returning needs a fresh one, safe only because the audit entry carries the reference and the date. `cloud_signoff_at` is supplied and never the `TimeSource`'s (ADR-0037's answer for `disposed_at`); when the switch happened is the audit row's. **The gate runs twice and both matter**, ADR-0042's rate-limit shape for a second reason: both extraction create routes refuse the ask before a row or a job exists, and the worker reads the setting again before the bytes are fetched — the half that is a bound, for the one case the routes cannot cover, a job already in Redis when consent is withdrawn. The **audit widens exactly once**, the change ADR-0043 said would be its own; extraction mutations still write nothing. 552 tests across 19 files, up from 508 — and **both halves of the gate were proven by deleting each and watching a named test fail**, the worker half driven through the real race rather than a mock of it (concurrency is 1, so holding the first run open at the agent seam keeps the second queued while the switch lands underneath it, with a call-counting OCR port standing in for a claim about a call that did not happen). Verified live in the browser and against the running API: default cloud, sign-off recorded through the form and shown, switched to local with the sign-off cleared from the project and standing in the audit, and the extraction refused 409 with the exact sentence. `pnpm --filter web build` still fails only on the pre-existing `components/ui/field.tsx` errors — an unused component, imported by nothing, that has broken the build since slice 12 and is left alone again. Review (two-axis, /code-review) found **one hard standard violation and one consequential spec finding, both fixed**. The standards axis caught that three documents — AGENTS.md, ADR-0044 and the glossary — asserted switching to local was "always available" while the route 409s the no-op on a project already local; the prose is narrowed in all three and the over-claiming test renamed, since the refusal is right and the sentence was wrong. The spec axis caught the sharper one: **ADR-0044 counted "the other six criteria are met" when criterion 2 is met only literally.** "Switching a project to cloud requires a recorded sign-off" has force exactly because local was the default; under a cloud default no project need ever be switched, so cloud processing with no recorded consent is the ordinary case — and this slice's own worker test demonstrates it, creating a project, recording nothing and watching bytes cross the port. The ADR now says that instead of counting it, which makes its risk paragraph and its tally agree. Also fixed: the document path refused for the wrong reason (the location 409 preceded the referenced-file 409, so a referenced file on a local job was told about the location); the sign-off form was twenty-five duplicated lines in two branches, now one `SignoffForm`; an untyped `let data` carrying three branches on the same discriminant, now one typed `LocationChange` built where the branch already knows it; the web action sent an explicit `null` where every sibling composer sends `undefined`; and `refusals.ts`'s header still claimed "more than one route module" and an inline count of thirty-two. One suggestion declined and recorded: routing the two extraction 409s through the `Refusal` shape would match the file's 404 idiom but not its 409s, and would make `worker.ts` read `.message` off a reply-shaped object. **Found independently of the review and fixed:** the route read the sign-off outside the transaction and wrote inside it, the read-then-write shape filed as issue #42 for memory — the write is now narrowed to the state each refusal was read against, as the archive route two above narrows its own. **Recorded rather than claimed:** that narrowing is unproven by this suite. A test firing twenty concurrent sign-offs pins the outcome (exactly one recorded, one audit row) but stays green with the narrowing removed, because these handlers never interleave between their read and their write in this harness; the test comment says so rather than implying a bound it does not demonstrate. Also recorded and not closed: a cleared sign-off survives only in the audit, which no path from the processing-location panel reaches — criterion 4 asks that the *current* setting be visible and it is, and a history view is its own change. |
| 2026-09-01 | Slice 19 built (issue #20): **extraction to a draft, human-confirmed** — the structured half of ingest, landing **inside the employer-consent gate without lifting it**, the pattern slice 18 established taken one step deeper. ADR-0043 records the decisions. An extraction is **one record** — `register_entry_extractions` carries the run's stamps, the proposal's thirteen fields and the resolution, with the seven states derived on every read and no status column, the shape a voice capture and a site visit report established. The source is **exactly one** of an arrival's file or a stored document's version, held by a CHECK so the ambiguity is not a value in a column, and the asking is **manual and per file** — a recorded narrowing of story 84's "automatically", because the engineer picks which file of an arrival is the correspondence and the covering note is not it. The run is a fourth BullMQ job dispatched on `job.name` at concurrency 1, and its ordering is what keeps the gate: the bytes are read, the OCR vendor is asked, its text is **stored on the row before the model sees it**, and only then does `AgentRunService.extractRegisterEntry` run — so a refusing OCR default fails the run honestly with the vendor's sentence and the row still says what happened. The agent's tool list is an allowlist naming `extraction_propose` and nothing else; the packet reaches the model inside `<<<UNTRUSTED DOCUMENT DATA` delimiters under an explicit non-instruction directive, so "ignore previous instructions" is data to read fields from, and the typed-shape constraint lives at the proposal route's body schema rather than in the prompt, which is a place a constraint can actually be held. **Confirming is the commit** and it commits everything at once: one transaction writes the document and its first version on the mail path (**reusing the arrival file's storage key** — the bytes are already in the store and a copy would be a second object nobody reads), the register entry, its first handoff and the join to the source version, with `confirmed_at` and `register_entry_id` stamped by compare-and-set so a double confirmation refuses. The register's boundary rules are re-stated rather than bypassed: an RFI still needs a question, a submittal still carries neither question nor response, and a number already in the register is still a conflict. **Rejecting keeps the source** exactly as it arrived and keeps the proposal on the record — the two facts the engineer will want when the same document comes back. The confirmation screen reviews against **what the agent read** — the OCR text and the envelope — rather than a rendering of the file, because arrival bytes are served as `application/octet-stream` attachments on purpose and this slice does not poke that hole; the honest version of story 86's "side by side", recorded as a narrowing. **Referenced sheets are deliberately not extracted** — the ticket's field list names them, the typed shape stops at the register entry's fields, and ADR-0043 records why: a sheet reference is a link to a document version, and resolving names against stored versions is its own ticket rather than a silently absent field. `OcrProvider` is the sixth port with a refusing default, and `OCR=stub` the labelled stub for the screen, never a real document. 539 tests across 19 files, up from 508 — 31 new ones drive the HTTP API only: the run states, the stream, the proposal's constraints, both confirm paths, reject, injection posture, the exact key sets, and `PATCH`/`PUT`/`DELETE` on an extraction all 404. Verified in the browser end to end: the honest failure path first (OCR stub ran, the agent's refusing default failed the run with "no model provider is configured"), then a pending proposal seeded through the tool's own real route (as slice 17's verification did), confirmed through the actual form with one field edited — the redirect landing on the entry carrying the corrected subject, the document created from the arrival's bytes, the clock running and the entry past it — and a second extraction rejected, its source document standing with no entry written. `pnpm --filter web build` still fails only on the pre-existing `components/ui/field.tsx` errors recorded since slice 12, which this slice also leaves alone. Review (two-axis, /code-review) found **no hard standard violations**; its two consequential findings are handled: a worker comment overstated the redelivery guard (a stalled-job redelivery *does* call the vendors again — that re-run is the only recovery path, there being no retry route — and the comment now says so), and the referenced-sheets narrowing was unrecorded and is now in ADR-0043. Three recorded as deliberate and unfixed: the enqueue's two-statement in-flight check is not a bound (a double-click degrades to one wasted vendor call, and the proposal and confirm compare-and-sets refuse the second); the confirm form gates the response field on the kind select, so re-kinding to a submittal drops a proposed response silently rather than tripping the boundary refusal; and `noSuchExtraction` lives in the refusals leaf though one record uses it, matching the existing `noSuch*` style over the leaf rule's letter. |
| 2026-09-01 | Issue #42 fixed: **three correctness gaps in project memory**, found reviewing slice 17 and filed together as one record type's correctness. (1) Accepting a proposal whose `base_content` is no longer what the memory says **silently dropped** the version written in between, and the review screen drew its diff from that base and so described a commit that would not happen; a stale accept is now refused, staleness is derived on every read and rides on the wire as `stale`, and the screen withholds Accept and says why. ADR-0040's snapshot decision **stands** — refusal is taken over re-diffing, because re-diffing would put text under review that the agent did not propose. (2) `reject` had no compare-and-set, so a concurrent accept and reject each passed the pending read and each committed, leaving both stamps set, the wire reporting `accepted`, and the append-only audit carrying both answers; both routes now settle by compare-and-set first in their transaction, and a CHECK holds the invariant the schema had only claimed. (3) "The current memory is the latest" broke a same-millisecond tie on a random v4 uuid, so the older version could read as current; `project_memory_versions.seq` is the tie-break, in one place the memory read and the proposal's base lookup share. 564 tests (up from 559); each fix proven non-vacuous by deleting it and watching its test fail. |
| 2026-09-01 | Issue #49 fixed: **the root `pnpm typecheck` and the web build are green again**, for the first time since the Tailwind slice `fee343e`, which is where `field.tsx` arrived and where the failure arrived with it — slice 12 is only where it was first written down. Both stopped on `apps/web/components/ui/field.tsx`, unused shadcn scaffolding from the Tailwind slice that stopped typechecking once two `@types/react` resolved in the tree. An exhaustive sweep — all ten of its exported names, word-boundary, across the other 142 tracked `.ts`/`.tsx`/`.js`/`.mjs`/`.json`/`.md` files, plus the module path in any spelling — found **no importer**: the one hit, `Field` in `app/open-item.tsx`, is that file's own local function. So it was deleted rather than repaired, and the deletion orphans nothing it imported (`separator.tsx` keeps its one real importer). Verified by deletion in both directions before the change was made. Three things are recorded rather than fixed. **The duplicate `@types/react` is still there and is not `apps/web`'s**: `apps/api`'s `@prisma/client` pulls `prisma` → `@prisma/studio-core` → radix components whose `@types/react` peer nothing in that subtree pins, so it takes the newest (19.2.18) while `apps/web` pins 19.2.2 and its own radix tree resolves there — which is why bumping `apps/web` would not have helped, and why a workspace-wide override is the fix for the class, priced (it pins a version on behalf of Prisma Studio, which this product does not run) and deliberately not taken. **`pnpm --filter web build` needs a generated `EDGE_SECRET`**: `next build` runs as production and `next.config.ts` refuses the `.env.example` value that `pnpm dev` has usually already copied into `.env`, failing under a misleading `Failed to load next.config.ts` headline that reads like a syntax error and is not one, the real cause being named on the line below — so AGENTS.md's frontend rule, followed literally, failed today for a second reason unrelated to `field.tsx`, and now says how to run it. And **`checkbox.tsx` and `select.tsx` are also imported by nothing**, left standing because ADR-0025 is why: those are exactly the controls the ADR keeps as native elements, so their being unused is a decision rather than an oversight. |
| 2026-09-01 | The instruction files were split so a session stops paying for every rule on every turn. `AGENTS.md` had grown from 1,470 to 68,670 bytes in fourteen days, 63,560 of them the 144 ground-rule bullets, and Claude Code loads it at launch into the main session and into every subagent but Explore and Plan. The bullets were moved, none rewritten, into eleven files under `.claude/rules/`, each carrying a `paths:` frontmatter so Claude Code loads it when a file it names is read; `AGENTS.md` keeps the seven that apply to every path and a table saying which file to read before editing what. The mechanism was probed before the move: a sentinel rule scoped to `routes/photos.ts` was injected into a fresh session's context after that file was read, and not before. The change log, 64,723 bytes of `CONTEXT.md`'s 82,132, moved here for the same reason. Measured on a fresh one-word session: 67,600 prompt tokens at launch before and 46,000 after; a session that has then read `CONTEXT.md` in full sits at 52,600 where it sat at 94,400. |
| 2026-09-02 | Slice 22 built (issue #56): the deployment ADR-0003 asked for on 2026-08-17, settled as **Fly** in ADR-0045 — the oldest open item on the vendor list. One machine runs both halves, the API on loopback and Next on every interface, which is ADR-0020's arrangement made literal rather than packaging convenience. The health check is `GET /unlock`, the one path the gate lets by, because ADR-0020 gates `/v1/health` with everything else deliberately. The object store is a **mounted volume**: it is the one unfinished seam that succeeds silently rather than refusing, so an ephemeral filesystem would have stored every photograph and recording and lost them on restart — the S3 adapter stays unwritten and the vendor pick stays deferred. `NODE_ENV=production` in `fly.toml` is what finally makes `apps/api`'s guard against the committed development `EDGE_SECRET` fire at all, closing the asymmetry ADR-0020 recorded. Six things broke, and four could not have been found locally: `unzip` missing, so puppeteer fails to extract Chrome while `pnpm install` **still exits 0** and only the report renderer breaks, at runtime; `prisma generate` running before the schema was copied; `wait -n` under dash; `HOME` not following the `setpriv` uid drop, sending pnpm to root's corepack cache and restart-looping the machine to its limit of ten; Fly's Managed Postgres having no TLS on 6PN (`P1011: unexpected EOF`), handled with a readable `DATABASE_SSLMODE` line rather than by encoding transport into a credential; and no public IP, because `fly apps create` allocates none. Chrome's **default sandbox** — which ADR-0035 kept deliberately, and which is why the image runs as uid 10001 — is verified on the machine that serves rather than inferred, since Docker's seccomp profile blocks the clone flags it needs and a laptop therefore cannot answer the question: `SANDBOX_OK pdf_bytes=8434`. Also fixed the same day (issue #54): `routes/photos.ts` and `routes/voice.ts` carried a base64 pattern admitting a length of 4n+1, which `Buffer.from` silently truncates, so a clipped photograph or recording stored and answered 201 — ADR-0039 recorded that defect on 2026-08-20 and left it standing, and this is the change about those records it said a fix would belong to. |
| 2026-09-02 | Slice 23 built (issue #60): **story 113, the full export** — the mechanism behind the PRD's own "survive a job change" goal, which until now was asserted by architecture rather than delivered by anything. It was in the spec of record, in none of the vault's remaining-work lists, and on no Out of Scope list: a story belonging to no step of the six-step sequence is a story nothing was watching. `GET /v1/export` returns every row of every one of the 31 tables in one document, not project-scoped, each table present even when empty so an importer never has to tell "no rows" from "this version lacked that table". A `storage_key` becomes the path that serves those bytes (ADR-0032 has kept that column off the wire since slice 10), so **an export is a document plus the fetches it names** — base64 in the same body was refused, since a walk's photographs at twelve mebibytes apiece is a response nobody can hold. `ingest_token` is dropped rather than renamed: it is a credential (ADR-0042) and the one value here that can simply be minted again. Both absences are asserted by searching the whole serialised document rather than field by field, so a table added in a later slice carrying either column fails the test without anybody remembering to extend it — the same argument that makes `edge-gate.test.ts` sweep every route rather than a sample, and the reason this route needed no gate test of its own. Ordered by primary key and never by a timestamp, because two rows written in one transaction share an instant and an export that reorders itself cannot be diffed against the last one; the eight join tables have no `id` and are ordered by the pair they join. No importer and no screen. 570 tests. |
| 2026-09-05 | **Postgres swapped from Managed to unmanaged** (ADR-0045's deferred cost decision, taken by the author). `epmos-db` on Fly's cheapest *Managed* plan was $38/month for a database holding zero rows; `epmos-pg` is a `shared-cpu-1x` machine with a 1 GB volume and daily snapshots at roughly **$2/month**. Done while the database was still empty, which is the only reason it cost nothing — no dump, no restore, and `prisma migrate deploy` applied all 21 migrations from scratch against the new cluster. After step 1 of the sequence this would have been a migration with data in it. Three CLI facts worth keeping: `fly postgres attach` refuses while a `DATABASE_URL` secret already exists, so the order is `mpg detach` → `secrets unset DATABASE_URL` → `postgres attach`; the unmanaged attach writes `?sslmode=disable` into the URL itself, which makes `fly.toml`'s `DATABASE_SSLMODE` a no-op that is kept as a fallback rather than deleted; and `fly mpg destroy` needs `--yes` and takes no `--org`. The trust boundary is unchanged — still Fly, still one vendor — which is why a free tier at a second vendor was not the answer, that being a consent question and not only a hosting one. Fly does not operate an unmanaged cluster: upgrades and disaster recovery are the author's, backed by daily volume snapshots and the export route ADR-0047 built. |
| 2026-09-05 | Issue #67 fixed: **Extract on a job on local processing showed Next's default error screen** instead of the refusal. The API was right — 409 with `PROCESSING_LOCATION_IS_LOCAL`, ADR-0044's gate firing exactly as designed — and the two actions behind the button tolerated only the in-flight 409 and threw every other refusal, with no `error.tsx` anywhere in `apps/web` to catch the throw, so the one refusal that keeps a client's documents from a third party read as a crash. The two actions now return the API's own sentence, and one client component (`extract-button.tsx`) shows it beside the button as every other refusal on the page is shown; the in-flight 409 is still swallowed as the second half of a double click. `apps/web/app/error.tsx` is added, the first error boundary in the frontend, so any unhandled throw below the root layout degrades to a page carrying the message, the digest and two ways back; there is no `global-error.tsx`, so a throw inside the root layout itself still reaches the framework default — a recorded gap, not an oversight. The issue's open question — whether the button should render at all on a local job — was decided in favour of rendering it and answering: the API is this product's only validator, hiding the control would put the gate's predicate in the frontend, and a page left open across a switch to local would show the button regardless with nothing to say why it failed. No API change. |
| 2026-09-05 | Issue #68 fixed: **nothing in the interface linked to `/export`**, so the door PR #62 built was reachable only by typing the URL — an export nobody can find is not much better than one that does not exist, and the moment it is most needed is the moment somebody is least likely to be reading source to discover it. The link is on the morning screen, last on the page: the export is an across-every-project action and `/` is the across-every-project screen the engineer is already on, where the header nav stays at four links because the product's shape leans on it. The copy says what happens — the whole record, every project, every table, as one JSON file named for today — and says what is not inside it, the photographs, recordings and documents the file names (ADR-0047). A plain anchor and never `Link`, because a client-side navigation or a prefetch of the route would fetch the entire record to render nothing. No import, deliberately (ADR-0047). No API change and no change to the route. |
| 2026-09-05 | Issue #50 closed: **`apps/web` has tests.** It had no `test` script at all, so the root `pnpm test` — the command `AGENTS.md` names as the gate — ran the API suite and nothing in the frontend, and every frontend claim in twenty-three slices was a person with a browser. ADR-0049 records the level decision the ticket insisted on taking first: **component-level, Vitest under jsdom**, on the same pinned runner version as `apps/api`, needing no API, database, Redis, browser or network. Sixteen tests across five files, and two of them are scans of the source rather than renders because the rules they hold render correctly when broken: the **door** (exactly one module calls `fetch`, one attaches the edge header, one reads the origin, one reads the secret — ADR-0020's "a call that does not come through here does not reach the API" now held by a test as well as by construction) and the **native select** (every `<select>` carries the shared styling and nothing imports the Radix one — ADR-0025), both walked over the whole tree so a file added later is covered without anybody extending them, which is ADR-0047's argument for searching the serialised document. The **first paint** is rendered on the server *and* hydrated over, with `console.error` watched, because a hydration mismatch is a logged error and not a throw — ADR-0028's class, where the ref runs, the value is right and the render keeps the old one; the `EventSource` the suite installs delivers nothing on purpose, since a stub that pushed events would let a component pass by being corrected afterwards. ADR-0038's asymmetry is asserted on **both** sides — both morning-screen cards at zero, both project-screen strips gated on being non-empty — so making them agree fails. The suite was verified by breaking the product on purpose: a rogue `fetch` outside `api.ts`, a `selectClassName` stripped from the pending sort select, and a `useLiveList` seeded with `[]` each failed exactly the test written for it and nothing else. What it does **not** cover is written down at equal length in ADR-0049 and is the arrangement production actually is — the edge gate end to end, the loopback API, Next serving anything, and hydration in a real browser — with end-to-end deferred under a named trigger: the first defect that reaches the deployed machine and could not have been caught here. `.claude/rules/web.md`'s frontend bullet is rewritten to say which half is now mechanical; `pnpm --filter web build` and loading the pages is narrowed, not lifted. No API change and no product behaviour change. |
| 2026-09-05 | Issue #63 closed: **project memory had no history screen**, and the read to build one had been written and tested since slice 17 — `listMemoryVersions` sat in `apps/web/app/api.ts` imported by nothing. `/projects/[id]/memory` lists every version newest first with its date, its size against the 4,000-character budget, how it arrived, and a disclosure holding the diff against the version it replaced. ADR-0040 made memory versions on the project precisely so that nothing is overwritten and accept-with-edit keeps the agent's own words; until this screen the earlier text survived somewhere nobody could read, which made the guarantee true and unobservable. **How a version arrived is derived, not stored**: `proposal_id` null is the engineer's own writing, and set is an accept whose text is compared with what the agent proposed — the same comparison the accept route already makes when it picks between its two audit lines, rather than a column giving that question a second answer. The diff is drawn by one component, `DiffView`, moved out of the proposal card so a version's diff and a proposal's are one rendering rather than two free to drift; it carries no `use client`, so the history reads it from a server component and the proposal card compiles it into the bundle it already ships. The project screen's version count is now the link to what it counted, the way exposure's and the clock's counts are. Reverting to a past version is deliberately absent: that is a write, and whether reverting is a new version is an ADR rather than a button. |
| 2026-09-05 | Issue #64 closed for findings: **nothing listed issues, observations, photographs or documents across projects**, and the first of the four with a real question behind it now does. `GET /v1/issues` and `/issues` list every finding still open on every job, filterable by category and sortable by age, each row linking to the finding and to the job it is on. Retrieval stays by identity (ADR-0019) and this does not weaken it: a finding is still reached through the job, and this is that reading widened to every job at once rather than a search over them — there is no search box, which is ADR-0019's own deferral with its own trigger. The other three record types the audit named are deliberately **not** built: the sketch says to wait until each has a question of its own, and only this one had. **The pending items view is the precedent in every respect** — only what is still open, oldest first because the age is the reason to look, the job attached to every row, and archived projects **included**, which is the line `/pending` draws and for its reason: this is not one of the two daily counts, and a finding still open on a finished job is exactly the thing that would otherwise be lost. Exposure and the clock draw it the other way because they are. **A list and never a number**: the morning screen links here and reads nothing off it, because a third across-every-project figure beside exposure and the clock is the shape a combined score would be computed from (ADR-0016), and the nav stays at four links, the arrangement issue #68 settled for the export. `id` breaks the ordering tie so the list is total; the tie-break is arbitrary but stable, which is all that is asked of it here — nothing reads this order to decide which row is current, which is what made a random uuid the wrong answer for memory versions (issue #42). The five categories moved from `issue-form.tsx` to `api.ts` when the filter became their second reader: the API refusing a sixth is what licenses a copy on this side of the wire, and it licenses one. |
| 2026-09-05 | **Story 106: the audit widened to every mutation.** It was written for **project memory** in slice 17 and widened once, for the processing location, by ADR-0044; both said widening it to every record was "its own change, not something to do row by row". The sweep before the change: **5** call sites (four in `routes/memory.ts`, one in `routes/projects.ts`), against **62** mutating routes registered under `apps/api/src/routes/` — 61 `POST` and one `DELETE`, no `PUT`, no `PATCH`, and no deliberate-404 stubs among them. All 62 now write a line, through one new leaf `apps/api/src/audit.ts` and inside the same transaction as the write it describes, so a mutation that cannot be recorded does not happen; `at` is the caller's `timeSource.now()` and is never read inside the leaf (ADR-0022). Routes that had no transaction now open one to say what they did. `POST /v1/projects/:id/memory/runs` — the memory mutation slice 17 itself missed — writes one now too. A **refused** write writes none, and a **no-op** writes none either: a second archive is narrowed by `updateMany` and the count is what tells the two apart, and a recording resent after a signal drop is answered with the row it already has. **Nothing an untrusted party wrote reaches a line**: an arrival's `sender` carries no length bound at all, so the mail path's line says how many files came and nothing else; a filename may be said, being bounded at 255 in two places, and the manual path's note may, being the engineer's. `apps/api/test/audit.test.ts` reads the routes Fastify actually registered and fails if a mutating one is missing from its written-down list, the shape `edge-gate.test.ts` gives ADR-0020's gate — so the widening cannot silently un-widen and a route added later cannot go unrecorded. `GET /v1/projects/:id/memory/audit` is unchanged and always returned the project's *whole* audit (`where: { projectId }`), which ADR-0044 recorded as a mismatch; now the answer is right and the **path** is the stale half, left where it is because a screen links to it. Glossary **Audit log** and the PRD's data-model line record the widening; the slice-17 and slice-20 narratives are left as the history they are. No schema change and no migration: the table already had every column this needed. |
| 2026-09-05 | **Story 107: the activity feed, and ADR-0048, which should have been written in slice 17.** The story was cut on 2026-09-01 and the cut was recorded in **one place** — a doc comment on `AuditEntry` in `apps/api/prisma/schema.prisma`, saying the feed was "a separate stream the MVP does not build"; ADR-0040 carries half a sentence of it with no reasoning and no trigger. That breaks this project's own rule that scope adjustments get recorded in the vault, and it left the vault saying story 107 was in the MVP while the code said it was out — the failure ADR-0047 found for story 113. ADR-0048 records the decision rather than reopening it, and reconstructs what was cut: **not the story, the separate stream**. Slice 17's audit was scoped to memory, so a feed over it would have answered "what happened on this project lately" with "somebody edited the prose" on a job where a submittal had gone out; there was nothing to feed a feed from, and slice 17 was right to stop and wrong only about where to say so. Story 106 having widened the audit to every mutation, the answer now exists. **`GET /v1/projects/:id/activity`** returns audit rows newest first and bounded — `?limit=`, 50 by default, **200 at most** — against the audit read's every-line-oldest-first. Same rows, opposite order, different bound: two answers to story 107's two questions, from one record that cannot disagree with itself, which is the guarantee a second stream could not offer at all. The maximum is load-bearing rather than a page size: without it the route is the compliance record with a query parameter on it. The PRD sketch's `activity_events` is refused, joining `approval_requests` and `referenced_files` as a sketched table deliberately not built — a second stream is a second place the same fact lives with sixty-two writers of its own, and a drift would put the version nobody checks in front of the engineer and the version nobody reads in front of the dispute. The response is a **list whose length is not a count**, unlike exposure's and the clock's (ADR-0027, ADR-0037): it is bounded, so nothing may render it as a figure, and ADR-0016 keeps this product to two. `audit_entries` gets **no `seq`** where `project_memory_versions` has one, recorded with its trigger: issue #42 needed that column because *which version is current* is a computed value, and nothing computes a value from the audit's order. `apps/api/src/routes/activity.ts` is named for the glossary's **Activity event** and `src/audit.ts` for the table it writes — reader and writer; `server.ts` gains one entry in its list of record types and no mechanism. Eight tests, including that the feed is the audit read reversed exactly and that `?limit=201` is a 400. **The screen is deliberately not in this change and is recorded as a gap in ADR-0048**: `apps/web` still renders the audit as a collapsed block inside the memory section with its length beside it, which since story 106 is the wrong heading and a count that must not be rendered. Glossary **Activity event**, the ADR README, the PRD's sketch line and CONTEXT's decision table all record it. No schema change and no migration. |
| 2026-09-12 | **Issue #104: a project has a timezone, and every stored time is a real instant (ADR-0054, superseding ADR-0050).** `projects.timezone` is an IANA name, required with no default and validated against `Intl.supportedValuesOf('timeZone')`, so an alias and a fixed offset are refused with an unknown name: a building is in a place. `composeInstant` composes a typed day and clock time **in that zone** and `asTypedInstant` is **deleted** — the two helpers ADR-0050 coupled changed in one commit, as 0050 required of whatever superseded it. A photograph's `taken_at` is now the instant its file already carried, unshifted; `binToFloor` did not change and became correct, because both sides of the comparison are instants at last. What made 0050 false was never the typed path: a floor window started by the advertised **blank-time** path is stamped by the injected `TimeSource`, so it was never in the fake frame, and every photograph on such a floor bound to nothing — silently, with nothing failing (#97). The real-clock test ADR-0052's fourth point asks for is written and is the thing that says so: the clock is injected at an instant whose New York wall clock differs from its UTC face, the floor is started blank, and the photograph binds — while the same wall clock labelled `Z`, which is what the screen used to send, is asserted to bind to nothing. Composition lives in one place (`apps/web/app/wall-clock.ts`) and a source scan in the web suite asserts it: the hour that happens twice composes to the first of the two, and the hour that never happens to the far side of the gap, so a midnight a zone skips lands on the right day. One migration moved every existing row — a typed-capable value iff its seconds and milliseconds are both zero (fifteen columns), every `photos.taken_at` unconditionally, no purely stamped column touched — printing a count per column, and re-ran floor binding after the shift. The counts on this database: `projects.cloud_signoff_at` 1, `site_visits.started_at` 2, `observations.observed_at` 2, `ball_in_court_events.held_since` 1, `register_entry_extractions.proposed_held_since` 1, every other column 0, and no photograph re-bound because there are none. Every screen renders in the project's zone, each row of a cross-project list in its own — which is why the project **stub** carries the zone and not only the whole project, since the date-densest screen in the product (a walk) never fetches a project. The report header prints the zone once, as the IANA name, because `EST` is four different zones and a report is read by people who were not on the walk. `apps/web/app/ingest.tsx` stopped being the one render in the viewer's own zone. **After the first real walk this answer flips**: a later frame defect is a dated correction, never a rewrite. |
| 2026-09-12 | **Issue #105: users and sessions replace the edge gate (ADR-0055, superseding ADR-0012 and ADR-0020).** ADR-0012 said "a personal tool … multi-user is a real future migration, not a config change"; the first half was reversed on 2026-09-08 when the author's answer became *other engineers at the firm*, and the second half was honest and is now due. A person is a `users` row (name, unique email, an **argon2id** hash, `created_at`, `disabled_at`) and one signed-in browser is a `sessions` row (an opaque id, the user, `expires_at`, `revoked_at`) — which is the difference from the secret it replaces: a shared secret had no state, so rotating it was a redeploy and there was nothing to revoke, while **a session is a row and comes off on its own**. `schema.test.ts`'s "no `users` table" assertion, cited to ADR-0012 and standing for twenty-one slices, is **replaced and not deleted**: it now asserts that `users` and `sessions` exist and that `roles`, `permissions` and `tenants` still do not. ADR-0020's three properties worth keeping are all kept, and each is a fact about the code rather than a claim: `POST /v1/ingest/inbound-mail` is still the **one** exempt route and `gate.test.ts`'s sweep over every registered route proves it **unchanged** — `POST /v1/sessions` is where a session is *obtained* and not a second exemption, which is why it is the one route in the product with **no body schema**, since a schema answers the sweep's bodyless POST with a 400 while the route itself answers 401 for a wrong password, an unknown address and no body alike; `apiFetch` is still the only thing in `apps/web` that reaches the API, now because it is the only place the cookie is read and the `x-session-id` header set, with `next/headers` imported *inside* it because client components read the same module's closed vocabularies and Turbopack refuses a server-only module in the browser's graph; and nothing stands between the engineer and a walk — the cookie is `httpOnly` for a year, as the unlock cookie was. A 401 sends the engineer to `/sign-in` **only when a session was actually presented**, so the sign-in form's own refusal is not swallowed by a redirect, and the two readers that must be able to hear *nobody* say so explicitly. The **agent** presents a run-scoped session in the name of whoever asked for the run: it is minted by the route that creates the run, found by the worker through `sessions.agent_run_id` or `sessions.extraction_id` — **not** carried in the job payload, where a live credential would sit readable and outlive a stalled job — and revoked when the run settles, with a day's expiry behind it for the run that never settles at all. This product has **two** run records, which ADR-0055 did not anticipate (ADR-0043 made an extraction a record of its own rather than an `agent_runs` row), so there are two link columns and a CHECK that at most one is set. `audit_entries.project_id` becomes **nullable**: creating a user and signing in are the firm's mutations and not a job's, and both readers of that table filter on the column, so a firm-level line is absent from a project's audit rather than mislabelled in it — the only route that returns one is the export. The first account is a **command on the machine** (`pnpm --filter api user create`, `scripts/user.sh` on Fly), which also resets any password until outbound mail exists; the password is read from the terminal with the echo off or from a pipe, and **never from an argument**, which is in the shell's history and in `ps`. Every account after the first is `POST /v1/users` by somebody already signed in — an audited mutation like any other, offered at `/users`, with **no roles**. `POST /v1/users/:id/disable` is the one thing here beyond the ticket's letter and is why `disabled_at` is not a column nothing can write: it stamps the account and revokes every live session of theirs in one transaction, so "cannot sign in" is true of the phone in somebody's pocket and not only of the sign-in screen. It is not a one-way door — anybody may close any account including the last one, and `pnpm --filter api user enable <email>` is the floor under that, the same floor the first account falls through. Both were verified by hand against the dev database, since neither is reachable over HTTP from the machine command's side: closed, refused at sign-in, re-opened, signed in, four audit lines. **`EDGE_SECRET` is gone.** Eighteen tracked files named it; it survives in five of them as prose dating its removal, and in none as a value read or configured — both apps, both `.env.example`, both `.env`, `next.config.ts`'s two boot guards, the `Dockerfile`'s build line and `fly.toml` (whose health check moves from `/unlock` to `/sign-in`) are clear of it, and `pnpm --filter web build` now needs nothing passed to it. `.claude/rules/edge-gate.md` is `gate.md`. 614 tests across 25 files in `apps/api`, up from 600 across 23, and 41 across 9 in `apps/web`, up from 35 across 8 — the five new ones close a hole that **predated this ticket**: neither `proxy.ts` nor the sign-in screen's open-redirect guard (itself a review finding on issue #22) had a test, and a regression in either was invisible to `pnpm test`, which would have stayed true with both rewritten. The guard moved to `app/sign-in/destination.ts` to become testable at all, since a `'use server'` module may export nothing but async server actions. Both were verified by breaking them on purpose — an empty cookie let past the proxy, and the guard returning its argument — and each failed exactly the assertion written for it; the suite's own bootstrap writes its user and session rows directly, which is the bootstrap a deployment has rather than an exception to "fixtures through the API", and its session outlives any clock a test advances because a dozen suites age a fake `TimeSource` by up to four hundred days. Verified in a browser end to end: the proxy redirecting `/pending` to `/sign-in?next=%2Fpending`, a wrong password answering with the one sentence, signing in landing on the asked-for page, the header naming the signed-in engineer, a **second** engineer added at `/users` and signing in as themself, and signing out. Not in this ticket and named: the audit line's actor and subject (#111), the person on the record (#112), and `users`/`sessions` in the export, which #111 adds with the hashes dropped by ADR-0047's credential rule. **One gap ADR-0055 did not price and this change leaves open deliberately: `POST /v1/sessions` has no rate limit.** The credential was 32 random bytes and is now a password with a twelve-character minimum, and argon2id's tens of milliseconds per attempt is the only throttle there is. Closing it needs a decision this slice did not take — a failed sign-in writes nothing, so there is nothing to count, and ADR-0042's rate limit counts rows and refuses a counter beside them; whether a failed attempt is a record is its own ticket and its own ADR — **issue #125**. Recorded in ADR-0055. |
| 2026-09-12 | **Issue #106: CI gates every push, and the health check watches the API (ADR-0052; closes #94).** `.github/workflows/ci.yml` runs the four gates on every push and pull request — `pnpm typecheck`, the API suite, the web suite, `pnpm --filter web build` — and has **no deploy step**: ADR-0045 chose one machine with a mounted volume, so a deploy on every green merge is a migration on every green merge, and the named trigger for taking that decision is the first deploy that is late because a person was. There is **no `services:` block**, and the ADR's "service Postgres and Redis" is satisfied anyway: the suite starts its own PostgreSQL 16 and Redis 7 through Testcontainers, migrates a template database with the same `prisma migrate deploy` a deploy runs, and never reads `DATABASE_URL` or `REDIS_URL` from the environment — a service container would sit unread beside the ones it starts regardless. The runner sets `kernel.apparmor_restrict_unprivileged_userns=0` so Chrome's **default** sandbox (ADR-0035, no `--no-sandbox`) can open, which is the same accommodation `seccomp=unconfined` is under Docker locally and says nothing about Fly, where no such filter applies. The platform check moves from `/sign-in` to **`/healthz`** — a Next route, exempted in `proxy.ts` beside the sign-in screen, that calls the API through `apiFetch` and answers 503 when nothing answers it. `GET /v1/health` stays **gated**: the third way rather than the second exemption, because "one named exception is a property a test can hold and two is the start of a list" and `gate.test.ts` sweeps every route to hold it. So the check proves the API process accepted the connection, ran its gate and replied, and deliberately not that its database and Redis are up — the payload stays behind the gate and an anonymous caller gets one bit. `.claude/rules/api.md` gains ADR-0052's two test kinds **by name**, the at-scale fixture and the real-clock test, pointing at `PAST_THE_STACK` (#98) and the clock tests (#104) that already existed — what shipped is the naming, which is what makes the next size rule and the next frame boundary ship with theirs. Two defects the gate found on itself, neither visible locally: three tests released a held vendor and returned, leaving the run's last two writes racing an `afterEach` that drops the database `WITH (FORCE)` — red with all 614 tests passing; and a **warm pnpm store** links puppeteer without re-running the install script that downloads Chrome, which lives outside the store, so a documentation-only commit went red on `Could not find Chrome` with the install exiting 0. Chrome now has a cache of its own and an explicit `puppeteer browsers install chrome`. The lesson recorded in ADR-0052: a green run says nothing about the run after it when a cache is what differs between them. One incidental fix: `extractions.test.ts` read `finishedAt` off the same poll that found the state `pending`, and the agent's proposal and the worker's finish stamp are two writes, so it went red about once a full parallel run — a gate that is red at random teaches everyone to re-run it. "Nothing merges red" is **enforced** from 2026-09-13: `main` requires the `gates` check, so a pull request with a red run cannot be merged. Two deliberate weakenings, both recorded in ADR-0052 — `strict: false`, so a branch need not be rebased before merging (named trigger to tighten: the second person with push access), and `enforce_admins: false`, so the author keeps `git-workflow.md`'s allowance to push a one-line doc fix straight to `main`, which a required check would otherwise block. The honest consequence: the check stops an *accidental* red merge, not a determined one, which is the whole of what a solo repository can buy. Force pushes and branch deletion are refused, and that half admins cannot bypass. **Verified on the machine that serves on 2026-09-13**, which ADR-0052's sixth point asked for and ADR-0045 requires of anything it claims: deployed as v17, both of the migrations it carried applied on boot, and three readings of `/healthz` recorded in ADR-0045 — both halves up `200 in 0.156688s`, the API process frozen with `kill -STOP` `503 in 3.144861s`, resumed `200`. The middle reading is the whole of #94, since `/sign-in` read green in that state. `SIGSTOP` and not a kill deliberately: an API that *exits* is already covered by `scripts/start.sh`'s `wait -n`. Not captured, and recorded as not captured: the per-column counts ADR-0054's migration prints never reached the logs, `prisma migrate deploy` swallowing `RAISE NOTICE`. |
| 2026-09-13 | **Issue #107: a helper skill is a route of this product — the registry, four helpers, one route (ADR-0053 and its 2026-09-11 correction; closes #92 and #93).** The PRD has promised since 2026-08-17 that the NEC helper skills are *"called, not absorbed"*. Half held completely — the product implements no calculation logic anywhere — and half was never built: nothing called one, and ADR-0041 had removed the only plausible mechanism when `cwd` turned out never to have bounded the SDK's file tools. What shipped instead was a **paste**, with no provenance, on one machine, and no way at all for a second engineer. The helpers now live in their **own repository**, `augustov58/electrical-helpers`, a git submodule pinned by commit at `apps/api/tools/` — the author's framework, which outlives any one employer — assembled out of the vault's `Electrical Engineering` skills with **nothing rewritten** on the way in: the four calculators are the files that were there, plus three manifests, a README, and a self-test the transformer sizer did not have. `#93` said one helper existed and the 2026-09-11 inventory found four, under hidden `.agents/skills` and `.claude/skills` directories that #93's sweep did not enter; three print the two blocks and `generator-sizing` prints a JSON dict and a summary with no `argparse` at all. **A directory carrying a `manifest.json` is registered and one without is not**, which is how the generator ships in the pin and stays out until it prints them — so nothing in `apps/api` names a helper, not the route, not the tool list, not the CI gate, and *adding a helper is adding a directory*. `POST /v1/tools/:name` is **one** route driven by the manifests: it validates the named helper's own JSON Schema (its own `Ajv`, with `coerceTypes` and `useDefaults` **off** — `"40"` is not `40` here, and an omitted flag is how a script's own default is asked for), runs the script as a subprocess, and returns the `ASSUMPTIONS:` and `FLAGS / VERIFY:` blocks **verbatim**, found by their two header lines and the rule of `=` a report closes with. Nothing reads a line's content: the `- ` and `! ` sigils stay unparsed for the reason ADR-0029 gives, and a registered helper that prints neither header is **broken** (502) rather than a record with empty blocks. **It records nothing** — no row, no stamp, no audit line — so it is the one mutating-method route `test/audit.test.ts` exempts, in a `RECORDS_NOTHING` set with one member on the gate's own argument that one named exception is a property a test can hold and two is the start of a list; and it answers **200, never 201**, there being no location because there is nothing there. Asserted twice: a project's audit is unchanged across a run, and the full export contains neither the helper's name nor the word `ASSUMPTIONS`. The **gate sweep covered it without a line changing**, which is ADR-0053's whole reason for a helper being a route of ours, and that was verified by breaking it on purpose — adding the route to the gate's `EXEMPT` set fails `gate.test.ts` with `POST /v1/tools/:name` in the allowed list. **The subprocess is given its own directory, and the code says in those words that this is not a jail**: the helper's directory as cwd, an environment carrying nothing but `PATH`, no shell, `python3 -B -I`, and an argv built only out of values the schema accepted — not a `chroot`, not a mount namespace, not a container. That is ADR-0041's own rule turned on ourselves, a sandbox claimed being no sandbox until the mechanism has been read; what stands behind it is that the code is ours and pinned, and the named trigger for a real jail is the first helper this product did not write. The wall clock is **ten seconds**, written once as `WALL_CLOCK_MS` because the ADR requires a limit and never names one; a run still going is killed and answers 504, and a script's own nonzero exit reaches the caller as **its** stderr and a 400 — the manifests express *"at least one of these four"* and never *"exactly one"*, mutual exclusion being argparse's rule and not ours. `short-circuit` and `voltage-drop` came out of **one** upstream skill and are registered as **two directories** carrying byte-identical script sets, because a helper is a directory and the directory is what its subprocess is given; the 40-case Bussmann/Eaton SPD handbook harness runs in **both** and the CI gate diffs the two trees, so a drift that changes an answer and a drift that does not each fail. The agent's helper tool list is **generated from the same manifests** and asserted by a test to be exactly them — underscored, because provider APIs reject a dot (ADR-0040) — and is deliberately **not yet given to a run**: neither run this product has may call a helper, and the one that will is the project conversation (ADR-0058, its own ticket). It lives in `agent.ts` now rather than then because that is what gives `helpers.ts` its second reader, which makes it a **leaf** under ADR-0033 from its first line rather than after a move. The image gains **`python3`** — no pip, no requirements file, no virtualenv, the helpers importing only the standard library — and CI gains a **fifth gate**, `./scripts/helper-tests.sh`, running each manifest's own `test` from its own directory: it gates what this repository *pinned* where the other four gate what it wrote, since a bad commit moved into the submodule is a wrong number on a drawing and not a red typecheck. It carries a vacuity guard, under three commands being a failure, because a submodule that did not check out would otherwise run nothing and exit 0; `actions/checkout` is handed `HELPERS_READ_TOKEN`, the default `GITHUB_TOKEN` being unable to read a second private repository. 632 API tests across 26 files, up from 614 across 25, and 50 web tests unchanged in count by this ticket. `.claude/rules/open-items.md`'s **"the three calculators"** now reads "the helper skills" and carries the dated note: the number is gone rather than corrected, because ADR-0029's decision rests on the sentence that does not count — a further helper is free to print something else. A new `.claude/rules/helpers.md`, the first rules file not moved out of `AGENTS.md` on 2026-09-01. ADR-0045 and ADR-0002 gain the dated lines ADR-0053 asked of them, and the glossary's **Helper skill** entry, which carried a 2026-09-10 amendment saying one existed directly beneath a 2026-09-11 one saying four did, is reconciled. **It is a copy and not a move, twice over**: the criterion says the vault's skills are *moved* into git, and the vault's `.agents/skills/` and `.claude/skills/` still hold all four unchanged and unversioned, free to drift from the pin without anything noticing — trigger for closing it, the first helper edited in one place while the other does not follow. The route also returns the **whole report** beside the two blocks, which the ADR does not ask for and which is there because the blocks alone carry no numbers and because containing them character for character is how *verbatim* is checkable at all. **Two things ADR-0053's correction assumes and this ticket did not do**, both held by the author: `~/skills/electrical` — a 345-line half-length duplicate of an 842-line script — is **not** deleted, and the follow-up ticket admitting the generator helper is not opened. **And one thing that must happen before this lands: `HELPERS_READ_TOKEN`**, a fine-grained token with read access to `augustov58/electrical-helpers`, has to exist as an Actions secret on this repository — the helpers' repo is private, so without it `actions/checkout` fails at the first step and all five gates go red, not only the fifth. No schema change and no migration. |
| 2026-09-13 | **Issue #108: extraction is reachable from where the document is (no ADR; closes #100).** Frontend only — the API is untouched, which is the ticket's fourth criterion held by construction rather than by review. What was missing was never the capability: `POST /v1/documents/:id/extractions` and `GET /v1/projects/:id/extraction-targets` both existed, and the Extract control had sat on the Documents section since issue #20. It was **the landing**. Asking revalidated the project screen and left the engineer to find the proposal under Extractions — four steps across three screens, none of them the one you are on when you decide to log an RFI (#100). So `requestExtraction` now reads the created row's id and `redirect`s to `/projects/:id/extractions/:extractionId`, the confirmation screen that already existed; the tolerated in-flight 409 does not and cannot, having created nothing to land on. The register screen gains `ExtractFromDocumentForm` beside `NewRegisterEntryForm`, reading `listExtractionTargets` — **the API's own predicate rather than a filter over `listDocuments`**, so the screen and the route that refuses a referenced file cannot come to disagree. Two calls the ticket did not make and this took: what is chosen is the **document and not the version**, because the route resolves the latest itself and stamps it, so a select of versions would let the engineer pick C and silently extract D — each option names the revision that will be read instead; and the control renders on **both registers**, since the agent proposes the kind and the confirmation screen carries a Register select, so narrowing it to RFIs would hide on one log what the other offers. The PRD's "a document version's screen" is the Documents section, the same sentence forbidding a new route. Landing before the run has read anything made the confirmation screen's one sentence for queued, reading and read-with-nothing-to-propose a **refusal on two of the three**, so each now says what the run is doing; and because the ask lands on a screen the project list's live one used to cover, `ExtractionProgress` wraps that sentence while the run can still move — the same `/projects/:id/extractions/stream` the list opens, summarised to this extraction's state and seeded from what the page was rendered with, so the first delivery refreshes only if something moved and a resolved run opens no stream at all. **Review found the one thing the landing broke**: the in-flight 409 is swallowed because the re-render shows what is true, and on the register screen there is nothing to re-render and the ask redirects away — so a second click changed nothing at all, and not only on a double click, since `extraction-targets` excludes a referenced file and nothing else and leaves a document whose run is already going in the select. Tolerating is now the caller's answer: the two project-screen buttons pass the sentence they can show the truth of and the register screen passes none. It cannot land on the run instead — the id of the one already in flight is not in the 409, and nothing in the API changes. 60 web tests across 11 files, up from 56 across 10; the API suite is unchanged at 633. Verified on the running dev stack, not only in the suite: the control and its three documents on the RFIs register, the ask landing on the confirmation screen from both entry points, ADR-0044's sentence in place on a project switched to local, and — with the queue paused, then resumed — *Queued* becoming *The run failed: no OCR provider is configured* with nothing reloaded. That failure is #51 and is what a real document still gets. No schema change and no migration. |
| 2026-09-13 | **Issue #109: the vendor picks — the OCR adapter and the transcription adapter (ADR-0060 and ADR-0061; closes #51 and #85).** Two ports that had carried **no adapter written** since slices 11 and 19 now have one each, and what that changed is recorded rather than glossed. For the OCR port the absence was **load-bearing**: it is what four successive narrowings of the employer-consent gate rested on, ADR-0043's sentence being that until somebody wrote an adapter no document's content left this process, not to an OCR API and not to the model provider either. That sentence is now **spent**, and what replaces it is weaker and is stated as such — the defaults still refuse, an **unrecognised vendor name falls through to the refusing default rather than being guessed at**, and the two processing-location gates are untouched and are what actually bound the port. For the transcription port the absence was load-bearing for nothing and simply cost: issue #85 recorded voice capture as **structurally broken wherever it was deployed**, and the author met exactly that on the site-visit screen on 2026-09-06. **Consent landed 2026-09-08** — the author's second post-MVP framing answer, and #109's own body — which supersedes the narrower posture in #51's comment of 2026-09-05 (*"not waived, deferred by not putting firm documents through the adapter"*), written three days before the answer that replaced it. **The OCR vendor is Azure AI Document Intelligence** (`prebuilt-read`), and the vault had the question mis-framed for two weeks: it had carried "Textract vs Google Document AI" since 2026-08-24 and **both are disqualified**, on the same fact about this product. Textract accepts PDF/TIFF/PNG/JPEG only — **one of the three content types** `routes/documents.ts` admits — and any PDF past one page must be in S3, which this deployment does not have and is designed without (ADR-0045; ADR-0020 and ADR-0032 both refused presigned URLs). Google Document AI runs Office formats **only** on Layout Parser at $10.00/1,000 pages against Read's $1.50, stops at **15 pages** on the online path before requiring GCS batch, and authenticates with a **key file**. Azure takes the bytes **inline** as `base64Source`, so object storage never enters the path, authenticates with two environment strings, and offers a **per-request delete** neither competitor has — the analyze result is deleted once read rather than left to the vendor's 24-hour expiry, and that delete runs in a `finally` and is never allowed to throw, since a failed clean-up must not become a failed extraction on a row whose `ocr_text` the worker has already stored. **The transcription vendor is Azure AI Speech fast transcription**, and its consent argument is kept **separate** from the OCR one because ADR-0042 priced site audio as the *stronger* case: a recording captures whatever was being said nearby, not only the thing somebody pointed at. The near-miss is the record's real point — Azure's **batch** speech API reads audio from a blob URL and would have been disqualified on the very same ground as Textract, and only the **fast** endpoint takes bytes inline and answers in the same HTTP response. One call, no job, nothing to poll, which is the only shape a `transcribe(bytes, type) -> string` port can hold. It was chosen over AssemblyAI, which has the best delete story of the six candidates, because it has **nothing to delete**: Microsoft names fast transcription under a heading reading *No data trace*. For audio that may catch a bystander who consented to nothing, never written down beats written down and then deleted. Google Cloud STT v2 was disqualified twice over — 60 seconds or 10 MB inline, Cloud Storage past that, and ADC key-file auth; OpenAI is a fallback at the same $0.36/hr held back by a 25 MB cap and sales-gated Zero Data Retention; Deepgram is the runner-up and the named fallback; Groq and ElevenLabs were refused on price alone being insufficient when retention, training, delete and DPA are all unverified. **The weakness is recorded, not buried**: "no data trace" is a documentation promise and not a DPA clause, and the named trigger for revisiting is the employer asking for a contractual guarantee rather than a cited one. **ADR-0044's pre-flight duty was discharged before any vendor was configured** — every existing project's processing location was read on the deployed database, selecting the local project identifier only and reducing the sign-off reference to a boolean, since a project's name and a sign-off's reference are both the class of material `docs/agents/issue-tracker.md` keeps off the tracker. **What it found is recorded in the vault ADR and nowhere in this repository**, #109's first criterion saying vault-only and a file in this repo being on the tracker as surely as a comment is. The duty attaches to the *state* rather than to the adapter's author, so it is live again the first time a real job is entered. Both credentials are two environment strings read through `requireEnv` **only when the vendor is named**, while `index.ts` is still wiring — so a deployment that names a vendor and forgets its key does not boot, rather than booting and failing one record at a time; `requireEnv` moved from `index.ts` to a new `env.ts` leaf on ADR-0033's trigger, three readers rather than one, and the function is unchanged. `vendor.ts` is a second leaf by the same trigger, holding the four things both adapters do identically — the bounded complaint kept for the row a person reads, the abort test, the clamped `Retry-After`, and the abortable pause — and deliberately **not** the wall clock, since a 2,000-page document and a ten-minute recording are not the same kind of wait and one shared number would suit neither. Both adapters are hand-written REST rather than SDKs: three calls and one call respectively, the seam a test needs is `fetch` itself, and no dependency was added. **No test calls a vendor** — the harness's substitutes at the port are untouched and still what every record test uses, and the 21 new tests in `test/vendors.test.ts` sit one level below them, asserting the request this product composes and the reading it takes of a reply — the URL, the api-version, the key header, the inline `base64Source`, the multipart part names, the delete, and both wall clocks. `.claude/rules/api.md` records this as the **second** sanctioned exception to the drive-the-HTTP-API rule, the first being `schema.test.ts`. Silence transcribes to the empty string and is **not** a failure, a recording the vendor answered perfectly well being the opposite of one it refused. **Two defects were found by review and fixed before the branch left the worktree**, both of the class no typecheck reaches. The vendor delete was issued with **no abort signal at all**, inside a `finally`, in a function whose whole promise is a five-minute bound — so a vendor that hung on the delete hung the worker job after the read had already succeeded; it now carries its own ten-second bound, deliberately not the read's, because the commonest way to reach that `finally` with the read's clock spent is that the clock expired, which is exactly when the document is still sitting at the vendor and most wants deleting. And the transcription adapter read `combinedPhrases[0]`, where the vendor returns **one entry per channel** — so a stereo recording lost a channel's words while the capture still read as *transcribed*, a rewrite by omission and an invisible one, which is the single thing ADR-0034 says a transcript may never suffer; every entry is now kept, on the ground that the two ways to be wrong are not symmetrical (a channel joined twice is duplicated text the engineer reads and deletes, a channel dropped is words nobody learns were said). The first of those two had a test written for it that **passed against the bug**: the fake `fetch` asserted `init.signal!` non-null, which threw a TypeError the `catch` swallowed, so the red run had to be forced before the green one was believed. Five verification corrections came out of re-checking #51's own comment against live documentation: XLSX is right and Microsoft's service-limits page saying `XLS` is the stale one (no smoke test needed); the SDK is 1.1.0 published 2025-05-13, not the `dev` alpha tag #51 read; **no Document Intelligence page carries any no-training statement at all**, confirmed absent rather than merely unfetched; SOC 2 and ISO 27001 remain unverified at service level while FedRAMP High and DoD IL2 are publicly confirmed by name; and the 500 MB cap is stated for the *document*, not for `base64Source`, which base64 inflates by 4/3. Step 5 of the Revised MVP sequence is **complete**. The `InboundMailProvider` port is **untouched** and still has no adapter — ADR-0042 priced it as the stronger consent case of the three and nobody has been asked. **Verified on the machine that serves 2026-09-15**, as ADR-0045 requires. Both vendors answered from the deployed process over Node's own `fetch` with the machine's own secrets: analyze 202, polled to succeeded, the word on the page came back, Delete Analyze Result 204; and transcribe 200 with `combinedPhrases`. A real browser recording **transcribed in 394 milliseconds** and committed to an observation — against two rows in the same table from 2026-09-06 that read *"no transcription vendor is configured"*, which is the failure #85 was filed about and its whole acceptance in one before-and-after. The **endpoint was wrong in both ADRs and the run corrected it**: every documentation source read on 2026-09-13 gave `<name>.cognitiveservices.azure.com`, the portal shows `<name>.services.ai.azure.com` and does not display the other at all, and steers Speech to a third host again — and it is the portal's that serves **both** routes. Recorded as a correction in each ADR rather than edited away, because the sources genuinely disagree and the next reader meets the same screen. The **extraction half is not met and is recorded as outstanding**: a real 56,236-character PDF was read twice, `ocr_text` stored, `failed_at` null, 27 seconds a run — and neither proposed, correctly, the prompt saying in as many words that a document which is not an RFI or a submittal should not be proposed and that *"a run that proposes nothing is a finished run, not a failed one."* An extraction that **proposes** needs correspondence put through it. That run also found what no test could: **nothing bounds `ocr_text` between the vendor and the model prompt** — `worker.ts` passes it whole and `extractionPrompt` interpolates it whole — and the vendor's own ceiling is 2,000 pages, which is millions of characters and no context window. Filed as **#132** rather than fixed here, being neither this ticket's scope nor an obvious patch. Issue #98's shape exactly: a rule correct and tested, never exercised at the size it exists for. |
| 2026-09-14 | Issue #110 closed (ADR-0051), closing #90: **the repo's own documents say what the vault says**. `CONTEXT.md` is the pointer ADR-0051 describes and nothing that is a copy — 164 lines became 42. The decision table, the per-slice milestone table and the open-decisions list are gone. **The decision table was never the thing three documents called it**: review counted it at **49 rows, not fifty**, and absent from it were **twelve of the 61 records** — 0001, 0004, 0005, 0006, 0007, 0008, 0010, 0051, 0056, 0057, 0058 and 0059 — so *the copy nobody checks* was missing a fifth of what it summarised, and ADR-0051 took a dated correction for the figure it and the ticket both used. Gone with them are and so are two copies the ticket did not name but decision (2) covers: the `## Stack` line and the **Outcome test** line, both verbatim from `PRD and Architecture.md`. What stays is the vault pointer, the update rules, one line saying why this is not a glossary, and a four-row table naming where each dropped thing lives — the decision table in the vault's `docs/adr/README.md`, the slices here, the six-step sequence's status beside the sequence in the PRD, and the open decisions in the README's `## Open, deliberately`. The six-step status was checked to be in the PRD *before* the table was dropped, step 1's "on 260001, not T-1" included. `AGENTS.md`'s per-slice sentence now names this file alone, and its ADR sentence was rebuilt from the index rather than from memory: **61 records, 0001-0061; six superseded (0001 and 0005 by 0012, 0007 by 0016, 0012 and 0020 by 0055, 0050 by 0054); six Accepted with a qualifier (0004, 0006, 0008, 0010, 0011, 0013); none Proposed**. It came out three bytes smaller than it went in, which its own 8 KB rule made a constraint — it sat at 8,188 of 8,192 before the edit and the first draft broke the cap by 71. Counting the index found **three rows wrong in it**: ADR-0046 amended the Status lines of 0006, 0010 and 0011 on 2026-09-02, the three ADR files carry it, and the index absorbed it for none of them, so for twelve days the index and the records disagreed about whether three decisions were deferred — corrected here, no decision changed, and recorded in 0051. In the vault, `MVP Spec.md` takes dated correction lines with the original prose left standing: #90's three stale claims (the processing-location default settled 2026-09-01 by 0044; the 0010/0011/0006 membership question settled 2026-09-02 by 0046, *deferred with triggers* and not *out of scope pending a decision*, so the spec's instruction to treat all three as out of scope is explicitly withdrawn; the glossary `Issue` duplicate fixed 2026-09-05), the framing sentence, and the cross-reference that was **wrong the day it was written** — only one of the two was ever on the README's `## Open, deliberately` list. The end-to-end sweep #90 asked for found three more: ADR-0020 superseded 2026-09-12 by 0055 where the spec still reads Accepted, the transcription vendor settled 2026-09-13 by 0061, and the **thin browser-driven pass that never existed** — 0049 built a component-level suite instead and 0052 refused end-to-end again, so that path is covered at the record level and by hand and by nothing else. `docs/agents/domain.md` changed with them: it called `CONTEXT.md` the decision table, and its ADR count had read 28 since long before this ticket. Nothing under `.claude/rules/` changed, and a sweep confirmed no rule file reads `CONTEXT.md` — consequence (3) of 0051, verified rather than assumed. **Two-axis review changed the outcome rather than confirming it.** The spec axis found the deletion would have destroyed a fact: `docs/changelog.md` had **no row for slices 13-16** (issues #14-#17) and none dated 2026-08-31, so the milestone table was this repository's only record of four slices and ADR-0051 decision (3)'s premise — *"the milestone table's rows are already there, one per slice"* — was false for exactly those four. Four rows were backfilled from the PRD's slice narratives and labelled as backfill, and 0051 took a dated correction. It also found step 2 was the one step of six carrying no completion statement in the PRD, the fact living only in the deleted table: a marker was added there. The standards axis found *fifty-row* wrong in three places and in 0051 itself; `CONTEXT.md` now names the table with no figure at all, and 0051 records the real ones. Two smaller losses were caught and repaired: the email vendors' candidate names (**SES vs Postmark vs SendGrid**) went into the vault README's `## Open, deliberately`, and `AGENTS.md`'s *"updates the vault progress section"* now names `PRD and Architecture.md`. One tension is **recorded and deliberately not resolved**: `AGENTS.md`'s status sentence is itself a copy of the index, which decision (5) forbids by name, and it stays only because this ticket's criterion requires it in those words — 0051 carries the trigger that retires it. Review also found the ADR **count** written in four places once this landed; three were decoration and now carry no figure, leaving exactly one copy — the one the criterion compels. Docs only: no source file, test or migration is touched. |
| 2026-08-31 | **Backfilled 2026-09-14 by issue #110.** This row was written from `PRD and Architecture.md`'s slice narrative and the milestone table then being deleted from `CONTEXT.md`, not on the day the slice landed — review of #110 found slices 13-16 had **no row here at all**, so the milestone table was this repository's only record of them and ADR-0051's premise that *"the milestone table's rows are already there, one per slice"* was false for exactly these four. It is a reconstruction and says less than the rows around it. Slice 13 built (issue #14): **registers, entries and the ball-in-court history**, opening step 4. Both registers — submittals and RFIs — are written in the same transaction as the project and backfilled onto jobs predating the migration, because which correspondence types exist is a fact about the product and not a choice about a job; there is no route that creates one. Beneath them entries, and beneath each entry the handoffs. **Ball-in-court is the last handoff, derived on every read and stored nowhere** — the decision this codebase had argued against four times, ADR-0031 having refused a transition log by name as "the `tasks` record the data model excludes arriving by the back door", and 0024, 0031, 0034 and 0035 each having refused a status column. ADR-0036 answers it rather than skirting it: an arithmetic reads this one. See ADR-0036 and the PRD's slice 13 narrative for the full record. |
| 2026-08-31 | **Backfilled 2026-09-14 by issue #110.** This row was written from `PRD and Architecture.md`'s slice narrative and the milestone table then being deleted from `CONTEXT.md`, not on the day the slice landed — review of #110 found slices 13-16 had **no row here at all**, so the milestone table was this repository's only record of them and ADR-0051's premise that *"the milestone table's rows are already there, one per slice"* was false for exactly these four. It is a reconstruction and says less than the rows around it. Slice 14 built (issue #15): **the clock and dispositions**, which **completes step 4**. An entry takes a contractual turnaround; the time it has spent in our court is summed off the handoffs beneath it; `GET /v1/clock` is the entries past that target across every live job; recording a disposition closes the loop in one action; and a Revise and Resubmit's next round is a new entry pointing backwards. The first slice in a while whose shape was decided before it started — ADR-0036 had written two answers down as forecasts and neither changed on contact. The clock is the fourth value derived on every read and the first that is an **arithmetic** rather than a lookup; the sketch's `clock_started` cannot exist, the ball reaching us more than once. See ADR-0037. |
| 2026-08-31 | **Backfilled 2026-09-14 by issue #110.** This row was written from `PRD and Architecture.md`'s slice narrative and the milestone table then being deleted from `CONTEXT.md`, not on the day the slice landed — review of #110 found slices 13-16 had **no row here at all**, so the milestone table was this repository's only record of them and ADR-0051's premise that *"the milestone table's rows are already there, one per slice"* was false for exactly these four. It is a reconstruction and says less than the rows around it. Slice 15 built (issue #16): **the morning screen**, the first slice whose record types were all already built. Exposure landed with slice 5 and the clock with slice 14, both already taking an optional `?projectId=`, 404ing on an unknown job and dropping archived projects from the across-every-project read while keeping their own — so **the API needed no change at all**: no route, no column, no migration. ADR-0038 records that as a decision rather than an absence, because `GET /v1/morning` answering `{ exposure: 3, clock: 2 }` would be the first object in this product from which a score could be computed without adding a query, and the superseded health score is exactly one field appended to it. `/` is the landing view **re-headed**, not a route of its own. See ADR-0038. |
| 2026-09-01 | **Backfilled 2026-09-14 by issue #110.** This row was written from `PRD and Architecture.md`'s slice narrative and the milestone table then being deleted from `CONTEXT.md`, not on the day the slice landed — review of #110 found slices 13-16 had **no row here at all**, so the milestone table was this repository's only record of them and ADR-0051's premise that *"the milestone table's rows are already there, one per slice"* was false for exactly these four. It is a reconstruction and says less than the rows around it. Slice 16 built (issue #17): **referenced files** — documents, immutable versions and retrieval by identity — built **outside the consent gate**, because nothing in it reads a document's contents. The thinnest prior authority any record type has had: one sketch line of three table names with no columns. Two of the three were built and **the third was refused** — ADR-0039 makes `referenced_file` a column, the glossary defining a referenced file as *a document*, so a second table would give one document two answers to "is this one?". Required with no default, because a default classifies by omission and the omitted answer puts an 86-sheet set in front of extraction. `documents` is the identity, `document_versions` holds the file, and `revision` unique within its document is the whole of "a new version never overwrites a prior one". See ADR-0039. |
| 2026-09-15 | **Issue #111: the audit line gains an actor and a subject, and the agent acts under a person (ADR-0055 part 2).** The one product whose value is a defensible record had no answer to *who recorded this* anywhere: every line said what and when, and not one of thirty-odd models carried an author. `audit_entries` now carries `actor_id`, `subject_type` and `subject_id`, and the run a line was written during. The actor is read from the session the gate validated and **never from a request body** — `actorOf` in `gate.ts` is the one way one is built — and **no model gains a `created_by`**, which a test reading `schema.prisma` holds: that would be thirty-odd copies of a fact one table already holds in the same transaction as the write it describes. Taken as **expand → migrate → contract inside the ticket**, three commits, each with the suite green: nullable columns, then all sixty-seven writers, then the fields required. The ticket said 61 call sites in 15 route files; there are **67 in 18 files**, because ADR-0055 counted them on 2026-09-08 and #105, #107, #108 and #109 each added more — and three are not on a route at all, which is why the sweep that proves no writer omits a subject **reads the source** where `audit.test.ts`'s existing sweep reads the routes Fastify registered. Three decisions the ADR did not take. The line carries **`extraction_id` beside `agent_run_id`**, under the CHECK `sessions` already has: an extraction is a run record of its own (ADR-0043), so one column would leave every line the extraction agent writes carrying a person and no run. `subject_type` and `subject_id` are **nullable in the database and required in TypeScript** — the lines written before this touched rows nothing now knows, and writing "project" onto a line that was about an observation is a wrong answer rather than a missing one (ADR-0039's rule, on a table where a wrong answer cannot be taken back); `actor_id` **is** backfilled, because there the answer is known. And signing in and signing out name the **user**, never the session: a session id is the credential itself, and a test searches the whole trail for the one it presented. The actorless lines come from one route and three commands — the ingest webhook, where a provider presents nothing (ADR-0042), and `user create`, `user reset` and `user enable` on the machine, the first account having by definition nobody to be recorded against — and a test asserts the exact four. The **export** gains `users` with `passwordHash` dropped the way `ingestToken` is, asserted by the same whole-document search; `sessions` stays out, reversing ADR-0055's own sentence, because a `sessions` row *is* its id and nothing survives dropping it. The **feed line** shows who by name, says "during a run" where there was one, and links to the row it is about where a screen exists keyed by that row's id — a finding's does not, its URL carrying its number, and that partiality is on the screen's side rather than the record's. Both readers go through one mapper beside the writer, so the feed and the audit read cannot drift (ADR-0048). The criterion about the agent's shared-secret header was **already met** by #105 and is confirmed rather than built: a sweep of all 259 tracked files found no live shared-secret header of any spelling. 661 API tests and 65 web tests pass; `pnpm --filter web build` green; the feed verified in a browser against the migrated development database, where all 42 pre-existing lines backfilled to the author and none gained a subject. |
| 2026-09-15 | **Issue #112: the record says whose it is, where the record is about a person (ADR-0055 part 5).** Issue #111 put an actor on the audit line and stopped there — "who recorded this" is a read of the audit and no model gains a `created_by`. Three columns answer a different question. `site_visits.conducted_by` is the **user who walked the building**, the acting user at creation and correctable afterwards, and the **report prints that name** — read through the relation at the moment of rendering, so a correction reaches the next rendering and the one already issued keeps saying what it said, while who *asked* for a rendering stays an audit fact. `open_items.owner_id` replaces the free-text `owner`, whose reason was ADR-0012's "there is no `users` table"; it is required where the text was optional, set from the session because an item is raised by somebody, and handed on by a named route whose line carries both names. The old text is dropped and not migrated: it held initials with nothing to resolve them against. `ball_in_court_events.user_id` is set **exactly** when the ball is ours, refused in both directions at the boundary and by a CHECK — ADR-0030's one-axis rule for a second record — and **supplied in the body, never read off the session**, for ADR-0037's reason the party beside it is: a transmittal log written up on Friday brings Monday's ball to whoever it came to, who is often not the person typing. An extraction proposes a party and never a person and is refused one, there being no column for an answer the agent cannot have. All three go out **named** — `namedUser` is the one include and `userOnTheWire` the one projection, which matters because an open item is read back in ten places. `?mine=` landed on `/v1/exposure`, `/v1/clock` and `/v1/open-items`, defaulting to **false** there and to true on the screens: each route means every job's, and which rows an engineer is shown first is the screen's decision — ADR-0038's no-endpoint rule applied to a filter rather than to a payload, with both morning cards drilling through carrying the toggle. The clock's *mine* reads the **current** handoff, since an entry handed to a colleague this morning is sitting in their court; `inCourtMs` is untouched. And the phase `position` race ADR-0026 left open "because this is a single-user tool" closed: it was two concurrent **creates** and not two concurrent reorders — six posted at once all landed on position 1 — so both writers now take a `pg_advisory_xact_lock` on the job, the create's `count()` moved inside it, and the reorder's read of the phases there are with it. Not a unique constraint: a non-deferrable one rejects the reorder mid-flight and a deferrable one is a constraint Prisma cannot express. A site visit gained the exact-key-set test it never had. 675 API and 66 web tests pass. |
| 2026-09-15 | **Issue #113: a photograph evidences what was seen, and a floor-only photograph is unfiled (ADR-0056).** ADR-0032 refused evidence on an observation for one stated reason — *"there is no third mechanism that would bind a photograph to one observation out of the dozen made on a floor"* — and the very next slice built one, `voice_captures.observation_id`, and framed it as a draft mechanism so nobody read it as the binding 0032 said did not exist. The consequence it accepted beside that was worse: it sent the report to join `site_visit_floors.floor` to `observations.floor` by value, the join was never made, and **half the binning mechanism produced nothing in the deliverable** — a photograph bound to a floor and no finding printed nowhere at all. `photos.observation_id` now sits beside `issue_id` under `num_nonnulls(...) <= 1`; **at most one**, because a photograph on a sighting of Issue N also stamped to Issue M would print under two findings. Still **no join table**: a photograph evidences one thing, and that is the same answer 0032 gave rather than a reversal of it. **A finding's evidence is derived** — what is stamped to it, union its sightings' — which is the one amendment to *stamped, never derived* and for this path only; **promotion writes nothing to a photograph**, so the order the work actually happens in (see, photograph, write up, promote) reaches the report with no filename at all. Four decisions the ADR did not take. Each of the two evidence routes **clears the other**, which is the whole of "moving a photograph is one action" — the CHECK is therefore unreachable from the boundary, which is the point of having it. On the screen they are **one** select and not two, ADR-0030's Side/Sector reasoning on a second record, with the encoding in a plain module because a `'use server'` file cannot import out of a `'use client'` one. `GET /v1/site-visits/:id/issues-without-photos` had to read **both** halves: its stamped-only clause had started listing findings whose sighting today *is* photographed, sending the engineer back for a picture they already took. And a photograph binned to **no** floor — or to a floor nobody formally started — has no schedule row to be counted in, so counting only per row would have left the page silent about the most unfiled photograph of all; both are summed into one line beneath the table, a deliberate step past what 0056 says and recorded there. The non-issue table's Evidence column is the **opposite** answer to the unfiled count's rendered zero, printed only when something is in it: a count is the signal, and a column of blanks under that heading in a document issued under the author's professional name reads as evidence that went missing. Two readers take the derived union — `withSightings` across every walk, `evidenceFor` narrowed to one afternoon — deliberately not shared, and neither deduplicates because the CHECK makes the halves disjoint. A photograph and the observation it evidences are refused across walks at the boundary, which is what makes a `where` on the observation's photographs unnecessary anywhere. One trap worth the line: Fastify's ajv **coerces types**, so `{ type: 'string' }` beside `{ type: 'null' }` made `null` match both branches of the `oneOf` and clearing the binding was a 400 — the floor and the finding schemas are each saved from this by accident, by `NOT_BLANK` and by `minimum: 1`. 686 API tests and 66 web tests pass; typecheck and `pnpm --filter web build` green; both screens driven in a real browser and both report behaviours read out of a real PDF. Closes #96. The incidental 0056 recorded and refused to fix — a filename naming Issue N before N is raised stays unbound forever — is filed as **#138**. |
| 2026-09-16 | **Issue #114: a conversation is a record on the visit, and the chat proposes a draft the engineer commits (ADR-0057 as amended by ADR-0058).** ADR-0018 deferred a conversational panel because *"a chat panel over a database that was never populated is worthless"*; twenty-two slices populated the schema, and issue #99's claim was never a panel over the data anyway — it is how the data arrives. So the copilot arrived **as capture**, which is ADR-0018's own order reached from the other side. `conversations` is a record: one per site visit, written in the same statement as the walk, `site_visit_id` nullable and unique so the project chat lands there without a second migration. `voice_captures` became **`turns`** under it — renamed and not copied, so every row keeps its id and a photograph or an audit line already pointing at what one became still points at it — and `site_visit_id` came off, the conversation carrying the walk, with the resend rule rekeyed to `(conversation_id, capture_key)`. A **capture is one record, spoken or typed** (ADR-0057): `kind` is a database enum, `storage_key`, `content_type`, `byte_size`, `recorded_at` and `capture_key` are null by role under CHECKs, and `transcript` holds the words whichever way they arrived and is still never rewritten. A typed capture queues a **proposal run**; a recording queues transcription and nothing else, which is ADR-0057's asymmetry and leaves the voice path exactly as it was — its 26 tests pass unchanged against the renamed record. The agent's reply is **a turn**, carrying either the draft's fields or one question when a field cannot be proposed, and the engineer's answer is the next capture rather than an edit: nothing edits a turn. Every agent turn is an **`agent_runs` row** (ADR-0058), which meant answering ADR-0043's reason for not reusing that table — *"no `kind` column, so a memory screen reading it per project could not keep an extraction run out of its list"* — with a **link and not a kind**: `agent_runs.conversation_id`, null on a memory run, narrowed through one shared constant by both readers of the memory screen. No third run record, so `sessions_one_run` and `audit_entries_one_run` are untouched. The run's tools are **three and one of them writes**: `site_visits_get_floors` (projected down to the schedule, `projects_get`'s shape — the walk's own read would hand the run its own conversation back as context), `issues_list`, and `capture_propose`, with a test asserting the list exactly; the conversation reaches the model as delimited untrusted data under `CAPTURE_DIRECTIVE`, ADR-0043's directive with the noun changed. *The agent never writes an observation* is true at the boundary **and** underneath: the confirm route refuses an agent turn by name and a CHECK refuses `observation_id` on one, and a test feeds an injection the agent obeys and asserts the walk still has no observation. Three things this slice settled that neither ADR said. A conversation reads in **`position` order and no longer by `recorded_at`** — a reply must follow the capture it answers, and a recording held in a basement would otherwise sort above a reply to something said after it; position is taken under `pg_advisory_xact_lock`, `routes/phases.ts`' shape. A **proposal's location is composed by the API** through the same `renderLocation` an observation's is: leaving it to the screen printed `Floor 3 — South stair, A` against the record's `Side A`, the exact drift ADR-0030 keeps one renderer against. And the panel's *reading* is the **run's state** and never the absence of a reply — a failed run leaves no turn, so a count of unanswered captures claimed the agent was still working on one forever, found in a browser against the refusing default adapter every deployment without `AGENT=pi` is running. 708 API tests across 27 files (up from 686) and 74 web across 12 (up from 66); `.claude/rules/voice.md` became `conversations.md`. Verified in a browser end to end on the dev database: the migration backfilled a conversation onto every existing walk, a typed capture landed and read back verbatim, the failed run said so rather than reading forever, and confirming wrote the observation with the grammar composed correctly. The **project** conversation, `assumption_record_propose` and ADR-0058's one generated tool registry are not built — that is the project chat's ticket. |
| 2026-09-17 | **Issue #117: the design system — the brief's tokens, the dark theme activated, and every screen measured (ADR-0059 point 4).** ADR-0025 chose Tailwind and shadcn in 2026-08 and nobody ever did the design on it: the `.dark` block shipped inert for twenty-two slices because nothing in `apps/web` ever set that class, and the 2026-09-17 baseline found **seven of sixteen screens failing WCAG AA** on one token. Both close here. The approved brief's `## Colour` tables are now the product's tokens — **six names, eleven declarations** — and the finding behind them is that **both `--destructive` values were outside sRGB**: nothing above chroma 0.235 is in gamut at the light one's lightness, so what the browser painted was a clamped `rgb(231, 0, 11)` the token never described. Both replacements are in gamut, and the browser resolves all ten moved values to exactly the rgb the brief names. The palette is written with **`light-dark()` and `color-scheme`**, one declaration a token carrying both halves, rather than a `.dark` block duplicated under a media query: the system preference and the override then reach the same values by the same route with no second copy to drift, and `color-scheme` is load-bearing rather than decorative — this product keeps native selects everywhere (ADR-0025) and without it the browser paints them as light widgets on a dark page. **The override is in this ticket and not a sibling**, the author's call on 2026-09-17: ADR-0059 point 4 said "by system preference" and the brief's decision 2 added a three-value control on the **user**, so `users.theme` is an enum column defaulting to `SYSTEM`, `POST /v1/users/current/theme` names **no id** — a theme is the caller's own, `callerOf` supplies the row and `actorOf` the actor, so nothing is read off a body (ADR-0055 part 2) — and it writes one audit line, none on a no-op (ADR-0040's rule). The class is written on `<html>` **by the server**: the root layout already reads the session on every request, so the theme is known before the first byte and there is no flash, no client hook and no hydration warning to suppress; *System* is **no class at all**, which is what leaves `prefers-color-scheme` to answer and is why it is not a third colour. The control is **three submit buttons** and not a dropdown — every other closed vocabulary here is the native select element, but those are fields inside a form that serialises something else, and this one is the whole form, so a dropdown would cost a second tap or JavaScript. It is the one layout change #117 makes, and it is the cost of the choice. **The measurement is the deliverable ADR-0059 point 4 actually asked for**, and the brief said so of itself: every figure in it is computed from oklch, and the discharge is a browser reading. `scripts/contrast.mjs` is the procedure written down — the baseline was driven by hand and left nothing that reproduces it, which was only found when this ticket had to write the harness from scratch. **Sixteen of sixteen pass in both themes**: the floor moved from 3.97:1 to **5.03:1** light and **5.79:1** dark, not one of 761 text nodes a theme is below its threshold, and field outlines went from 1.26:1 to 3.13:1 and 3.67:1 against the 3:1 AA asks of a UI component. Every dark figure was taken **twice** — once with the class, once with `prefers-color-scheme` and no class — and all fifteen signed-in screens agree to the second decimal, which is what proves the two paths reach the same tokens rather than one of them being the one that was built. Three smaller things. The browser agrees with the brief's arithmetic to within **0.04**, smaller than the 0.05 it predicted for the oklab-versus-sRGB compositing difference. A field outline is read against the surface **outside** it, which is what WCAG 1.4.11 means by an adjacent colour: in dark a field also carries `dark:bg-input/30`, so its own fill is the outline's colour at 30% and the inside reading is 2.62:1 — a fact about a fill deliberately of the same hue, not a field that cannot be found. And the brief has **one factual error**: it calls `bg-amber-500` on the budget meter "the one hard-coded colour in the app", where `memory-versions.tsx` has carried an emerald and a red for a memory diff's added and removed lines since that screen was built. The meter became `bg-chart-2`; the diff was left alone and a test pins it as the one named exception, because a diff needs two hues to be a diff and this palette has one — a decision the brief never took. No screen's layout changed otherwise: the two measures the brief names are declared for the screen-group tickets and applied by none of them here, and the only other edits are the two `h1`s the brief calls wrong. 712 API tests across 27 files and 85 web across 13; typecheck and `pnpm --filter web build` green; both themes driven in a real browser on the dev database. |
| 2026-09-19 | **Issue #118: the field screens redesigned to the approved plates (ADR-0059 point 3).** The first redesign ticket, and the first screen in the product built to a drawing: `docs/design/design-brief.md` and plates F-01…F-04 in the vault, approved 2026-09-17 under issue #116. The walk is now the **record measure**, 704 px — `--measure-record` shipped inert at #117 and this is its first consumer, the layout staying 1024 px so the desk keeps its width. Above it a **sticky 44 px section jumper**, plain anchors and no JavaScript: Floors · Observations · Conversation · Photographs · Report, which is the whole of density rule 5 and the whole of bar 3's *taps to reach* — Photographs began 2 733 px down at the baseline and is now one tap. Five `text-lg` heads became the brief's **Section** step, 12/16 uppercase with a rule under and the count pushed to the end (`app/section-head.tsx`); an observation's words and a turn's became the **Record** step, 16/24, which is the step the product did not have and the reason an observation read at the size of the label above it. Four creation forms sit behind native `<details>`, closed (`app/disclosure.tsx`, density rule 1) — starting a floor, adding an observation, adding the walk's photographs, and promoting one, which creates a record like the others. Floors reads as a table carrying each floor's **unfiled** count and rendering a zero, with the line under it for what binned to no floor at all — the report's own schedule table arriving on the screen. The conversation became **one component** (`app/conversation-panel.tsx`), lifted out of the 847-line screen because ADR-0058 needs it on two records and ADR-0059 point 2 put it in the brief rather than in either ticket; only the visit is wired, and the slot that differs — an observation on a visit, an assumption record on a project — is deliberately not parameterised until there is a second caller. Inside it **the commit moved under the agent turn that proposed it** and still writes the engineer's capture, the confirm route refusing an agent turn by name; and record and type became **one capture bar**, where they were a card with a rule across it. Every field control is at the 44 px target, the photograph-binding select included — `fieldSelectClassName` beside the desk's 32 px `selectClassName`, two numbers and not two designs. **No route changed**: F-02 draws a correction affordance, that is ADR-0059 decision 3, and it needs its own ADR first. Measured: the walk went **3 676 px → 2 976 px** on the record the baseline was taken against, every text node clears AA in both themes on all sixteen screens, and bars 3 and 6 are re-timed beside the baseline in the vault's `docs/walk-protocol.md`. The ≤ 1 600 px target is **not met** and the arithmetic is written down there. |
| 2026-09-19 | **Issue #119: the report redesigned as issued output (ADR-0059 point 3).** ADR-0025 called it the harder problem in 2026-08 and nobody had read it: twenty-three slices printed a PDF nobody opened as a document. The acceptance is a reading and not a figure — ADR-0059 point 5 has no baseline for this group, because the 2026-09-17 baseline measured screens — so two walks were rendered through the real worker, the real Chrome and the real store and read page by page: one the size of a real walk (three floors, four non-issues, four findings across five categories, portrait and landscape evidence stamped and bound, filed and unfiled) and one **built to break**, a single finding taller than A4, since three of the brief's four questions are about a page boundary and no walk in the database has one. Both read as raster **and as extracted text**, which is the half a picture cannot show. The correctness finding is that **`text-transform: uppercase` reaches a PDF's text layer**: the header printed `MERCY GENERAL — 4TH FLOOR ICU RENOVATION`, `pdftotext` read it back that way, and a reader searching the one artifact this product issues for the job as written found nothing — while the footer printed the same name in its own case, leaving one document spelling the job two ways. Exactly ADR-0035's `letter-spacing` finding arriving through a second property, which strengthens that record rather than amending it. The brief's four questions, answered: a finding **does** split when it is taller than A4 and `break-inside: avoid-page` never could stop it — what it was doing instead was splitting **inside a sighting**, ending a page with a bare `10:35 · Floor 1 — Corridor 1A, Side A` and beginning the next with the words that line labels, so `.sighting` gains `break-inside: avoid` and a long finding now breaks *between* sightings; the evidence did **not** land beside its sighting, printing as one row under the finding where a finding seen twice gave no way to tell which look produced which picture, so a sighting's photographs print inside it and the finding's own — the ones a filename stamped to it, which are on no sighting — under the finding, ADR-0056's union unchanged in membership and in order and no longer doing double duty as the grouping; the non-issues table **is** legible at 10.5 pt with four columns (Time 21 mm, Location 50 mm, Observed 68 mm, Evidence 39 mm inside the margin, about 38 characters of prose) and stays as it is. Six more lines changed: `h2` gains `break-after: avoid`, *Issues* having printed alone at the foot of a page with its rule under it and the first finding overleaf; the **footer runs on every page** through Chrome's `footerTemplate` with the job, the visit date, `rendering_since` and `page N of M` — Chrome implements no `@page` margin boxes, so the flowing `<footer>` printed once and left pages 2 and 3 anonymous sheets, and ADR-0035's note against `displayHeaderFooter` was against Chrome's *defaults*, met by saying what goes there; the day prints **in words** on the page and stays the ISO face on the wire and in the `<title>`; the schedule is sized to its four columns, its unfiled count having sat 12 cm of white from the floor it counts; the floor designation prints bare under a column headed FLOOR; and a figure is **as wide as its photograph** at a fixed 45 mm, so the rule hugs the picture instead of framing an empty box and a row of filenames sits on one line. Three places the approved plate loses, each recorded rather than quietly followed: the section heads stay **un-uppercased** (ADR-0035's reason, pinned by a test since #13 — the same rule seen from the other side), the footer names **no zone** (ADR-0054 prints it once, and a running footer would print it once a page — the existing exactly-once assertion is what caught it), and the footer *does* carry the job and the visit date, which is the whole reason it became a running footer. The evidence bound tightens to **45 mm** inside ADR-0035's 70 mm rather than against it. `rendering_since` is read once and used twice — stamped on the row and handed to the composer — so the record and the page it is a record of cannot disagree about when; no clock is read inside the composer. **No route changed and no record changed**; the rendering stamp is the one line of new content, taken on plate R-01's authority and named in the ADR as such. Four tests added, all against the document: the job comes back out of the text layer as entered, a sighting's photographs print with that sighting, a sighting is never split from the location that labels it, and every page says which rendering it is and where it falls in it — the last two driven by fixtures that page-break on purpose. ADR-0035's own tests are untouched and still pin the identifier, the sightings and the evidence paths. 716 API tests across 27 files and 92 web across 14; typecheck, `pnpm --filter web build` and `./scripts/helper-tests.sh` green. |
| 2026-09-20 | **Issue #120: the desk screens redesigned to plates D-01…D-04**, which completes ADR-0059 point 3 — field, report and desk are all built. The morning screen, the project record, the registers and the submission are built to the approved brief; the *last* `text-lg` heads go with them, from the finding record, the extraction confirmation and the error boundary, so the step **leaves the product entirely** and `desk-screens.test.tsx` sweeps every product source for it — superseding the nine-file field list `walk-screen.test.tsx` carried while 30 heads were still on the desk. Measured on job **260117**, the baseline's own, with `main` built and served immediately before on the same rows and reading within **+4 px** of all four recorded figures. **Project record 4 479 → 1 314 px** (≤ 1 800), **submission 2 715 → 1 075 px** (≤ 1 400), **register entry with its disposition recorded 1 931 → 800 px** (≤ 1 100); with the disposition form still up it is 1 117 px, 17 px over, and density rule 2's own words are *before any form is opened* — that form is the one on the desk deliberately not behind a disclosure, because bar 4 asks for its two inputs visible at once. Head and count strips at desk width, sections at `--measure-record`; the project record is the only screen using both. **Density rule 4 is built** as a query parameter and not a record: resolving an open item lands back carrying `?kept=<id>`, which keeps that row where it was with its undo for one load, and *Resolved* is now a collapsed disclosure directly under *Open items* rather than 4 009 px down — the baseline's *hunting*. **The nav wraps**, the finding #118 handed over: the document is 390 px at a 390 px viewport on every screen, the walk included, where the shell's 735 px min-content width had made all of them scroll sideways. **ADR-0038 is untouched** — no `/morning`, the morning cards still render at zero where the project strips are still gated on non-empty, and neither test file's assertions changed. Bars 1, 2, 4 and 5 re-timed beside the baseline in the vault: 0.4 → **0.10 s**, 1.2 → **0.53 s**, 111 ms → **0.05 s** with the form 435 px down 2 265 px → **322 px down 1 117 px**, and the counts **equal six times**. Contrast re-run with `scripts/contrast.mjs`: **47 of 47 pass**, floors 5.10:1 light and 5.79:1 dark, unmoved. **No Conversation on the project record** — that is issue #121's, and a test asserts the absence — and **no edit path anywhere**: the brief's decision 3 still has no ADR, so of 117 routes there is still no PUT and no PATCH; what landed is decision 3's *words*, which need no route. 716 API tests across 27 files and **101 web across 15**; typecheck and `pnpm --filter web build` green. |
| 2026-09-20 | **Issue #121: the project chat** — a conversation on a project that reads, asks the helpers and proposes an assumption record (ADR-0058 part 4, the last of that record still unbuilt). `conversations.site_visit_id` was already nullable and unique, so the second context needed no table: what the migration adds is the agent turn's four `proposed_*` columns for a record, `assumption_records.turn_id` **unique** — the provenance ADR-0058 asks for, null on the paste path, which stands — and a loosened `turns_engineer_captures`, which now takes the capture machinery **whole or not at all** so a project turn may have no kind and no instant and must have its words. The run's tools are **thirteen and one of them writes**: `projectReadTools` (the memory run's own eight reads, shared by construction rather than copied, which is the only way *every read the memory agent has* stays true), `documents_list`, the three helper routes from the manifests, and `assumption_record_propose`. This is the **first run in the product that may call a helper**, which ADR-0053 predicted by name and left `helperTools` generated and uncalled for. The conversation reaches the model as delimited untrusted data under `CHAT_DIRECTIVE` — a third sentence, `EXTRACTION_DIRECTIVE`'s with the noun changed, as `CAPTURE_DIRECTIVE` is — and the **allowlist is the disabling** — `piAgentRunService` passes exactly the list the test asserts, which is the same proof the three runs before it have and no more: no test builds a Pi session. Confirming is **the existing route**, `POST /v1/submissions/:id/assumption-records`, with `turnId` in the body: one writer of `assumption_records`, and a second confirm of the same proposal is a 409 the database holds. Asking for a calculation with **no submission named** answers in words carrying the two blocks verbatim and writes nothing, which is ADR-0053's *asking and recording are two acts* reaching the chat. On the screen the panel is now **one component with two callers**, which is what the design brief said it would be: almost every difference is data — a project turn has no `kind`, so no audio control, no capture state and no *Ask again*; an agent turn carries one proposal shape or the other, so which commit sits under it is read off the turn — and three things are the caller's: the live summary's words, the recorder (a walk's; plate D-02 draws the project bar as one text control), and the bound commit. Measured at 390 px on job **260117**: the document is 390 px, the panel costs **+261 px** idle, **+363 px** carrying a question, and **+1 224 px** with a live proposal open, which is what twenty-five lines of a sizer's two blocks are in editable boxes the engineer has to read before capturing. The record reads 1 831 px without it on today's rows — already past plate D-02's ≤ 1 800 closed, before this ticket. **Proposing any other record type is not built** and ADR-0058 part 6's trigger is unchanged; part 4's *"both tool lists are generated from one registry"* is **narrowed to the chat's list** by a dated amendment on ADR-0058 itself rather than a note under it — only one of the visit's three is a shared read and it carries a different description there, and the amendment names the trigger that would reverse it: the first change to a shared read's path, or a second genuinely shared entry. 739 API tests and 108 web tests green. |
| 2026-09-21 | **Issue #123: an audit line's prose reads the building's clock** (ADR-0054 decision 3, extended by a dated amendment). ADR-0054 made every stored `DateTime` a real instant and every screen render one back in the project's zone, and said nothing about the instants written **into** a stored string: twelve of the seventy-five writers of `audit_entries` composed one into `detail` with `toISOString()`, and the activity feed renders `detail` verbatim beside a column that has been in the building's zone since #104. So one row read `2026-07-23 13:00` beside `started 2026-07-23T17:00:00.000Z` — **two faces of one event on one line**, which is #97's `10:33 – 01:42` arriving by a different door, through prose instead of through a column. The decision the ticket asked for first: of its three honest answers, *render into the zone at write time* is taken and the other two are refused on the record. **Dropping the instant** — the answer ADR-0055's refusal of `created_by` most resembles — loses a compliance fact, that a walk was recorded days after it happened, and empties five of the twelve details outright; **leaving it** keeps the shape ADR-0054 was written against on the one screen that reads this table. The twelve render through `zone.ts` **at the precision the value was typed at**, which is the rule the sweep produced rather than a format chosen: `apps/web`'s `composeDay` types a day alone for a sign-off, a calculation and a held-since, so those three print a day and no clock time that nobody entered, and every other one of the twelve prints both. The rule has **no exception**, which a first pass got wrong: the report line names its walk to the minute although a walk is *filed* under its day (ADR-0030), because two walks on one building in one day would otherwise be one sentence, and `site visit recorded` names that same walk to the minute a few lines above it. `readIn` is the new pair in the leaf, deliberately the same order and separator the feed's own column uses. **No column moved and no migration runs** — this is characters in a `text` column, where decision 2 is about `DateTime` ones — and `audit()` is untouched, still a leaf that never sees a zone: the cost is **nine Prisma selects widened** and one read introduced in the archive route, which was the one of the twelve with no project in scope at all and now reads inside the transaction, only on the branch that writes. The screen **names the zone once** above the list rather than on every row, which is ADR-0059's *the header states the frame once and it governs* applied to a screen. Two tests own the halves the architecture keeps apart: `audit.test.ts` pins the prose with the injected clock **nine hours off the typed side**, so a route rendering its stamp instead reads a different sentence (ADR-0052 point 4), and `activity-screen.test.tsx` pins the row — the same face twice, and the zone named exactly once — which is the half the API cannot see. `projects.test.ts`'s sign-off fixture moved from midnight UTC to **midnight where the building is**, which is what `composeDay` actually sends; at the old value the line would have correctly printed a date nobody typed. **Every line written before today keeps its UTC face** and nothing goes back for them: 8 of 35 on job 260113, read in a browser after the change, beside a new line reading `started 2026-07-23 13:00`. That is decision 4's *a later frame defect is a dated correction, never a rewrite* reaching the prose, and it is written on the amendment as the cost, with the trigger that would reverse the whole answer — a project whose `timezone` turns out to be wrong leaves prose written under it unfixable where the columns are still recoverable. 741 API tests across 27 files and 109 web across 16; typecheck, `pnpm --filter web build` and `./scripts/helper-tests.sh` green. |
| 2026-09-22 | **Issue #125: a failed sign-in is a record, and the throttle counts it** (ADR-0062, the decision the ticket said had to come first). `POST /v1/sessions` accepted as many attempts as anybody cared to make, and since #105 the credential in front of everything has been a password with a twelve-character minimum rather than 32 random bytes — ADR-0055 named that as a gap it did not price and named exactly why it could not just add a limit: *"a failed sign-in writes nothing today, because a refused write writes no audit line, so there is nothing to count"*, and ADR-0042's limit counts rows in the trailing hour while explicitly refusing a counter beside them. So the ADR settles the prior question. **`sign_in_failures` is a record**: the address exactly as presented, where the attempt came from, and when — **no password and no hash of one**, and not an audit line, which it could not be, nothing having been mutated and the caller having no session to take an actor from. The argument had to be made rather than inherited, because ADR-0047 had already ruled the sibling question the other way (`sessions` is not exported, *"a session is not a record of the work"*): a session row **is** its credential and nothing survives stripping it, where this row opens nothing and is exactly the material of *who has been trying to sign in as me*. **Ten refusals against one address from one source in a quarter-hour, thirty from one source across every address**, counted before the hash is verified. **The source is on the address count, and that was a defect before it was a decision** — counting the address alone is the obvious shape and is wrong here: somebody who knew an engineer's address could spend its ten and hold them out of a walk for as long as they cared to, a request every ninety seconds renewing it and `user reset` not clearing it, the count being on the address and not the credential. That is a stranger reaching the outcome the record's own last section forbids, and it is the property the record already spotted for the advisory lock it refused, arriving through the count instead; what the keying gives up is bounding a *distributed* guess at one account, which the same record already names as bounded by nothing. The **window is fifteen minutes and not ADR-0042's hour**, which is the one number here with an argument behind it — that limit being wrong costs a late document, this one being wrong costs an engineer stood in a building unable to start a walk, ADR-0020's *the one cost this product cannot pay* — and the rest are invented, as ADR-0042's were, that record naming no limit value anywhere. A **throttled caller is told nothing new**: the same 401 and the same sentence, so ADR-0055's note 1 stands word for word and `gate.test.ts`'s sweep still reads the ingest webhook as the one route an anonymous caller gets anything other than a 401 from. **Three things write no row** — an attempt already over the limit (which keeps the window rolling rather than extending itself every time somebody knocks), a body that is not credentials, and a successful sign-in. **The source is forwarded by `apps/web` on the sign-in call and on no other**, as `x-sign-in-source`: `index.ts` binds `127.0.0.1` and `fly.toml` publishes only Next, so `request.ip` in the API is loopback for the whole internet and a source is a fact only that side can hold; it is trusted as far as the session id already travelling the same way, and a forged one moves the forger to a fresh source count and leaves the address count untouched. Null under `pnpm dev`. **Two departures from ADR-0042, both on the record**: no advisory lock and one count rather than two — that limit stands *in the gate's place* so a burst through it is a burst through the gate, where this stands *behind* the password and argon2id, and the only key available here is the address the caller typed, so the lock would be one a stranger could hold against a real engineer's own sign-in. **Retention: there is none**, and that is the answer rather than an omission — nothing in this product has ever deleted a row on a schedule, `worker.ts` has no repeatable, and `ingested_documents` accumulates by the same silence; what makes it survivable is that a throttled attempt writes nothing and the source count caps one place at under three thousand rows a day, and what is **not** bounded is a distributed attempt, named as the gap with its trigger. **No lockout, ever**: nothing stamps a user, and a stranger's typing must never do what only ADR-0055 part 5's route may. An address over 254 characters (RFC 5321) is not credentials — the only bound on that field, this being the one route with no body schema. The record is **in the export whole**, nothing to drop, and on **no screen**, which is the answer ADR-0047 gave for the export itself; the first person to ask *who tried to sign in as me* is the named trigger for one. Nine API tests and one web: the limit fires and the eleventh attempt refuses the correct password too, the window rolls a quarter-hour later, two mistypes on a phone still sign in, one address under attack does not close another, thirty from one source stops that source whatever address it tries, a non-credentials body is recorded nowhere, a failure records the address unnormalised and the source and no password, a success records nothing, and **a stranger guessing at a known address from their own machine does not stop that engineer's phone signing in** — the test that reproduced the lockout before the source went on the count. `x-sign-in-source` gets the guard `x-session-id` has, named in two files and no third. All ages advance a fake clock (ADR-0022). 750 API tests across 27 files and 112 web across 17; typecheck, `pnpm --filter web build` and `./scripts/helper-tests.sh` green. |
| 2026-09-22 | **Issue #138: a filename naming a finding not yet raised stays unbound, and nothing back-fills** — decided and recorded, no code. A photograph named `issue-7` is bound to Issue 7 only if Issue 7 exists when the photograph is added; raising it later reads no photograph. Of the issue's two branches the no-back-fill one is taken, on the precedent both ADRs already set: ADR-0032 makes the binding **stamped when added and corrected in one action**, and a back-fill on promotion would be a **second writer of that stamped column**, silently moving a photograph the engineer may already have corrected by hand; ADR-0056 had just said promotion writes nothing to `photos`, and narrowed the blast radius the other way — a finding's evidence is derived through its sightings, so the ordinary order of work (photograph → bind to the observation → promote) reaches the issued report **with no filename at all**. That premise was verified before the decision was written, since the decision rests on it: `reports.test.ts` (*a finding's evidence includes its sightings' photographs*) renders the PDF and finds the photograph under `Issue 1`, and `photos.test.ts` (*promoting an observation writes nothing to its photographs*) pins that promotion leaves `issue_id` null — so **no test was added**, both properties being pinned already. Every write to `photos` is in `routes/photos.ts`. Recorded as dated lines on **ADR-0032** and **ADR-0056** (an amendment, not a superseding ADR, since nothing changes), in the glossary's **Photo** entry in the sentence that binds by the filename grammar, and as a bullet in `.claude/rules/photos.md`. The filename path keeps its escape hatch, the one-action correction from the photograph's row. |
| 2026-09-22 | **Issue #132: the text an extraction hands the model is bounded at 300,000 characters, and refused past it** (ADR-0063). `worker.ts` passed the whole of `ocr_text` to `extractionPrompt`, and nothing bounded it anywhere between the vendor and the model — invisible until a real specification produced 56,236 characters on `epmos-t1` (ADR-0060), with the vendor's own ceiling, Azure Document Intelligence S0's **2,000 pages / 500 MB**, orders of magnitude above that. Of the issue's three options the **bound with a refusal** is taken, on ADR-0042's *bounded, refused past the bound and never truncated* out of ADR-0039's base64 lesson: truncating would propose a record saying something the document did not, and a note saying *truncated* does not tell the engineer which field was read from which half. `EXTRACTION_TEXT_MAX` sits where the packet is built, **after** `ocr_text` is stored whole (ADR-0043's order untouched, so the confirmation screen still reviews all of it) and **before** the agent is called, so past it the row fails through the extract job's existing catch with a sentence naming the length — *"the document is too large to extract: its text is N characters and one run reads at most 300,000"* — and the model is never asked. **The number is derived, not chosen**, and the arithmetic is at the constant and in the ADR: no model is pinned in the repository, so the one `AGENT=pi` actually resolves to on the machine was read there by a probe that built a session and never prompted it — `kimi-for-coding`, a 262,144-token window with 32,768 of output; less two outputs for a run that is at least two calls, less the rest of the packet at every one of its bounds (269,814 characters: the envelope at ADR-0042's maximums, the one tool's schema, the SDK's system prompt), at three characters a token, the low end of the model vendor's documented figure — 320,010, rounded down. The measured document is 19% of it. That probe also found the machine's configured default model is **not the one that runs**, filed as #157 rather than changed. The sentence lives beside the constant and not in `refusals.ts`, which holds the refusals more than one writer says. One fixture at the bound in `extractions.test.ts` (ADR-0052): 300,000 characters reach the agent whole and finish, 300,001 fail with the sentence and are stored whole, and the agent is asked once — it reads *finished* for both rows with the check removed. A line in `.claude/rules/ingest.md` makes the bound findable. 751 API tests across 27 files and 112 web across 17; typecheck, `pnpm --filter web build` and `./scripts/helper-tests.sh` green. |
| 2026-09-23 | **Issue #154: an extraction's proposed Since is a date, and pre-fills as the date on the document on either side of UTC** (ADR-0064). The model reads a date off a letter and `extraction_propose` asked it for *"the date on the document, as an ISO timestamp"* without telling it the job's zone, so it wrote midnight UTC; `extractions.tsx` then read that instant in the project's zone, as ADR-0054 says an instant is read, and west of UTC that is the evening before — seen on `epmos-t1` over a synthetic RFI during #134, where a document dated the 22nd pre-filled *Since* as the 21st. Confirming is one click on a pre-filled form and writes the first handoff in the same transaction (ADR-0043), so the wrong day reached the register and aged the entry's clock. Of the issue's candidates, **the proposal is a date**: the tool asks for `YYYY-MM-DD`, `proposed_held_since` becomes a `DATE` column, the wire carries it as `YYYY-MM-DD`, and the screen pre-fills it as given; confirming is unchanged and still composes midnight in the job's zone through `composeDay`, as every typed day does. Refused on the record: **composing the instant in the API**, which would put a second copy of `instantFrom`'s daylight-saving rule in `apps/api`, whose `zone.ts` says the API never composes; **reading it as a UTC day on the screen**, a one-line change that keeps a date masquerading as an instant in a `DateTime` column and is wrong the first time the model writes an offset; and **telling the model the zone**, which hands a model timezone arithmetic. **The one field where the agent's boundary and the engineer's differ**: `proposalBodySchema` is `fieldsBodySchema` with `heldSince` as `format: 'date'`, built from it so every other field stays one schema, and an instant proposed is a 400. The migration reads every existing value as its **UTC date**, the date the model wrote — rows shifted by `20260912000000` to four or five hours past midnight keep their date, which was checked in a scratch database with the session set to Los Angeles; a value the model wrote with its own offset was never recoverable from the column. **Not run on `epmos-t1`**: it runs at the next deploy, and the one proposal there reads 2026-09-22 after it. The export still carries the column as Prisma renders a `DATE`, midnight UTC of the day. Tests: `extractions.test.ts` proposes on a Los Angeles job and a Tokyo job with the injected clock at an instant that is the 23rd in UTC and Tokyo and the 22nd in Los Angeles (ADR-0052's frame rule), refuses an instant, and reads back the date; `extraction-since.test.tsx` renders the confirmation form in both zones, asserts the pre-fill, and submits it through `confirmExtraction`, reading the instant sent back through ICU and not through `wall-clock.ts`. Both failed before the fix — the web one with `2026-09-21` in Los Angeles. The harness's fake agent proposes a date now. 752 API tests across 27 files and 114 web across 18; typecheck, `pnpm --filter web build` and `./scripts/helper-tests.sh` green. |
| 2026-09-23 | **Issue #157: the model a run uses is the one a person chose, and the deployment says which** (ADR-0065). `piAgentRunService` built every session with no model, so `@earendil-works/pi-coding-agent`'s `findInitialModel` chose: the Pi settings' default first — but a package's provider is registered while the session is being built, *after* that search, so a default naming `alibaba-plan` was skipped — and then the first credentialed provider in the SDK's own table. On `epmos-t1` that was `kimi-coding/kimi-for-coding`, a model nobody picked, reading client correspondence. The author chose **`alibaba-plan/deepseek-v4.1-flash`** from the plan on the machine's Pi account: a 1,048,576-token window with 16,384 of output, read off the provider's catalogue on the machine. It is **`AGENT_MODEL` in `fly.toml`** beside `AGENT=pi`, where the next deploy reads it, as `<provider>/<model>` split at the first slash so a model id may carry its own. **`AGENT=pi` without it does not boot** (`requireEnv`, the vendor adapters' posture), and a malformed value refuses with the value in the sentence. Each of the four runs calls **`pinModel`** once its session is built: by then the package's provider is registered in the `ModelRuntime` this product passed in, so the choice is looked up there and set; a model the deployment cannot find **fails the run** naming it, and nothing falls back to what the SDK picked. The mechanism was **read on the machine before it was written**: a probe built a session as `agent.ts` does and never prompted it — it resolved `kimi-for-coding`, found `deepseek-v4.1-flash` once the session existed, `setModel` accepted it (so the account has its key), and an unknown id came back as nothing. **ADR-0063's bound stays 300,000** by the author's choice: re-derived against the new window it would allow about 2,777,000 characters, so it is safe and conservative, keeps a run's cost where it was, and keeps its margin if the model is changed back to a smaller window — which is now the named trigger, a change to `AGENT_MODEL`. Six tests in `vendors.test.ts`'s *what a deployment selects*: no agent named is still the refusing default, an agent without a model and a model without a provider each refuse to boot, naming both builds the adapter, the split, and `pinModel` against stand-ins for the two SDK objects — pinning the named model and refusing an unknown one without setting anything. The SDK is still never loaded by a test (ADR-0040). |
| 2026-09-23 | **Issue #162: a provider error fails the run, and the model is one that answers** (dated lines on ADR-0065). The project chat on `epmos-t1` never replied after release v20: two runs read *finished* with no failure and wrote no turn. **Two defects, found on the machine.** First, `alibaba-plan/deepseek-v4.1-flash`, pinned by #161, refused every call — `400 … developer is not one of ['system', 'assistant', 'user', 'tool', 'function']`: the SDK sends a reasoning model's system prompt as the `developer` role unless the model's `compat` says otherwise, and detects DeepSeek's lack of it only on DeepSeek's own endpoint, which this plan's is not. The probe that pinned it in #161 built a session and never prompted, so it proved the pin and not the model. Second, and worse, **a provider error read as a finished run in all four run types**: `session.prompt` does not throw on one, the SDK appends an assistant message stopped on `error` and returns, so an extraction would have said *the agent found no correspondence here* about a document no model had read. `failIfTheModelFailed` now reads the **last** assistant message after every prompt — the last, because the SDK retries an error — and throws its sentence when it stopped on `error` or `aborted`, or on `length`, an answer cut off at the output limit that the SDK leaves in place after its one compact-and-retry (found by the second review), which lands on the row through the worker's existing catch. **Except once the run's proposal has landed** — its own proposal tool answered with a 2xx: the SDK calls the model again after every tool call, and failing a run over an error on that wrap-up turn would put *failed* beside a proposal already on the record (found by the first review). The model becomes **`alibaba-plan/qwen3.8-flash`**, the author's choice after a stand-in chat turn on the machine — the real prompt and tool list, a recording `call` that returned nothing and sent no job data — on four models: DeepSeek refused, `kimi-coding/kimi-for-coding` and Qwen 3.8 Flash answered through `assumption_record_propose`, and `qwen3.7-plus` answered in plain text without calling it, which loses the reply and is noted rather than fixed. Qwen 3.8 Flash has the same 1,048,576-token window with 65,536 of output; ADR-0063's arithmetic gives about 2,483,000 characters of room, so 300,000 still holds. One test in `vendors.test.ts` over stand-in messages: an `error` stop throws the provider's sentence, an `aborted` stop throws, a clean stop and a stop after a tool call do not, an error the SDK retried past does not, and neither does one after the run's proposal was answered 201 — while a proposal the route refused with a 409, another tool's success, and a tool that threw do not count as landed; a `length` stop throws too. 759 API tests across 27 files and 114 web across 18; typecheck, `pnpm --filter web build` and `./scripts/helper-tests.sh` green. |
| 2026-09-23 | **Issue #164: the project chat is the last thing on the job record, and its turns scroll in a capped list** (a dated line on ADR-0058; plate D-02 amended by the author). Issue #121 put the conversation **second** on the record, open, as plate D-02 drew it, and its turns grew without bound, so every question pushed resolved items, submissions, walks, registers and documents further down; the author, on the first day of using it: *"I don't like that chatbot taking space in the middle. Once it gets too long it will bury the things below."* Of three layouts offered — bottom and capped, collapsed in place, a side panel on desktop — the author took **bottom and capped**. The panel moves after Phases, which also puts *Resolved* directly under *Open items* again, where its comment always said it sat. On a project the turns are a `max-h-[28rem]` list that scrolls, and **`flex-col-reverse` on the scroller opens it at the newest turn with no script**, so the panel stays a server component and the turns stay in its first paint (ADR-0028); the typing bar is outside the scroller and never scrolls away. **The walk's conversation is unchanged**: there it is the capture record the engineer works down, and capping it is a question nobody asked. One test in `desk-screens.test.tsx` — the conversation is the record's last child, its list sits in a capped `overflow-y-auto` `flex-col-reverse` scroller, and the typing bar is outside it — which failed before the move; one in `walk-screen.test.tsx` holding the walk's list uncapped. `.claude/rules/web.md` says both, and that #121's measured heights predate the cap. |
| 2026-09-23 | **Issue #65: a photograph added in error is removed, while its walk is open** (ADR-0066, narrowing one sentence each of ADR-0031, 0032 and 0034). A photograph added in error was *"un-addable-away"* (ADR-0032) — the wrong afternoon off a camera roll, a receipt, a person — on a walk whose report prints evidence under the author's professional name. The author chose a **real delete, narrowly** on 2026-09-05 and **while the walk is open** on 2026-09-23: the walk's end is a one-way stamp already on the record (ADR-0030), and an ended walk is the one whose report goes out, so past it the record would stop agreeing with the document it produced — a report rendered *during* the walk keeps what it printed, as every rendering does (ADR-0035), and refusing once any report exists was the stricter reading, not taken — a fixed time window was the alternative and is refused. **`DELETE /v1/photos/:id`** removes the row under a `FOR UPDATE` lock on the walk's row — so an end already in flight is waited for and then seen, which a filter on the delete alone, a plain read under READ COMMITTED, would not (found by review) — in the same transaction as an audit line that **carries what the row said** — name, floor, what it evidenced, when taken and on which walk, the times in the building's zone — because the line is the only thing that survives; the **bytes go after**, outside the transaction, through the new `ObjectStore.delete` (idempotent, so a half-finished removal can finish; a store that fails there is logged and the removal still answers 204, the record being right), which is ADR-0032's store-then-row order reversed for the reverse act. An ended walk answers **409** and writes nothing; a removal that lost a race answers 404. Nothing cascades: nothing in the schema points *at* a photograph, checked before the ADR said so. A finding the photograph was the only evidence for is back on the walk's *issues without photos* with nothing else written, its evidence being derived (ADR-0056). On the walk it is **Remove this photograph**, behind a closed disclosure on the photo's row and only while the walk is open — *delete* is struck under **Archived** in the glossary, and there is no dialog; the button is a 44 px field target that wraps a long filename. **A document added in error is not answered** — ADR-0039 defers it in the same words, and a document version is not a leaf. The submission detach route's comment called itself *the one route that deletes a row* and now says it is one of two. Three API tests (removed while open, with the bytes gone, the finding back on the list and the line saying what the row said; refused after the walk ends with nothing written; a 404), `PATCH`/`PUT` still 404, and one web test (offered behind a closed disclosure while open, a field target, absent once ended). 761 API tests across 27 files and 117 web across 18; typecheck, `pnpm --filter web build` and `./scripts/helper-tests.sh` green. |
