# Project Context: Engineer Project Management OS

*Last updated: 2026-09-01*

## What this is

This repo is the code workspace for the Engineer Project Management OS, an internal operations dashboard for engineering projects with a Pi AI copilot.

## Source of truth

All planning documentation lives in the Obsidian vault. This vault location is the single source of truth and MUST be kept current:

```
/home/augusto/Obsidian Notes/Projects/Engineer Project Management OS/
├── PRD and Architecture.md   ← requirements, architecture, milestones, backlog
├── docs/
│   ├── adr/                  ← architecture decisions 0001-0039 (check Status lines)
│   └── glossary.md           ← domain terms
```

**Update rules:**
- When a milestone is completed, update the progress section below and mark it in `PRD and Architecture.md`.
- Any plan adjustment, scope change, or new decision gets documented here AND in the vault (as an ADR if architectural).
- Do not treat this file or AGENTS.md as the plan. The vault files are authoritative.

## Key decisions (from vault ADRs)

Forty-four ADRs (0020 was Proposed until 2026-09-01 and is now Accepted, confirmed by the author and built as slice 21; 0021 and 0022 were decided while building slice 1, 0023 while building slice 2, 0024 while building slice 3, 0025 by the author after slice 3, 0026 while building slice 4, 0027 while building slice 5, 0028 while building slice 6, 0029 while building slice 7, 0030 while building slice 8, 0031 while building slice 9, 0032 while building slice 10, 0033 afterwards as its own change, 0034 while building slice 11, 0035 while building slice 12, 0036 while building slice 13, 0037 while building slice 14, 0038 while building slice 15, 0039 while building slice 16, 0040 while building slice 17, 0042 while building slice 18, 0043 while building slice 19, and 0044 while building slice 20; slice 21 wrote no new ADR, confirming 0020 instead and recording what building it settled inside that ADR). The 2026-08-24 grilling session overturned several 2026-08-17 decisions
that rested on a false premise. Read [[docs/adr/README]] in the vault for current status;
do not treat 0001-0011 as current without checking.

**Outcome test:** every deliverable I issue states what it rests on, with nothing sitting
in my court past its clock as the daily layer under it.

| # | Decision |
|---|----------|
| 0012 | Single-user personal tool (supersedes 0001 tenancy, 0005 auth) |
| 0013 | Cloud extraction default, local fallback (qualifies 0008) |
| 0014 | Open item is the central record |
| 0015 | Submissions first-class, may be provisional |
| 0016 | Exposure + clock replace the health score (supersedes 0007) |
| 0017 | No money model (strikes budget from 0006, 0010) |
| 0018 | Capture and extraction first, copilot later (narrows 0004) |
| 0019 | No vector search; retrieval by identity |
| 0020 | Single shared secret at the edge — **Proposed**, closes the gap between 0003 and 0012 |
| 0002 | Pi SDK behind `AgentRunService` port — required by the author |
| 0003 | Cloud-managed deployment |
| 0009 | Email forward-to-ingest |
| 0011 | Risk register: manual + agent-proposed |
| 0021 | Fastify, not NestJS — decided by slice 1, not revisited |
| 0022 | Time source is a port called `TimeSource`; `Clock` stays a domain word |
| 0023 | API routes carry a `/v1` prefix — settles the spec's "versioned prefix" against slice 1's unversioned routes |
| 0024 | An open item's `unresolved` column, `resolved_at` + `resolution_note` as the whole of resolution, and `subject_type` as a one-value enum |
| 0025 | The interface is designed, not incidental — Tailwind + shadcn/ui, owned in-repo; the site visit report is a separate print problem |
| 0026 | What a submission rests on is a join table, not a second subject on the open item; a phase is a row the submission points at, and renaming it propagates |
| 0027 | Provisional is two columns and no more; detaching is narrowed to what was attached after the issuance; exposure is a list whose length is the count |
| 0028 | Superseded is a successor existing, derived and never stored; `supersedes_id` is unique, which is the whole of "at most one successor"; exposure counts only the current issuance |
| 0029 | A captured block goes in verbatim and a line of it is how an entry is addressed; counterfactuals are rows, one per assumed input; a record is bound to one submission and nothing edits it |
| 0030 | A location is four components with the grammar rendered on read; exactly one of side or sector, refused at the boundary and by a CHECK; the floor is free text; a walk exists before it is over and its date is derived from its start |
| 0031 | An issue's identifier is a high-water mark on the project, never reused and never renumbered; an issue owns no content — no summary, no location — and the sightings are its history; an open item on a finding is a join whose subject stays `PROJECT`; the category is text with a CHECK, not an enum |
| 0032 | The filename grammar is written down at last — a marker and a number, never a bare integer; a binding is stamped when the photograph is added and corrected in one action, not derived; exactly one floor window binds or nothing does; the bytes go to an `ObjectStore` port and are served through the API, not a presigned URL |
| 0033 | A record type is a file under `apps/api/src/routes/`, named to match the test file that drives it; `http.ts`, `refusals.ts` and `wire.ts` are leaves that import no route module, which is what stops the records that read each other forming a cycle; the route modules are plain functions and not plugins |
| 0034 | A draft is a record of its own, never a state of an observation; the transcript is verbatim and a correction never rewrites it; the state is four stamps with no status column beside them; transcription is the first thing on the queue; a resend after a signal drop is answered with the row rather than refused |
| 0035 | A report is a record of a rendering — nothing edits one and generating again is another row, so there is no retry route; its state is four stamps and it owns nothing it prints; an issue's identifier prints as `Issue N` and a report prints this walk's sightings, the two questions ADR-0031 deferred to issue #13 by name; the renderer is deliberately not behind a port, because a port defers a pick and a browser engine is not one |
| 0036 | Ball-in-court is a history and not a field, because an arithmetic reads it — the entry has no status column and no `ball_in_court` column, and *ball-in-court* is the last handoff derived on every read; whether the ball is ours is a stored boolean beside the party's name and never a reading of it; both registers are written with the project and there is no route that creates one; the register kind is a database enum, reversing 0031's run for the reason 0031 gave; the link to the issuance that responded is a column on the entry, because a column on the submission would need the update route 0026 made impossible |
| 0037 | The clock is elapsed in-court time summed from the handoff history and stored nowhere — `clock_started` is refused, because the ball reaches us more than once and there is no single moment a clock began; *past its clock* is three facts and one of them is that the ball is ours now, which is how a disposition takes an entry off the list without stopping anything; recording a disposition writes the outcome and hands the ball back in one transaction, and the party is supplied rather than read off the entry's fixed cast; the turnaround target is a duration in whole days, never a date, and is set once; the clock is a list whose length is the count, as exposure is; a next round is a new entry pointing backwards and inherits nothing |
| 0038 | The morning screen serves **no endpoint**: a payload carrying both counts is the first object a score could be computed from without adding a query, so the screen reads the two lists it already links to and renders their lengths. `/` is the landing view re-headed, not a route of its own, with the project list a section beneath the two counts. Both cards render at zero, unlike the project screen's strips, because here the count *is* the screen |
| 0039 | A **referenced file** is a column on a document and not the sketch's third table — the glossary calls one *a document*, so a second record would give "is this one?" two answers; it is required with no default, because a default classifies by omission and the omitted answer puts an 86-sheet set in front of extraction. `documents` is the identity and `document_versions` holds the file, departing from 0028's one-table chain because two versions share their whole identity where two links of a supersede chain share nothing; `revision` unique within its document is the whole of "a new version never overwrites a prior one". A submission's sheet list points at a **version** through a join written after the issuance, so the text column stands and 0026's per-sheet migration is still priced rather than taken. Extraction targets are a **read**, so the exclusion is one predicate rather than a `where` clause in a worker that does not exist. Not gated on employer consent: nothing here reads a document's contents |
| 0040 | Memory is versions on the project with no identity table — a project has exactly one memory, so the project is the identity; the proposal **is** the approval request, with its base snapshotted so the diff cannot drift, and is never edited — accept-with-edit writes the version and keeps the agent's words. A run is a row with four stamps, one proposal at most, and no retry: asking again is another row. The budget (4,000 characters) is surfaced and never enforced. The tools call the internal API over HTTP and never the database, their names take underscores because provider APIs reject dots, and the session's tool list is an allowlist — bash, edit and write are absent, not denied. The default adapter refuses and says so; `AGENT=pi` builds the real one. The audit is scoped to memory and append-only by construction |
| 0041 | The memory agent gets **no file tools at all**, amending 0040's tool-list section. The allowlist half of 0040 was verified and stands; the claim that `read`, `grep`, `find` and `ls` were "scoped by `cwd`" was wrong — the SDK's `resolvePath` uses `cwd` only as the base for a *relative* path, returns an absolute path as given and expands `~`, and none of the four tools carries a containment check, so a run could have read the SDK's own credential store and put it in a proposal. A containment check was refused as this product re-implementing a guarantee inside a vendor's path resolution; a memory run reads no file, so the tools are removed rather than fenced. The rule it leaves: a sandbox claimed for a vendor's tool is not a sandbox until the vendor's resolver has been read |
| 0042 | The ingest address is built and the mail provider is **not**: the port has no adapter, the default refuses, so nothing leaves the process and the employer-consent gate moves from a note in the vault to a fact about the code — it now fires on writing the adapter and naming the vendor. 0042 also prices, for the first time anywhere, that an inbound-parse provider holds the **whole message** and is a stronger consent case than the OCR API 0008 and 0013 scope the trade-off to. An arrival is **not a document**: `referenced_file`, `revision` and the title have no answer when a message lands, and inventing them is what 0039 refused. Its content type is free text where a document version's is a closed three, because refusing a `.dwg` loses the record the manual fallback protects — and the served-under-our-origin hole that opens is closed at the read, where the bytes route answers `application/octet-stream` always. `arrived_at` is stamped and the sender's `Date` header is never read. The rate limit is a count of the rows in the trailing hour, not a counter beside them |
| 0043 | Extraction is **one record** that runs, proposes and resolves — the run's stamps, the proposal's fields and the resolution on `register_entry_extractions`, the state derived and no status column. The source is **exactly one** of an arrival's file or a document version (a CHECK holds it), and the enqueue is **manual and per file**, narrowing story 84's "automatically". The OCR vendor sits behind a port whose default refuses, and the extraction agent's default refuses too — the gate kept a second time, inside the built feature, so a run fails honestly with the vendor's sentence. The agent's one tool is `extraction_propose`; the content reaches it as delimited untrusted data under an explicit directive, and the proposal is constrained to the typed shape. **Confirming is the commit**: one transaction writes the document and version (on the mail path, reusing the arrival's storage key), the register entry, its first handoff and the join. Rejecting keeps the source as it arrived. The review is against the OCR text, not a rendering — the honest version of side-by-side, since arrival bytes are deliberately served as attachments. Referenced sheets are deliberately not extracted, priced for a later ticket |

## Stack

TypeScript monorepo · Next.js · Node.js API (Fastify, ADR-0021) · PostgreSQL + Prisma · Redis + BullMQ · S3 object storage · `@earendil-works/pi-coding-agent` SDK

## Milestones and progress

The five-phase plan is superseded by the revised sequence in `PRD and Architecture.md`.

| Step | Scope | Status |
|------|-------|--------|
| 0 | Walking skeleton and test harness (issue #2) | **Done** 2026-08-24 |
| 0b | `Project` record: create, list live, view, archive (issue #3) | **Done** 2026-08-25 |
| 0c | Open items + the pending items view (issue #4) | **Done** 2026-08-25 |
| 0d | Submissions, per-project phases, sheet list, revision (issue #5) | **Done** 2026-08-25 |
| 0e | Provisional state and exposure (issue #6) | **Done** 2026-08-25 |
| 0f | Reissue and supersede (issue #7) | **Done** 2026-08-25 |
| 0g | Assumption records (issue #8) | **Done** 2026-08-27 |
| 0h | Site visits and observations (issue #9) | **Done** 2026-08-27 |
| 0i | Issues with stable per-project identifiers (issue #10) | **Done** 2026-08-27 |
| 0j | Photo binning by timestamp and filename (issue #11) | **Done** 2026-08-28 |
| 0k | Voice capture to draft observation (issue #12) | **Done** 2026-08-29 |
| 0l | The site visit report, rendered to PDF (issue #13) | **Done** 2026-08-29 |
| 0m | Registers, entries and the ball-in-court history (issue #14) | **Done** 2026-08-31 |
| 0n | The clock, dispositions and the past-its-clock view (issue #15) | **Done** 2026-08-31 |
| 0o | The morning screen: exposure and clock as the landing view (issue #16) | **Done** 2026-08-31 |
| 0p | Referenced files: documents, immutable versions, retrieval by identity (issue #17) | **Done** 2026-09-01 |
| 0q | Project memory: versions, proposals, agent runs and the audit (issue #18) | **Done** 2026-09-01 |
| 0r | The ingest address and untrusted inbound mail (issue #19) | **Done** 2026-09-01 |
| 0s | Extraction to a draft, human-confirmed (issue #20) | **Done** 2026-09-01 |
| 0s | Processing location per project (issue #21) | **Done** 2026-09-01 |
| 1 | T-1 open items entered | Unblocked — no code needed |
| 2 | Open items + submissions (provisional, supersede) | **Done** — issues #4, #5, #6, #7 |
| 3 | Site visit capture (voice, photos, stable issue IDs, the report) | **Done** — issues #9, #10, #11, #12 and #13 |
| 4 | Registers: submittals, RFIs, clock, dispositions | **Done** — issues #14 and #15 |
| 5 | Ingest: forward-to-email, extraction, human-confirmed | Referenced files (issue #17, outside the gate), the ingest address (issue #19), extraction (issue #20) and the processing location (issue #21) all done inside the gate without lifting it; what remains is #22 (the edge gate) and the OCR adapter's vendor pick |
| 6 | Curated project memory | **Done** — issue #18 |

## Open decisions (deferred to implementation)

- Fly vs Render (hosting)
- Textract vs Google Document AI (OCR)
- Object storage (S3 vs R2 vs B2) — added 2026-08-28; it was on no list here or in the vault
  though the stack has said "S3-compatible" since ADR-0003. Slice 10 put it behind the
  `ObjectStore` port with a filesystem adapter, so the pick is deferred, not absent.
- SES vs Postmark vs SendGrid (email ingest)
- Transcription vendor for voice-to-observation capture (step 3) — added 2026-08-24, none named in the plan.
  Slice 11 put it behind a `Transcriber` port (ADR-0034) with **no adapter written**: the default refuses and
  says so. A step short of where object storage stands, because a filesystem is a real place to put bytes and
  there is no offline stand-in for understanding speech.

~~Clerk vs Auth0 (OIDC provider)~~ — struck 2026-08-24, dead under ADR-0012. Access control
is ADR-0020's single edge secret, which is a gate, not an identity provider.

**Two contradictions open in the vault**, recorded in `docs/adr/README.md`, neither resolved:
ADR-0013 vs the glossary on the processing-location default; and ADR-0010 / 0011 / 0006
reading Accepted while appearing in neither the MVP workflow list nor the sequence above.

## Change log

| Date | Change |
|------|--------|
| 2026-09-01 | Slice 17 built (issue #18): **project memory** — the curated prose per project, versioned, with the agent proposing edits and the engineer confirming, and `AgentRunService` standing up the Pi port ADR-0002 requires. The sixth record type built on thin prior authority: the PRD sketch's `project_memory` line and three glossary entries written for the deferred copilot. ADR-0040 records what it settled: there is **no identity table** — a project has exactly one memory, so the project is the identity and the versions hang off it directly, where `documents` needed the split because a job has many; the proposal **is** the approval request, refusing the sketch's `approval_requests` as a second record of the same question, with the base content snapshotted at the write so the diff under review cannot drift as memory moves underneath it (ADR-0027's snapshot shape); the proposal is never edited — accept-with-edit writes the *version* from the engineer's text and keeps the agent's words on the proposal, ADR-0034's two-facts transcript posture for a second record; a run is a row with the four stamps and no kind column, produces at most one proposal (`run_id` unique), and has **no retry route** for ADR-0035's reason — every input is still in the database, so asking again is another row; the **size budget** (4,000 characters) rides on every memory read and is surfaced with a meter that pushes back, never enforced, because what is worth keeping is the engineer's judgement; the domain tools call the internal API over HTTP and never the database, registered with **underscored names** because the provider APIs reject the PRD's dotted spelling (`memory.propose_edit` survives only as the display label), and the session's tool list is an **allowlist**, so bash, edit and write are absent by construction rather than denied, with the remaining file tools scoped by `cwd` to a per-project workspace; the SDK is imported lazily so no test process ever loads Pi, the default adapter refuses with "no model provider is configured" (the `Transcriber` posture — there is no offline stand-in for a model), and `AGENT=pi` builds the real one; and every memory mutation writes an `audit_entries` row in the same transaction, append-only by construction and scoped to memory — widening it is its own change. The acceptance criterion "tests substitute `AgentRunService` and assert no proposal commits without confirmation" is the test named for it: the run finishes, the proposal is pending, and the memory is exactly what the engineer last wrote. 472 tests across 17 files, up from 452. Verified in the browser end to end — write directly, ask (fails honestly with no provider configured), a proposal seeded through the tool's own route showing as a diff, edit-and-accept committing the corrected text with the agent's words still standing, the audit growing by one line per mutation — because neither `pnpm typecheck` nor `pnpm test` sees the frontend, and `pnpm --filter web build` is still failing on the pre-existing `components/ui/field.tsx` errors recorded in slice 12's note, which this slice also leaves alone. The agent-browser CLI's synthetic clicks did not trigger React state updates reliably during verification; programmatic `.click()` and `requestSubmit()` did, and the flows themselves are confirmed working. |
| 2026-08-17 | Planning complete. PRD, architecture, ADRs, and glossary finalized. |
| 2026-08-18 | Code workspace created. Development not yet started. |
| 2026-08-24 | Grilling session: 8 superseding ADRs (0012-0019), glossary rewritten to real field vocabulary, MVP resequenced. Money model, health score, RBAC, and vector search cut. |
| 2026-08-24 | Slice 1 built (issue #2): pnpm monorepo, Fastify API, Next.js frontend, PostgreSQL + Prisma migrations, Redis + BullMQ idle, and the test harness every later slice copies. ADR-0021 (Fastify) and ADR-0022 (`TimeSource`) recorded; the PRD stack line no longer says "Fastify or NestJS". |
| 2026-08-25 | Slice 3 built (issue #4): the **open item** — create against a project, resolve with a note and a date, reopen, and the cross-project **pending items** view, filterable by who owes the next move and sorted by age. ADR-0024 records the three things the PRD's data-model sketch left out: a column for what is unresolved, a resolution model, and `subject_type` as a one-value enum. Glossary gained **Counterfactual**, **Waiting on**, **Owner**, **Resolved** and **Pending items**, none of which had an entry. |
| 2026-08-25 | Slice 2 built (issue #3): the `Project` record — create, list live, view one, archive one-way — behind a `/v1` prefix (ADR-0023). `skeleton_records` dropped, as slice 1 said it should be. Glossary gained **Project number** and **Archived**, which had no entry anywhere, and the stale **Project** and **Milestone** definitions were corrected. |
| 2026-08-24 | Full-MVP spec written and published as GitHub issue #1 (`ready-for-agent`), covering all 7 workflows and all 6 sequence steps. Vault updated with what the spec decided: ADR-0020 (Proposed), provisional split into historical + derived, ball-in-court as a history, report renders to PDF, transcription vendor added to the open picks. Two vault contradictions surfaced and recorded, not resolved. |
| 2026-08-25 | Slice 6 built (issue #7): **reissue and supersede**. Correcting an issuance records a new submission carrying `supersedes_id` and writes nothing at all to the one it replaces, so "no path edits an issued submission" holds because no such route exists — a test asserts `PATCH`, `PUT` and `DELETE` on a submission are refused with the record unchanged after. ADR-0028 records the three things left open: *superseded* is derived from a successor existing rather than stored, which settles the spec's "never edited beyond being marked superseded" against ADR-0026 in the vault's favour; "at most one successor, and the chain is linear" is a unique constraint on the column rather than a guard; and exposure excludes superseded ancestors, a filter ADR-0027 did not authorise and which carry-forward makes necessary. What the superseded set rested on comes forward by default and is editable before committing, with the successor's own snapshot stamped fresh. Glossary **Reissue** gained what was built; **Submission** and **Exposure** were amended. A frontend bug found by loading the page: a state update made from a ref during the hydration commit is discarded, so the "going out on N unresolved items" warning claimed nothing while two boxes sat ticked. |
| 2026-08-25 | Slice 5 built (issue #6): **provisional state** and **exposure**. Provisional is two facts and the record now keeps both — `issued_provisional` stamped at the moment of issuance and never recomputed, and *currently provisional* derived on every read and stored nowhere — so resolving everything a set rested on takes it out of exposure without unsaying that it went out on unconfirmed inputs. ADR-0027 records the three things neither the sketch nor ADR-0026 settled: the snapshot is one nullable boolean on `submission_open_items` carrying three real states, not two columns admitting a meaningless fourth; detaching is narrowed to items attached after the issuance, which settles the collision ADR-0026 flagged for this ticket rather than moving the snapshot off the table; and exposure is a list whose length is the count, so clicking a count lands on exactly the records it counted and there is no figure in the payload to combine into a score (ADR-0016). Archived projects leave the across-every-project count and keep their own, following the glossary's line under **Pending items**. Glossary **Provisional** and **Exposure** both gained what was built. |
| 2026-08-25 | Slice 4 built (issue #5): the **submission** — a dated issuance to a named recipient at a phase, with sheet list and revision — and per-project **phases**, defined, renamed and reordered as free text, with a current phase a new submission defaults to. ADR-0026 records the two things the sketch left out: what an issuance rests on is a `submission_open_items` join rather than a second subject on the open item, because reading ADR-0024's enum expectation literally would have made issue #7's carry-forward destroy the record of what the original rested on; and a phase is a foreign key, so a rename propagates on purpose. No route updates a submission, so issue #7's edit prohibition holds by construction. Glossary gained **Current phase**, **Sheet list**, **Revision** and **Rests on**. |
| 2026-08-27 | Slice 7 built (issue #8): **assumption records** — the `ASSUMPTIONS` and `FLAGS / VERIFY` blocks captured verbatim from a helper skill, with the code edition and the date, bound to the submission they justified. The first record type no ADR said anything about: the whole prior authority was two glossary sentences and the five-word sketch `assumption_records (assumptions, flags, counterfactuals, code_edition, dated)`, which has no submission foreign key at all. ADR-0029 records the four things it left open: the blocks are text columns holding exactly what was pasted and an entry of either is addressed by its **line number**, because splitting them into rows at capture is the transcription by hand the record type exists to avoid and the sigils the three calculators print are their convention rather than a contract; `counterfactuals` is rows keyed by the line they are about, because the spec asks for one per assumed input and one field cannot say which input each belongs to; a record is bound to one submission and nothing edits or deletes one, so a rerun is another record dated its own day; and a flag raised as an open item is attached *after* the issuance, which makes the set currently provisional and puts it into exposure while leaving `issued_provisional` exactly as it was stamped. Glossary **Assumption record** gained what was built and had "from a calculation" reconciled to the PRD's "from a helper skill"; **Counterfactual** gained its cardinality on this record. Verified in the browser end to end, because neither `pnpm typecheck` nor `pnpm test` sees the frontend. |
| 2026-08-27 | Slice 8 built (issue #9): **site visits and observations** — one dated observation event against a building, with a start, a nullable end and a per-floor schedule, producing observations that carry what was observed, when, and where. The second record type no ADR said anything about: the prior authority was four glossary sentences and the two-word sketch `site_visits`, `observations`, with no columns at all, and the location grammar written down in exactly one place in the whole vault. ADR-0030 records the four things it left open: the location is four columns with `Floor N — <qualifier>, <Side|Sector>` rendered on every read, because a single string would dissolve two independent axes into one; exactly one of side or sector is set, with *neither* refused as well as both since the grammar has no optional segment, enforced at the boundary and again by a CHECK constraint; the floor is free text holding the designation without the word, because buildings have B1, M and PH that an integer could not record; and a walk exists before it is over, so the end is nullable and the visit's date is the day of its start, derived. Nothing makes an observation a finding — no status, no category, no promotion route — so the non-issues table stays the default path and issue #10 will point at an observation rather than write a column on it; a test asserts the exact key set so it cannot be added quietly. Glossary **Site visit**, **Observation** and **Location** all gained what was built. Review caught the column named `note`, a word the glossary's `_Avoid_` list strikes for this record, with a comment above it quoting the rule it broke; renamed to `observed` before it reached a deployed database. Verified in the browser end to end, twice, because neither `pnpm typecheck` nor `pnpm test` sees the frontend. |
| 2026-08-27 | Slice 9 built (issue #10): **issues with stable per-project identifiers** — an observation becomes a finding carrying a category from the closed set of five, an identifier scoped to the job, every later sighting of it, the open items being chased for it, and a close that takes the note that closed it. The third record type no ADR said anything about: the whole prior authority was one glossary sentence and the five-word sketch `issues (stable per-project ID, category, location)`, and the glossary defines **Issue** twice in two incompatible senses, the field-finding one being what the MVP spec resolves it to. ADR-0031 records the five things it left open: the identifier comes off `projects.issues_allocated`, a **high-water mark and not a count**, read and incremented inside the transaction that writes the issue, because `MAX(number) + 1` and `COUNT(*) + 1` both hand the same number out twice the moment a row goes away while "never reused or renumbered" is the property being promised — the counter is stripped from the project on the wire and a test asserts the exact key set, since what a project's issues *are* is the list whose length is the count, the shape ADR-0027 gave exposure; an issue **owns no content**, so the `location` the sketch named on it and the summary nobody asked for are both absent and what was seen, when and where is read through the sightings, one re-observed on three walks having three of them; a sighting is a row with `observation_id` unique, which stops a double tap burning an identifier that can never be given back, and those rows are the whole of the history rather than a per-visit status, which would be the `tasks` record the data model excludes arriving by the back door; closing is `closed_at` plus `closure_note` moving together, ADR-0024's shape and for its reasons, with reopening clearing both and a second close refused rather than repeated; and an open item raised on a finding is the `issue_open_items` join with the item's subject still `PROJECT`, which **overrules ADR-0030's prediction** that story 69 would need a second value in `OpenItemSubject` — under that reading the item would reach the pending items view with no job beside it, the opposite of what the story asks, and nothing in that view had to change for it to work. The category is text with a CHECK naming the five rather than a database enum, because a Prisma enum member cannot be named `Physical / Safety` and the real words would then live in a lookup in the API and again in the frontend. Nothing deletes an issue and no route renumbers one; a test asserts `PATCH`, `PUT` and `DELETE` on one are all 404. Glossary **Issue** gained what was built; the duplicate agent-system **Issue** entry and the sketch's `issues_log` beside `risks` are left unreconciled, as ADR-0031 records — whoever settles ADR-0011 has to find another name or another table. Review caught three things the hand pass had not thought to walk: `issues_allocated` reaching the wire from the one project-returning route that did not strip it, a bad issue number in a URL rendering an error page where every other bad URL renders a 404, and a finding closed since a walk still reading as open on that walk. All three fixed, and the helper that strips the counter renamed `projectOnTheWire` — it is the only one in `server.ts` that removes a field where `withDate`, `withLocation` and `withSightings` all add one, and reading as a converter is part of why the miss was easy. Verified in the browser end to end — record as an issue, re-observe from a later walk, raise an open item that then appears in the pending items view carrying its job, close with a backdated date, reopen — because neither `pnpm typecheck` nor `pnpm test` sees the frontend. |
| 2026-08-28 | Slice 10 built (issue #11): **photo binning** — a photograph added to a walk, binned to a floor by its timestamp against the per-floor schedule and bound to a finding by the filename grammar already in use, either, both or neither, and both corrected in one action. The fourth record type no ADR said anything about, and the first whose blocker was not a design question but **a fact about the world nobody had written down**: ADR-0031, the glossary and the schema's own doc comment all recorded, in the same words, that the filename grammar was described in ADR-0018 as "already in use" and written down nowhere, and all three refused to invent it. It was asked for and supplied, and ADR-0032 is where it now lives — `issue` or `iss`, then the number, separated by a hyphen, an underscore, a space or nothing. **A marker is required and a bare integer never counts**, which is the load-bearing half: those filenames carry the floor as well as the finding, so `3-west stair-issue-12.jpg` opens with a bare `3`, and reading any integer as an identifier would bind every photograph taken on floor 3 to issue 3 — a mechanism wrong about most of a hundred photographs is worse than one that binds none, because then all hundred have to be checked. Six more things ADR-0032 settled: a binding is a **column stamped when the photograph is added**, breaking this product's own derive-on-read reflex, because a derived value has nowhere to keep a correction and a floor time fixed the next morning would silently move photographs between floors; a photograph binds to a floor only if **exactly one** window contains its timestamp, which extends the ticket's "outside every window is left unbound rather than guessed at" to the two-window case the schedule actually produces when an engineer doubles back, with an uncompleted floor's window staying open so the last floor of a walk still bins; `taken_at` is required and never falls back to the injected clock, since that fallback is the same guess under another name, and nothing reads EXIF; photo evidence lands on the **floor and the finding and not on the observation**, correcting a promise the glossary's **Observation** entry had carried since slice 8, because no mechanism picks one observation out of the dozen made on a floor; the bytes go to an `ObjectStore` port — the shape ADR-0022 gave `TimeSource` — with a filesystem adapter now and S3 when there is somewhere to deploy to, and are served **through the API rather than a presigned URL**, which would be a second thing reachable without ADR-0020's still-Proposed edge gate; and binning runs in the request rather than on BullMQ, a deliberate drift from the PRD diagram and the spec stack line, because it is date comparison and one regular expression. Glossary gained **Photo** and **Photo evidence**, and **Observation** was corrected. Object storage was on no open-vendor list anywhere despite the stack saying "S3-compatible" since ADR-0003; it is on both now. Review caught five things the hand pass had not reached, three of which the first browser pass *could not* have reached because it used a 116-byte PNG: the Next byte proxy interpolated a caller-controlled path segment raw, so `/photos/..%2Fexposure%23/bytes` returned the exposure register — an open GET proxy for every route on an API deliberately bound to loopback, reproduced at 200 and 3994 bytes and reproduced again against the fix; the form could not add a real photograph at all, because a server action's body defaults to one megabyte, and raising it was only half the fix since the whole selection went in one body and a hundred photographs would have been one 300 MB request, now 16mb and one request per file; the grammar missed the underscore names it claims to accept, `\b` counting `_` as a word character so `photo_issue_4.jpg` bound to nothing; a filename number above int32 — `ISS-20260723131500.jpg`, an ordinary messaging-app name — 500'd the add and **lost the photograph**, the opposite of "left unbound rather than guessed at" in the one place that rule is load-bearing; and a zero-byte file in a selection shifted every later timestamp by one, binning photographs to the wrong floor and returning 201. Four quality fixes with them: the object store's `put` moved out of the transaction that writes the row, since a network write inside an interactive transaction blows Prisma's five-second timeout and rolls back a row whose object already stored; `FilesystemObjectStore` resolves its root, a trailing slash in `OBJECT_STORE_DIR` having made the guard refuse every write to its own store; a browser that reports no type for a HEIC gets it from the extension; and the duplicated read shape became one named const. Two findings were deliberately not fixed and are recorded in ADR-0032: the floor select is narrower than the column it writes, offering the walk's scheduled and observed floors and no free-text box; and `ObjectStore.get()` lets an ENOENT out raw, the one error shape in the slice that is not this product's `{message}`. Verified in the browser end to end — three photographs added at once, binding to both, to a floor only and to a finding only, one re-binned from a single select change, then re-verified after the fixes with real 11.11 MiB and 9.53 MiB JPEGs, which is what the first pass could not reach — because neither `pnpm typecheck` nor `pnpm test` sees the frontend. |
| 2026-08-29 | Slice 11 built (issue #12): **voice capture** — the engineer speaks instead of typing, the audio goes to object storage, a queued job asks a transcription vendor what was said, and what comes back is a **draft** read and corrected before an observation exists. The fifth record type no ADR said anything about, and the one with the thinnest prior authority of any of them: the glossary had no entry for Voice capture, Transcript, Draft, Audio or Progress, and the data model sketch named **no table for it at all** — not a thin line, no line. ADR-0034 records five decisions. The **draft is a record of its own and not a state of an observation**: the ticket's own words read as a column on `observations`, which ADR-0030 forbids and built a test to keep forbidding, so committing writes an ordinary observation and stamps `observation_id` on the capture — slice 9's shape, where promotion writes nothing at all to the row it points at. The **transcript is verbatim and a correction never rewrites it**: the corrected words are the observation's and both facts are kept, which is what makes story 52's "transcription error never becomes record error" checkable afterwards rather than merely asserted; nothing parses a transcript, which is ADR-0029's posture toward a calculation's output and ADR-0032's toward EXIF, and it is why `AgentRunService` is not in this slice despite ADR-0004 and the PRD's `observations.create_draft` putting it here. The **state is four stamps and no status column**, derived on read, for the reason `resolved_at` and `closed_at` have none, with a retry clearing the failure the way reopening an issue clears its close. **This is the first thing that ever enqueues** — BullMQ has been wired and idle since slice 1, and ADR-0032's "it is date comparison and one regular expression" does not carry to a vendor call of unbounded duration; that sentence is now spent rather than a general licence. The worker runs in the API's process from the same injected dependencies, so the harness runs a real worker over the real Redis instead of a second copy of the wiring. And a **resend after a signal drop is answered with the row rather than refused**, the one place this slice departs from its own precedent on purpose: a photograph's duplicate filename is refused because there the refusal *is* the answer, but a refusal here cannot tell the phone whether the first attempt landed, so it would keep the recording forever or throw one away — and story 112 is about not losing one. The phone writes every recording to IndexedDB **before** sending and deletes it only once the API answers, retrying on load and on `online`. Progress is the state over SSE and never a percentage, polled from PostgreSQL rather than carried on a second transport, on a `reply.hijack()` route with no Fastify plugin so ADR-0023's single `register` call stays the only place a prefix could be added. "Leaves the audio recoverable" is three things and the third is load-bearing: the bytes stay in the store and are served through the API, retry queues it again, and **a failed capture can still be committed by hand** — a vendor that never answered must not stop the walk being written up. The vendor is still an open pick and sits behind a `Transcriber` port with **no adapter written**: the default refuses and says so, so a development run exercises the failure path honestly, and a labelled stub behind `TRANSCRIBER=stub` exists only so the review screen is reachable and is recorded in ADR-0034 as the weakest thing in the slice. `observationBodySchema` and `observationData` are exported from `routes/site-visits.ts` and used by both writers, which is what ADR-0030 asked for when it predicted this route and named the guard a later writer forgets; `ObservationFields` is shared by the typed form and the draft form for the same reason, with per-instance field ids because a walk with three drafts renders four copies at once. Verified in the browser end to end at `localhost` — recorded through a real `MediaRecorder` (232 KB of WebM, the microphone hardware stubbed by an oscillator the way a fake capture device would), sent, transcribed, the draft appearing without a reload, corrected and committed as `Floor 3 — Stair B, Side A` dated from the recording rather than the review; then a second recording against the refusing default, which read as failed with the vendor's own sentence, whose 84 KB of audio still came back through the proxy, which offered the draft form anyway, and which recovered to *awaiting review* live when asked again with a vendor present. The new byte proxy was checked against ADR-0032's open-GET-proxy hole and 404s a traversal. **Two things are recorded as gaps, not done**: `getUserMedia` needs a secure context, so recording does not work on a phone over `http://<address>:3000` and story 111 is not real until there is a deployed instance or a tunnel — the screen says so rather than failing silently; and the thin browser-driven pass the MVP spec authorises for exactly this path is still not stood up, so recording and poor-signal reconciliation are exercised at the record level and by hand, not by a test — which also means **there is no longer a later ticket to carry that pass**, and README's claim that only #12 was left to carry it was corrected rather than quietly left standing. Review found six more things, four of them defects the hand pass could not have reached. A capture stuck at *queued* was a dead end on the screen: the retry route accepts one and its own comment names that case — Redis has no volume and `queue.add` can throw after the row is written — but "Ask again" was offered only where there was a failure, so the one recovery the route exists for was unreachable, and the phone had already dropped its copy. A recording the API will always refuse — a type outside the closed three, a body over the cap — was held in IndexedDB and resent on every load and every `online` for the life of the device, with a banner that never cleared: story 112 asks capture to *reconcile*, and that never reconciled, so a 4xx now releases the recording and shows the message while anything else keeps holding it. The worker could write a second transcript over the first, defeating in fact the refusal the retry route makes in words, since two taps while queued enqueue two jobs; it is a compare-and-set now, and a job for a recording already answered returns without asking the vendor a second time. The SSE heartbeat could write after the response had ended — `preClose` ended the socket but left the timers running — which is `'error'` on a `ServerResponse` with no listener and takes the process down, so the hook now holds each stream's *stop* rather than its reply. And two narrower ones: the recorder invented `audio/webm` for a browser that reported no type, which would have stored audio as something it is not and served it back under `nosniff` as a recording that silently would not play — the empty string goes up instead and the API refuses it, which is the photo form's answer to a browser that cannot name a HEIC; and the record button was re-entrant, so a double tap in the window before `getUserMedia` resolved opened two microphone streams and left the first one's tracks live. Four quality fixes with them: the duplicated capture lookup in the add route became one helper, the four-state union is read through two named predicates rather than switched on in six places, the web proxy segment was renamed `captures` to `voice-captures` to match what the record is called everywhere else, and CONTEXT.md's own decisions table turned out never to have listed ADR-0033. Four findings were **not** acted on and are recorded as deliberate: the retry route, the stub adapter, the shared `ObservationFields` and `wall-clock.ts` were each flagged as scope beyond the ticket, and each stays — the retry route is what "leaves the audio recoverable" is made of, the stub is what makes the review screen reachable before a vendor exists, and the two shared pieces exist because the alternative was a second copy of the one-axis form and a second copy of the timezone helper, which is the drift ADR-0030 predicted by name. |
| 2026-08-29 | `apps/api/src/server.ts` split, as its own change between issues #11 and #12 rather than inside a slice. Four ADRs had recorded the deferral and grown the figure in it — 1,745 lines across seven record types in ADR-0029, then 2,228, then 2,779, then 3,246 across fourteen in ADR-0032 — each for the same good reason, that splitting inside a feature slice mixes two changes and a reviewer then cannot tell which one a failing test is about. ADR-0033 records the shape. A record type is a file under `apps/api/src/routes/`, named to match the test file that drives it, holding its schemas and its single-use helpers beside its routes; the alternative of splitting by layer was refused because it puts the schema, the route and the derivation three files apart and the reason they belong together survives in none of them. The obstacle was never size but that the records genuinely read each other — a site visit returns its photographs, a photograph is corrected against a walk and is evidence of a finding, a finding is read with the location of every sighting — which is `site-visits → photos → issues → site-visits`, and not the harmless kind, since `photosTaken` is a `const` reading `issueInclude` at module load. Three leaves resolve it: `http.ts` (the validation vocabulary, the one way a Prisma error is read, `RouteDependencies`), `refusals.ts` (every way the API says no) and `wire.ts` (only the read shapes two or more records return — `withDerivedState` and `withLines` are used by one each and stayed put). The route modules are plain functions and not Fastify plugins, because nine plugins would be nine more places a prefix could be added and ADR-0023's single-`register` guarantee would become a convention. Verified as a move rather than a rewrite: `tsc` passes, 282 tests pass, `app.printRoutes()` is byte-identical before and after, and a normalised line-multiset diff accounts for every line of the original — six are absent, the old combined Fastify import and a five-line banner explaining why the photo helpers sat above the issue helpers *in that file*. `server.ts` is now 76 lines; thirteen files, 3,476 lines, largest 728. Nothing about the API changed, so `apps/web` is untouched and there is no migration. Review found no cycle, no initialisation hazard and a clean dependency seam, and six things worth acting on: two single-record refusals (`alreadySuperseded`, `endsBeforeItStarted`) had been argued into `refusals.ts` by the change that wrote the warning against exactly that, and moved back; three section banners had ridden to the end of the *preceding* record's module, each now announcing a section in another file, and were deleted; and three new header claims were false — "one per test file" (there is no `phases.test.ts` and submissions has three), `http.ts` crediting itself with `wire.ts`'s acyclicity, and "every way this API says no, in one place" when thirty-two inline 4xx bodies remain. The one recommendation **refused** was hoisting the open-item schemas into `http.ts` to remove the two cross-module load-time schema reads: the argument for it was that one fails silently by handing ajv `properties: undefined`, and that is not what happens — a `const` in an ESM cycle raises `ReferenceError` at load, verified against the exact shape in both load orders, so the hazard is loud and a comment at each read naming the sink constraint is the proportionate answer. |
| 2026-08-29 | Slice 12 built (issue #13): the **site visit report** — a walk generated into the PDF that is issued outside the tool, with the metadata block, the per-floor schedule, the notable observations and every finding sighted on that walk, its stable identifier, its category and its photographs. `site_visit_reports`, `routes/reports.ts`, `report.ts` (the walk as HTML), `pdf.ts` (Chrome through puppeteer), `stream.ts` (a new shared leaf), and three routes: `POST /v1/site-visits/:id/reports`, `GET /v1/site-visit-reports/:id/pdf`, `GET /v1/site-visits/:id/reports/stream`. **Step 3 of the revised sequence is complete with it.** ADR-0035 records the decisions. A report is a record of a **rendering** and not a document kept current: nothing edits one, and generating again INSERTs another row — ADR-0028's reissue shape and ADR-0029's rerun shape arriving for a third record — which is why there is **no retry route** at all, the one place this slice departs from the voice capture it otherwise copies. A recording is retried in place because its audio is irreplaceable and the phone has already let go of it; a report's every input is still in the database, so a second attempt is a second row and the first keeps saying what happened to it. That is also the answer to story 66: a finding read as having no photograph gets one, and the report is generated again. Its state is **four stamps** derived on read — `rendering_since`, `rendered_at` + `storage_key`, `failed_at` + `failure`, queued being all four null — the fourth record asked for a status column and the fourth to refuse (ADR-0024, ADR-0031, ADR-0034), and nothing here is ever cleared, since clearing is what a retry in place needs. A report **owns nothing it prints**: metadata, schedule, observations and findings are all read at the moment of rendering and copied into no column, so it cannot come to disagree with the record it is a rendering of. Two questions ADR-0031 had deferred to this ticket **by name** are now answered: an issue's identifier prints as **`Issue N`**, the record's name and the integer, which invents nothing — it is ADR-0030's floor rule, where the column holds `3` and the render supplies the word, and it is what the filename grammar already carries in as `issue-7`, with `T-1-007` refused as ADR-0031 refused `T-12-003`; and a report prints **this walk's sightings, all of them**, the same `where` clause `GET /v1/site-visits/:id/issues-without-photos` already uses, which is ADR-0032's reasoning about evidence applied to locations ("July's photograph does not evidence August's re-observation"). The renderer is deliberately **not behind a port**, which departs from ADR-0022, ADR-0032 and ADR-0034: each of those ports defers a pick no test can exercise — a bucket that does not exist, a vendor account nobody has chosen — and Chrome is not a pick, needing no account, no key, no network and no per-call cost, with puppeteer pinning its own. A port would defer nothing and would cost the acceptance test its subject, since the ticket requires asserting on the resulting *document*; the test for a port is whether the thing behind it is a pick. Rendering is **on BullMQ**, ADR-0034's case rather than ADR-0032's, as one queue and a second job name dispatched on `job.name` inside `buildWorker`, concurrency still 1 — one browser printing at a time is the right number on one machine, and BullMQ is no longer carrying a single kind of job. `stream.ts` is a **new leaf**: ADR-0033 said a thing used by one record moves out when a second reaches for it, and a walk's reports are that second reader; the 24 voice tests pass unchanged against it. `storage_key` is **nullable**, inverting ADR-0032's bytes-before-row order because the queue now sits between the row and the document — the invariant itself is untouched, the key being written in the same statement as `rendered_at` and after the object is stored. There is no `content_type` column: a report is always `application/pdf`, and a column holding one value forever is a place for it to one day hold another. Three things the document itself taught, none of them guessable from the screen: **`letter-spacing` above about a tenth of an em destroys a PDF's text layer**, because Chrome emits every glyph as its own text run and a tracked-out heading prints as `N O TA B L E …`, unsearchable and uncopyable in the one artifact this product issues outside itself — measured, breaking at `0.11em` and fine at `0.09em`; photographs are inlined as **data URIs**, since the renderer must load one string and a linked `<img>` would need the API reachable from inside the process serving it, and are bounded to 70mm tall or a portrait phone photograph is a page each; and every value printed is **HTML-escaped**, this being the one place where what the engineer spoke becomes markup. `startTestApi({ worker: false })` substitutes nothing — it starts no worker, which is a state production has too — and is how *queued* became a state a test can stand in and look at. 324 tests pass across 13 files, up from 306. New dependencies in `apps/api`: `puppeteer` 25.9.0, with `pnpm-workspace.yaml` allowing its postinstall under `onlyBuiltDependencies` so it downloads its own Chrome, and `pdfjs-dist` 6.3.289 as a devDependency used only to read the rendered PDF's text back in the test. **Recorded honestly: `pnpm typecheck` and `pnpm --filter web build` were already failing before this slice**, on `apps/web/components/ui/field.tsx` — unused shadcn scaffolding from the Tailwind slice that no longer typechecks now that two `@types/react` resolve (19.2.2 pinned, 19.2.18 through radix's peer). Nothing imports it. Verified pre-existing by checking out a pristine tree and running `pnpm install --frozen-lockfile && pnpm -r typecheck`; not fixed here, because it is not this slice's change. It does mean this slice's frontend was verified by loading the pages against `pnpm dev` rather than by a production build. |
| 2026-09-01 | Review of slice 17 (issue #18), then a fix branch for what it found. The slice itself is sound — all eleven acceptance criteria met bar one, 472 tests passing, ADR-0040 and the vault genuinely updated. Two things were wrong and are fixed here. **The agent's file tools were never sandboxed**: ADR-0040 kept `read`, `grep`, `find` and `ls` and said `cwd` scoped them to a per-project workspace, but the SDK's own `resolvePath` uses the base directory only for a *relative* path — executed against the real resolver, `~/.pi/agent/auth.json` resolves to the SDK's credential store and `../../apps/api/.env` walks out to the repository, with no containment check in any of the four tools. A run could have read either and passed it to `memory_propose_edit`, where it would be stored, rendered and committed to memory on accept. The tools are removed rather than fenced, because a memory run reads no file and every fact it needs already arrives over HTTP; ADR-0041 records it. Latent rather than live — `AGENT` is unset, so no run has ever reached the adapter. **And slice 17's migration silently downgraded four foreign keys** from `ON DELETE RESTRICT` to `ON DELETE SET NULL` — `submissions.supersedes_id`, `photos.issue_id`, `register_entries.submission_id`, `register_entries.previous_round_id` — none of which that slice had any business touching. Prisma defaults an *optional* relation to `SetNull` and those four carry no explicit `onDelete`, so `migrate dev` rewrote the SQL to match the schema; the same commit added a doc comment asserting "every foreign key in this schema is RESTRICT on delete", which it had just made false, and slice 11 had warned about this exact hazard by name. Restored explicitly in the schema and in migration `20260901023535_restore_restrict_on_delete`. A full sweep of `pg_constraint` found **five** non-RESTRICT keys, not four: `projects.current_phase_id` was created `SET NULL` in its own migration and is deliberate, so its behaviour is untouched and the doc comment was narrowed to tell the truth instead. Three further findings from the review are recorded and **not** fixed here, being memory's own and not security: accept commits against its snapshotted base with no check that memory has not moved since, so a direct write between proposal and accept is silently dropped while the screen shows a diff against the older base; `reject` has no compare-and-set, so a concurrent accept and reject leave both stamps set and two contradictory lines in the append-only audit; and `readMemory` breaks a `created_at` tie on a random uuid, so "the current memory is the latest" is a coin flip for two versions written in the same millisecond, which the harness's frozen clock makes reachable. |
| 2026-09-01 | Slice 18 built (issue #19): **the ingest address and untrusted inbound mail** — change-log entry restored, the slice's own session having recorded the milestone row, the 0042 decision row and the vault updates but missed this table. See the 0042 row above for what it settled; ADR-0042 is the record. |
| 2026-09-01 | Slice 21 built (issue #22): **the edge gate** — one long-lived shared secret in front of every route, and the whole of this deployment's access control. This is the first slice whose **first acceptance criterion was a decision rather than code**: [[0020-single-secret-edge-gate]] had been *Proposed* since the MVP spec was written and the ticket refused to be built until the author confirmed it. Confirmed as written; the ADR is now Accepted and carries a new section recording what building it settled, which is where slices 18-20 would have written a new ADR. ADR-0012 removed **identity** and could not remove **access control** — no `users`, `roles` or `permissions` table is added here, and none is needed. **The ADR says *the* edge and this deployment has two**, so `apps/web` and `apps/api` each carry a gate against the same secret; a gate on one leaves the other reachable. The engineer's presentation is the **cookie** the ADR names, in `apps/web/proxy.ts` — Next 16 deprecated `middleware.ts` and renamed the convention — with `/unlock` taking the secret once and setting an HttpOnly, SameSite=Lax, year-long cookie. The API's is the `x-edge-secret` **header**, and that is not a second mechanism for one fact: a cookie is how a *browser* carries a credential and no browser reaches the API, which is the same invariant ADR-0032 refused a presigned URL over. Both compare `timingSafeEqual` over SHA-256 digests, so neither the comparison nor a length mismatch is a side channel. `EDGE_SECRET` is **required with no default** — `buildServer` takes it the way it takes `objectStore`, and a default is a way to run ungated by accident; `apps/api` guards it with the same `requireEnv` as `DATABASE_URL` and `apps/web` throws in `next.config.ts`, the only thing Next loads before it serves. Both refusals were verified by booting each app with the value emptied. **Three decisions went against the grain and are recorded rather than smoothed over.** `GET /v1/health` is **gated**, though a managed platform's HTTP check cannot present a header (Fly can be configured to; Render cannot) — one named exception is a property a test can hold and two is the start of a list, so a deployment uses a TCP check or configures the header, and the README says so. `/unlock` is exempted **inside the proxy function and never in the matcher**, because Next's own docs warn that a server action is a POST to the route it is used on, so a matcher exclusion silently un-gates that route's actions too. And a request without the cookie is redirected **only if it is a page navigation**; everything else gets a 401 where it stands, because an `EventSource` follows a redirect into an HTML page and then reconnects forever without ever showing anybody an error. The test is **exhaustive rather than representative**: the harness collects every route Fastify registers through an `onRoute` hook, and the sweep walks all **142** and asserts the allowed set is exactly `POST /v1/ingest/inbound-mail` — so a route added in a later slice is covered without anybody remembering. **Proven by removing the hook and watching 141 of the 142 answer.** 559 tests across 20 files, up from 553. Verified live in the browser: locked out of `/`, redirected to `/unlock?next=%2F`, a wrong secret refused in place, the right one unlocking and landing on the morning screen with live API data, and both SSE streams on a project page connecting through the gate with a clean console. Verified against the running API by curl: five routes 401 without the secret, a wrong and a truncated secret both 401, the ingest webhook reaching its own route, and a real `_next/static` chunk served at 200. **Not done, and recorded:** `pnpm --filter web build` still fails only on the pre-existing `components/ui/field.tsx` errors. Its cause is now diagnosed — prisma resolves `@types/react@19.2.18` as a **peer**, beside the 19.2.2 `apps/web` pins, so tsc reads the ambient `React` namespace from one copy and `import from 'react'` from the other. A pnpm `overrides` entry does not fix it, because overrides do not rewrite peer resolutions; that attempt was made, found not to work, and reverted rather than left as noise on this branch. The frontend was verified against `next dev` in a browser instead, which is what this repo has done since slice 12. Review (two-axis, /code-review) found **one blocking defect, and both axes found the same one**: of `apps/web`'s twenty-five outbound calls, twenty-four got the secret and `setProcessingLocation` did not — so slice 20's whole screen answered 401, and **nothing in the repo could have caught it**, since the API's sweep only tests anonymous callers and `apps/web` has no tests at all. It was found because the standards axis enumerated the call sites rather than trusting the pattern the original edit had grepped for; the missed one wrapped its URL onto its own line. Fixed, and then **the class was closed rather than the instance**: `apiFetch` is now the only thing that reaches the API, and `apiPath` is reduced to returning a *path and not a URL* so a second door cannot be built out of it — this product's own habit of holding an invariant by construction, applied where it had not been. Also fixed: the open-redirect guard on `/unlock?next=` refused `//elsewhere.example` but let `/\elsewhere.example` through, because `URL` normalises a backslash to a slash, so it now **resolves** rather than prefix-tests; two AGENTS.md rules the diff had made stale (`server.ts` "the boundary and nothing else" still listed three things, and the leaves list omitted `edge-gate.ts`); a route count written into prose that would go stale on the next route; and the harness handing out its live route array. **Found from a review note rather than a finding, and closed:** `pnpm dev` copies `.env.example` to `.env`, so a deployment that shipped the working tree would have run on the in-source development secret while passing every check — both apps now refuse that value when `NODE_ENV` is production, and the ADR records that `apps/web`'s half always fires (Next sets it) while `apps/api`'s only fires if the deployment does. **One judgement call declined and priced:** the header name and the refusal sentence are duplicated across the two apps where `refusals.ts` would ordinarily make them one — they are two packages with different module resolution, so sharing means a third workspace package, and a header-name drift is the loudest failure available rather than a silent one. Independently verified during review, against the running servers: `/_next/image` is **not** a bypass (its internal fetch of the exempt `/unlock` reached the app; its fetch of the gated `/pending` never did), RSC payload requests and server-action POSTs are refused 401, and `/unlock` renders no record data. The processing-location screen was re-driven in the browser after the fix, both directions, with the sign-off cleared and re-recorded. |
| 2026-09-01 | Slice 20 built (issue #21): **the processing location per project**, landing **inside the employer-consent gate without lifting it** for the fourth slice running. ADR-0044 records the decisions. It opens by **resolving the contradiction the vault has carried under "Open, deliberately" since 2026-08-24** — the ticket's own seventh acceptance criterion — in **ADR-0013's favour, on the author's explicit call**: cloud is the default and local is the per-project second option. Three things support it and are now written down. Decisively, ADR-0008 line 3 names *its* qualifier and it names 0013, while the glossary's `Processing location` entry claimed that same role in the same sentence and in the opposite direction — two documents qualifying 0008 and disagreeing, which nobody had noticed until this slice. The PRD agreed with 0013 twice already (lines 85 and 151), so the glossary was the outlier. And 0013's grounds — work machines cannot run local models — have not changed. A **third** formulation was found at PRD line 129, "the processing location agreed with the firm", which is neither default and circular besides; it is corrected too. The glossary entry is rewritten with the struck sentence preserved in 0044 rather than deleted, and 0013 needed no change and got none. **What the resolution cost is recorded rather than smoothed over**: under the glossary's reading "a project on cloud has a recorded sign-off" would have been a CHECK, and it cannot be one now, because a project reaches `CLOUD` by being created and the invariant is false by design. `processing_location` is a **database enum** on ADR-0036's test rather than ADR-0031's, both members naming themselves; the written CHECK holds the pairing alone (`reference IS NULL = at IS NULL`). **`LOCAL` is a permission and not a selector** — there is no local OCR and `OcrProvider` takes no project, so it means the port is never called and manual entry is the path; a two-adapter selector with one adapter was refused. The sign-off therefore gates the **switch** and not the state: switching to cloud requires the reference and the date and refuses a second, recording the first against a never-switched project is not a second, and **switching to local requires nothing and is always available** — consent can be withdrawn, which keeps ADR-0039's principle while inverting its mechanism. Going local **clears** the sign-off so returning needs a fresh one, safe only because the audit entry carries the reference and the date. `cloud_signoff_at` is supplied and never the `TimeSource`'s (ADR-0037's answer for `disposed_at`); when the switch happened is the audit row's. **The gate runs twice and both matter**, ADR-0042's rate-limit shape for a second reason: both extraction create routes refuse the ask before a row or a job exists, and the worker reads the setting again before the bytes are fetched — the half that is a bound, for the one case the routes cannot cover, a job already in Redis when consent is withdrawn. The **audit widens exactly once**, the change ADR-0043 said would be its own; extraction mutations still write nothing. 552 tests across 19 files, up from 508 — and **both halves of the gate were proven by deleting each and watching a named test fail**, the worker half driven through the real race rather than a mock of it (concurrency is 1, so holding the first run open at the agent seam keeps the second queued while the switch lands underneath it, with a call-counting OCR port standing in for a claim about a call that did not happen). Verified live in the browser and against the running API: default cloud, sign-off recorded through the form and shown, switched to local with the sign-off cleared from the project and standing in the audit, and the extraction refused 409 with the exact sentence. `pnpm --filter web build` still fails only on the pre-existing `components/ui/field.tsx` errors — an unused component, imported by nothing, that has broken the build since slice 12 and is left alone again. Review (two-axis, /code-review) found **one hard standard violation and one consequential spec finding, both fixed**. The standards axis caught that three documents — AGENTS.md, ADR-0044 and the glossary — asserted switching to local was "always available" while the route 409s the no-op on a project already local; the prose is narrowed in all three and the over-claiming test renamed, since the refusal is right and the sentence was wrong. The spec axis caught the sharper one: **ADR-0044 counted "the other six criteria are met" when criterion 2 is met only literally.** "Switching a project to cloud requires a recorded sign-off" has force exactly because local was the default; under a cloud default no project need ever be switched, so cloud processing with no recorded consent is the ordinary case — and this slice's own worker test demonstrates it, creating a project, recording nothing and watching bytes cross the port. The ADR now says that instead of counting it, which makes its risk paragraph and its tally agree. Also fixed: the document path refused for the wrong reason (the location 409 preceded the referenced-file 409, so a referenced file on a local job was told about the location); the sign-off form was twenty-five duplicated lines in two branches, now one `SignoffForm`; an untyped `let data` carrying three branches on the same discriminant, now one typed `LocationChange` built where the branch already knows it; the web action sent an explicit `null` where every sibling composer sends `undefined`; and `refusals.ts`'s header still claimed "more than one route module" and an inline count of thirty-two. One suggestion declined and recorded: routing the two extraction 409s through the `Refusal` shape would match the file's 404 idiom but not its 409s, and would make `worker.ts` read `.message` off a reply-shaped object. **Found independently of the review and fixed:** the route read the sign-off outside the transaction and wrote inside it, the read-then-write shape filed as issue #42 for memory — the write is now narrowed to the state each refusal was read against, as the archive route two above narrows its own. **Recorded rather than claimed:** that narrowing is unproven by this suite. A test firing twenty concurrent sign-offs pins the outcome (exactly one recorded, one audit row) but stays green with the narrowing removed, because these handlers never interleave between their read and their write in this harness; the test comment says so rather than implying a bound it does not demonstrate. Also recorded and not closed: a cleared sign-off survives only in the audit, which no path from the processing-location panel reaches — criterion 4 asks that the *current* setting be visible and it is, and a history view is its own change. |
| 2026-09-01 | Slice 19 built (issue #20): **extraction to a draft, human-confirmed** — the structured half of ingest, landing **inside the employer-consent gate without lifting it**, the pattern slice 18 established taken one step deeper. ADR-0043 records the decisions. An extraction is **one record** — `register_entry_extractions` carries the run's stamps, the proposal's thirteen fields and the resolution, with the seven states derived on every read and no status column, the shape a voice capture and a site visit report established. The source is **exactly one** of an arrival's file or a stored document's version, held by a CHECK so the ambiguity is not a value in a column, and the asking is **manual and per file** — a recorded narrowing of story 84's "automatically", because the engineer picks which file of an arrival is the correspondence and the covering note is not it. The run is a fourth BullMQ job dispatched on `job.name` at concurrency 1, and its ordering is what keeps the gate: the bytes are read, the OCR vendor is asked, its text is **stored on the row before the model sees it**, and only then does `AgentRunService.extractRegisterEntry` run — so a refusing OCR default fails the run honestly with the vendor's sentence and the row still says what happened. The agent's tool list is an allowlist naming `extraction_propose` and nothing else; the packet reaches the model inside `<<<UNTRUSTED DOCUMENT DATA` delimiters under an explicit non-instruction directive, so "ignore previous instructions" is data to read fields from, and the typed-shape constraint lives at the proposal route's body schema rather than in the prompt, which is a place a constraint can actually be held. **Confirming is the commit** and it commits everything at once: one transaction writes the document and its first version on the mail path (**reusing the arrival file's storage key** — the bytes are already in the store and a copy would be a second object nobody reads), the register entry, its first handoff and the join to the source version, with `confirmed_at` and `register_entry_id` stamped by compare-and-set so a double confirmation refuses. The register's boundary rules are re-stated rather than bypassed: an RFI still needs a question, a submittal still carries neither question nor response, and a number already in the register is still a conflict. **Rejecting keeps the source** exactly as it arrived and keeps the proposal on the record — the two facts the engineer will want when the same document comes back. The confirmation screen reviews against **what the agent read** — the OCR text and the envelope — rather than a rendering of the file, because arrival bytes are served as `application/octet-stream` attachments on purpose and this slice does not poke that hole; the honest version of story 86's "side by side", recorded as a narrowing. **Referenced sheets are deliberately not extracted** — the ticket's field list names them, the typed shape stops at the register entry's fields, and ADR-0043 records why: a sheet reference is a link to a document version, and resolving names against stored versions is its own ticket rather than a silently absent field. `OcrProvider` is the sixth port with a refusing default, and `OCR=stub` the labelled stub for the screen, never a real document. 539 tests across 19 files, up from 508 — 31 new ones drive the HTTP API only: the run states, the stream, the proposal's constraints, both confirm paths, reject, injection posture, the exact key sets, and `PATCH`/`PUT`/`DELETE` on an extraction all 404. Verified in the browser end to end: the honest failure path first (OCR stub ran, the agent's refusing default failed the run with "no model provider is configured"), then a pending proposal seeded through the tool's own real route (as slice 17's verification did), confirmed through the actual form with one field edited — the redirect landing on the entry carrying the corrected subject, the document created from the arrival's bytes, the clock running and the entry past it — and a second extraction rejected, its source document standing with no entry written. `pnpm --filter web build` still fails only on the pre-existing `components/ui/field.tsx` errors recorded since slice 12, which this slice also leaves alone. Review (two-axis, /code-review) found **no hard standard violations**; its two consequential findings are handled: a worker comment overstated the redelivery guard (a stalled-job redelivery *does* call the vendors again — that re-run is the only recovery path, there being no retry route — and the comment now says so), and the referenced-sheets narrowing was unrecorded and is now in ADR-0043. Three recorded as deliberate and unfixed: the enqueue's two-statement in-flight check is not a bound (a double-click degrades to one wasted vendor call, and the proposal and confirm compare-and-sets refuse the second); the confirm form gates the response field on the kind select, so re-kinding to a submittal drops a proposed response silently rather than tripping the boundary refusal; and `noSuchExtraction` lives in the refusals leaf though one record uses it, matching the existing `noSuch*` style over the leaf rule's letter. |
